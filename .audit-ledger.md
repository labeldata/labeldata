# V2 화면 감사 원장

> 매일 한 번 도는 감사가 여기에 기록을 덧붙인다. 지침은 `AUDIT_GUIDE.md`.
> **아직 안 본 화면을 고를 때 이 목록을 본다.**

## 한 바퀴를 다 돈 것이 **아니었다** — 2026-09-13 정정

앞서 이 자리에 "V2 화면 중 안 본 것이 없다" 고 적었다. **틀렸다.**

원장이 "안 본 화면" 목록을 들고 있었는데, 그 목록 자체를 누가 만들었는지
아무도 확인하지 않았다. 실제로 템플릿을 전수 조사해 보니 **한 번도 감사한
적 없는 화면이 열다섯 개 넘게** 있었다 — 로그인·가입·비밀번호 재설정 같은
**인증 화면 전체**, 초대 착지, 부적합 상세(독립 페이지), 팝업 세 종.

목록에서 지워 나가는 방식이 이렇게 무너진다. 그래서 **훑는 방식을 바꿨다** —
`AUDIT_GUIDE.md` 참고. 이제 화면 목록은 사람이 적어 두는 것이 아니라
`extends base_v2` 를 훑어 그때그때 만든다.

## 인계 — 2026-09-14 (이어서 할 사람에게)

이 메모는 **다른 자리에서 이어 받으려고** 남긴다. 앞선 작업은 로컬 세션에서
했고 그 대화는 이 저장소에 없다. 여기 적힌 것과 커밋 메시지가 전부다.

기준 커밋: `a297442`. 이 판에서 43개 커밋이 들어갔고 전부 `main` 에 있다.

### 돌리는 법

```bash
# 시험 (3,104건). 바깥 venv 에는 DRF·corsheaders·ratelimit 이 없다
./env/Scripts/python.exe manage.py test v1 --settings=v1.config.settings_test
./env/Scripts/python.exe manage.py check --settings=v1.config.settings_test
```

배포는 `git pull` → `migrate --plan` → `migrate` → `collectstatic --noinput`
→ Web 탭 **Reload**(README 의 절차).

**여기 적혀 있던 "migrate 는 운영에서 돌리지 않는다" 는 틀렸다** — 2026-09-15
정정. 그 제약은 `96c1b08`(2026-08-30)에서 이미 풀렸는데 원장·계획 문서·
`settings_test` 주석 셋이 낡은 채로 남아 그 뒤의 작업을 계속 막고 있었다.
지금은 서버·로컬 모두 `migrate --plan` 이 "No planned migration operations."
를 낸다. **모델을 고쳐도 된다.**

앱 저장소는 `labeldata-mobile`(별도). 배포 절차는 그쪽 `RELEASE.md`.

### 화면에서 아직 확인 안 된 것

배포 뒤 사람이 눌러 봐야 하는 것만 적는다. 시험으로 덮은 것은 뺐다.

**2026-09-14 에 다섯을 확인했다** — BOM 비고 칸 왕복 · 칸 고르기 일괄 적용 ·
곁판 닫힘 · 두 표의 글자 크기 연동, 그리고 **영양성분 성적서 [읽기]**(V-5).
남은 하나만 아래에 둔다.

| # | 어디서 | 무엇을 | 정상이면 |
|---|---|---|---|
| V-4 | 거래처 → 자료 요청 | 협력사에게 요청 → 제출 → 내 문서함 | 들어온다. 못 들어오면 **안내가 뜬다** |

V-4 는 계정 둘이 필요해서 미뤄 둔 것이다(요청하는 쪽과 내는 쪽). 이 판에서
유일하게 남은 화면 확인이다.

V-5 가 오래 열려 있었던 까닭은 **스낵바가 어느 V2 화면에서도 뜬 적이
없어서**였다(`15d455a` 에서 고쳤다). 판독 자체는 멀쩡했고, 되고 있다는 말을
화면이 못 했을 뿐이다 — 화면이 아무 말도 안 하면 "안 되는 것" 과 "된 줄
모르는 것" 을 구분할 수 없다는 사례로 남긴다.

### 판단이 필요한 것 (owner)

**① 링크 없는 화면 여섯** — 조사를 마쳤다. 근거는 아래 「링크 없는 화면」 절.

    지운다 : product_recent · product_starred · doc_requests · doc_requests_dashboard
    살린다 : expiring_documents · expired_documents

`product_recent` 삭제에는 `ProductAccessLog` 테이블 드롭 마이그레이션이 딸린다.
**막고 있던 것이 없어졌다** — 위의 정정 참고. 배포 때 `migrate` 가 돈다.

**② `EMAIL_HOST_PASSWORD` 가 소스에 박혀 있다.** `v1/config/settings.py:280`
의 `default=` 가 실제로 쓰이고 있고 git 이력에도 남아 있다. 순서:
① Gmail 앱 비밀번호 폐기·재발급 ② `.env` 에 넣기 ③ 소스에서 기본값 제거 +
`_PLACEHOLDER_SECRETS` 에 키 추가. **② 전에 ③ 을 하면 가입 인증 메일이 멈춘다.**

**③ `MOBILE_REQUIRE_AUTH`** — 이제 켤 수 있다(확인 완료). 앱이
`Authorization: Bearer` 를 싣고(`api_client.dart:73`), 401 이면
`POST /token/refresh/` 로 갱신하며(`:80`), 그 서버 경로가 이번 판에 생겼다.
비회원 기기는 영향 없다(`v1/mobile/views.py:119` 위에서 먼저 돌아간다).
남는 위험은 **계정 연결은 서버에 있는데 앱에 토큰이 없는 설치본** — 다시
로그인해야 한다. 앱 새 판을 올린 뒤 며칠 지켜보고 켜는 것을 권한다.

**④ 검증 탭의 성격** — **실제로 막고 있던 것은 풀었다(2026-09-15).** 구조는
그대로 두고 화면에서만 영양정보 표를 접는 길을 냈다. PDF 가 찍기 직전
`pv-exporting` 을 붙였다 떼는 장치가 이미 있어서(`label_preview.js:2559`,
`:2621`) 규칙 하나를 `:not(.pv-exporting)` 으로 얹으면 인쇄 경로가 보는 DOM 은
그대로다. 워드는 `labelRowData` 가 `box.style.display` 로 넣을지를 정하므로
**인라인 style 이 아니라 클래스로 감춘다** — 인라인이면 워드에서도 사라진다.

남은 것은 급하지 않은 구조 변경이다. 지금 PDF 는 `html2canvas` 로 찍어
`addImage` 하는 **그림**이라 글자를 복사·검색할 수 없다. 인쇄물을 데이터에서
다시 그리면 그것이 풀리고 검증 탭도 iframe 에서 떼어낼 수 있는데, 규제 문서라
기존 PDF 와 픽셀 대조가 필요하고 시험으로 덮이지 않는다. **그 요구가 생겼을
때 함께 한다.**

### 아직 안 고친 것 — 이번 판에서 새로 나왔다

- ~~「만료일 알림」 토글은 켜도 알림이 안 간다~~ · ~~탐색기의 「만료 임박」
  수가 5로 잘린다~~ — **2026-09-15 에 고쳤다.** 아래 「기록」 참고.
  남은 것은 **예약 등록**이다(사람이 Tasks 탭에서 한다).
- **`ProductAccessLog.get_recent_products()` 는 돈 적이 없다.**
  `product__deleted_yn` 로 거르는데 `Product` 는 `MyLabel` 의 별칭이고
  (`v1/products/models.py:12`) 거기 있는 이름은 `delete_YN` 이다 — FieldError.
  `deleted_yn` 은 **`ProductMetadata`** 의 칸이라 헷갈리기 쉽다.
  호출자가 0이라 아무도 몰랐다(위 ① 과 함께 정리된다).
- ~~`applyIngredientSelection` 이 여섯 칸을 덮어쓴다~~ — **의도로 확정했다
  (2026-09-15, owner).** 원료를 고르는 것은 "이 줄은 그 원료다" 라고 말하는
  일이므로 딸린 값이 따라온다. 이 줄에서만 다르게 쓰려면 고른 **뒤에** 고친다.
  결함처럼 보이는 동작이라 시험으로 잠갔다(`BomIngredientSelectionTests`).
  같이 고친 것 — "afterChange 를 건너뛴다" 던 주석은 **틀렸다**. `setRowProp`
  이 `source || 'edit'` 으로 넣어 afterChange 가 돌고, 그 경로로 표의 값이
  오른쪽 패널에 옮겨진다. 끊으면 저장 때 패널의 옛 값이 표를 도로 덮는다.
- **칸 순서 도우미가 9열을 가정한다.** `bom_detail.html:1005-1015` 와
  `sheet_paste.js:568-586` 이 `BOM_SHEET_HEADERS`(항상 9)를 돈다. 칸을
  감추면 `toVisualColumn` 이 `null` 을 돌려주는데 그대로 쓴다.
- **앱: 데이터 안전성 양식이 실제와 다르다.** `firebase_analytics` 를 뺐고
  안 쓰는 의존성 여덟을 걷어냈다. Play Console 에서 신고 내용을 고쳐야 한다.
- **앱: 계정 삭제 웹 주소가 없다.** 앱 안의 탈퇴 경로는 있지만 웹 화면이
  없다(모바일 API 만 있다). Play 가 아직 문제 삼지 않았다고 한다.

### 이번 판에서 배운 것 — 다음 사람이 같은 데 빠지지 않게

- **화면 증상은 브라우저에서 잰 값으로 짚는다.** 2단 배치가 깨진 것을 폭
  계산 쪽에서 네 번 고쳤는데 원인이 아니었다. 콘솔에서 열 너비와 칸 너비를
  한 번 재자 그 자리에서 갈렸다(열은 105/195, 칸은 21). 미리보기는 iframe
  안이라 콘솔 문맥을 그 프레임으로 옮기거나 `contentDocument` 로 들어가야
  한다.
- **인라인 style 은 스타일시트를 이긴다.** 스낵바가 어느 V2 화면에서도 뜬
  적이 없었던 까닭이고, 영양정보 표가 라벨 설정을 안 따르던 까닭이다.
- **시험이 제 주석에 걸린다.** 무엇을 왜 뺐는지 적어 두면 그 설명 안의
  낱말이 검사에 잡힌다. 이 판에서 세 번 겪었다 — 주석을 먼저 걷어내고 본다.
- **`_strip_js_noise` 는 문자열 속까지 비운다.** 그것으로 소스를 훑으면
  `'previewTableBody'` 도 `'none'` 도 사라져 무엇을 찾든 통과한다. 주석만
  지우는 도우미를 따로 쓴다(`v1/products/tests.py` 의 `_no_comments`).

## 마친 화면

| 날짜 | 화면 | 결과 |
|---|---|---|
| 2026-09-12 | products/_tab_permissions.html (권한 설정) | 18건 발견·수정 |
| 2026-09-12 | products/contacts.html (연락처 관리) | 18건 발견·수정 |
| 2026-09-12 | products/sharing/inbox.html (공동 작업) | 4건 발견·수정 |
| 2026-09-12 | products/_tab_documents.html (문서함) | 6건 발견·수정 |
| 2026-09-12 | products/doc_requests.html (자료 요청) | 도달 불가 화면으로 확인 |
| 2026-09-12 | vendor/upload_form.html (협력사 제출) | 재제출 허용으로 수정 |
| 2026-09-13 | label/my_ingredient_* (원료 목록·상세) | 20건 발견 / 전부 수정 |
| 2026-09-13 | label/food_item_list · food_additive_search · ocr_lab | 위 감사에 포함 |
| 2026-09-13 | regulatory/* (부적합·처분 알림) | 16건 발견 / 전부 수정 |
| 2026-09-13 | main/home_v2_dashboard · user_management/user_profile | 위 감사에 포함 |
| 2026-09-13 | products/bom_detail · documents/* · board/* | 30건 발견 / 전부 수정 |
| 2026-09-13 | products/product_explorer · _tab_label · product_starred | 2건 발견·수정 (스케줄 1회차) |
| 2026-09-13 | products/_tab_basic_info.html (기본 정보) | 9건 발견 / 전부 수정 |
| 2026-09-13 | products/documents/type_list.html (문서 타입) | 3건 발견 / 전부 수정 |
| 2026-09-13 | label/label_preview.html (미리보기) | 12건 발견 / 전부 수정 |
| 2026-09-13 | label/_condition_panel.html (조건 패널) | 4건 발견 / 전부 수정 |
| 2026-09-13 | user_management/* (로그인·가입·비밀번호 재설정·이메일 인증) | 계정 탈취 포함 수정 |
| 2026-09-13 | products/sharing/invite_landing.html (초대 착지) | 죽은 로그아웃 단추 수정 |
| 2026-09-13 | includes/_topbar_account.html (모든 V2 화면 상단바) | 저장형 XSS 수정 |
| 2026-09-13 | vendor/upload_submit (협력사 제출 서버 경로) | 파일 유실·무검사 수정 |
| 2026-09-13 | products/nutrition_editor · label/_ingredient_nutrition | 데이터 손상 수정 |
| 2026-09-13 | 모바일 API (v1/mobile) + Flutter 앱(labeldata-mobile) | 인증 부재 포함 수정 |

## 기록

### 2026-09-18 — 영양성분DB 의 이상 행을 찾는다

owner 제안: "배합비를 모르니 이상 데이터를 찾는 게 최선." 맞는 말이고, 필요한
것이 이미 다 있었다 — `PublicFoodNutrition.item_report_no` ↔
`FoodItem.prdlst_report_no` 로 붙고, `rawmtrl_nm_sorted` 가 **많이 쓴 순**으로
정렬돼 있다.

운영 실측: 적재 322,426 · 원재료를 붙인 행 188,058(58 %).

**규칙을 네 판에 걸쳐 깎았다.** 4,311 → 2,832 → 2,104. 깎은 것은 전부
`--sample` 로 **무엇이 걸렸는지 눈으로 보고서야** 알았다.

| 무엇이 틀렸나 | 어떻게 알았나 |
|---|---|
| 부피 기준을 무게 자로 쟀다(A4) | 숫자가 유난히 컸다(1,081) |
| 가짓수가 적으면 2순위가 미량이다(B3) | 뻥과자 표본 |
| 원물로 기름을 짜면 탄수화물 0 이 맞다(B4) | 옥수수유 표본 |
| 소금은 2순위여도 0.1 % 다 | 가짓수를 올려도 안 잡혔다 |

> 배운 것 — **건수로는 오탐인지 알 수 없다.** 1,081 을 보고 의심한 것은
> 맞았지만, 832 와 455 는 수가 비슷한데 하나는 전부 오탐이고 하나는 전부
> 진짜였다. 표본 세 줄이 그것을 갈랐다.

남은 2,104 건에서 되풀이되는 것: **미측정을 0 으로 채운 행**(탄수화물 0 인데
당류 30), **기준량 오기재**(포화지방 138 g/100 g). 둘 다 그 행을 원료 기본값
으로 쓰면 그대로 틀린 표가 나온다 — 잡는 것이 맞다.

B3 는 이제 **원재료 순서가 틀린 행**을 잡는다("우리밀치즈빵 | 소금, 글루텐,
효모, …" — 빵에 소금이 1순위일 리 없다). 의도한 것은 아니지만 값이 있다.

화면은 관리자 목록 하나(읽기 전용). 판정이지 진실이 아니라서 고치는 화면으로
두지 않았다.

마이그레이션 있음(label 0033). 시험 3,208건.


### 2026-09-17 (11) — 눌러도 아무 일이 없던 단추

새로 만든 출처 단추가 **눌리지 않았다**(owner). 화면에는 멀쩡히 있는데 눌러도
상태가 안 바뀐다.

까닭: `bindCalcSource()` 가 **두 자리에서 불린다** — 저장된 값을 불러온 뒤
(793)와 화면을 처음 꾸릴 때(1288). 그때마다 리스너를 달면 한 번 누른 것이
**두 번 처리된다.** 첫 번째가 'lab' 으로 켜고, 두 번째가 (이미 'lab' 인)
같은 값을 보고 꺼서 제자리로 돌아온다. 그래서 아무 일도 안 일어난 것처럼
보인다.

고르는 상자였을 때는 티가 나지 않았다 — 상태를 브라우저가 들고 있었고
리스너는 그것을 읽기만 했기 때문이다. **상태를 우리가 들기 시작하는 순간**
두 번 도는 것이 드러났다.

리스너는 한 번만 단다(`source.dataset.bound`). 그리고 단추마다 달지 않고
묶음에 한 번 단다 — 단추가 늘어도 그대로 돈다.

덤으로 딸려 나온 것: `fromSpecBtn`·`useBox`·오차/출처 입력의 리스너도 전부
**두 벌로 달려 있었다.** 성적서 창이 두 번 그려지고, 값을 고칠 때마다 계산이
두 번 돌고 있었다는 뜻이다. 같은 빗장 뒤로 들어갔다.

> 배운 것 — **두 번 부르는 함수는 두 번 불려도 되게 쓰여 있어야 한다.**
> 여기서는 "다시 칠하기" 와 "듣기 시작하기" 가 한 함수에 섞여 있었고,
> 앞의 것만 두 번 필요했다.

시험 3,104건(2건 늘었다).


### 2026-09-17 (10) — 회색 화면의 진짜 까닭, 그리고 출처를 단추로

**영양성분 탭에서 성적서 판독을 부르면 배경막만 깔리고 창이 안 보였다.**
앞 판에서 인스턴스 문제로 짚었는데, 그것도 사실이었지만 **이것이 진짜
까닭이다** —

문서함의 창들은 `_tab_documents.html` 에 적혀 있고, 그 파일은
**`<div class="tab-pane" id="tab-docs">` 안에** 들어간다. 그 탭이 안 보이는
동안에는 그 안의 모든 것이 `display:none` 이다. 창을 띄우면 배경막은
`body` 에 붙으니 깔리는데 **창 자체는 숨은 조상 밑이라 안 나온다.** 회색
화면으로 멈춘 것처럼 보이는 그 증상이다.

띄우기 직전에 `body` 로 옮긴다(`show.bs.modal`). 창은 화면 전체를 덮는
것이라 어느 탭에 속할 까닭이 없다. 옮겨도 되는지는 확인했다 — `#tab-docs .`
로 좁힌 스타일이 없다. 전제가 깨지면 알아채도록 시험으로 잠갔다.

> 배운 것 — **증상 하나에 까닭이 둘일 수 있다.** 인스턴스를 고치고 "됐다"
> 로 넘어갔는데, 그때 고친 것은 문서함 탭에서 열 때의 문제였고 영양성분
> 탭에서 여는 길은 그대로였다. 화면을 옮겨 가며 같은 단추를 눌러 봐야 한다.

**값의 출처를 단추 둘로 바꿨다**(owner). 고를 것이 둘뿐인데 상자를 열어
고르고 닫는 동작이 한 겹 더 있었다. 무엇을 고를 수 있는지가 **열어 보지
않아도** 보이고, 지금 무엇인지도 한눈에 보인다. "고르지 않음" 은 단추로 두지
않는다 — 둘 다 안 눌린 것이 그 상태이고, 누른 것을 다시 누르면 그리로
돌아간다(잘못 골랐을 때 되돌릴 길).

값은 숨은 칸이 들고 있다. 저장·판정·오차가 모두 그 id 를 읽기 때문이고,
단추는 그것을 비출 뿐이다 — 저장된 값을 불러올 때처럼 **값만 바뀌는 길**이
있어서, 단추가 제 상태를 따로 들면 둘이 어긋난다.

시험 3,102건(6건 늘었다).


### 2026-09-17 (9) — 한 줄에 한 항목, 그리고 왔던 자리로

**판독 창이 세로로 너무 길었다**(owner). 이름을 위에 칸을 아래에 두었더니
아홉 항목이 화면 두 개 높이가 됐다. 원본을 옆에 놓고 견주는 창인데 오른쪽이
길어지면 **왼쪽 성적서를 스크롤로 따라다녀야 한다** — 나란히 놓은 뜻이
없어진다. 이름·칸·단위를 한 줄에 둔다. 환산 전 값 같은 덧말만 아래로 접힌다.

**영양성분 탭에서 올렸으면 그리로 돌아온다.** 올린 뒤 문서함 탭으로 데려다
놓고 있었다 — 값을 넣고 나서 영양성분 탭을 다시 찾아 들어가야 한다. 문서를
올리러 온 사람은 목록에서 보고 싶을 테니 그쪽은 그대로 두고, **왔던 자리를
기억한 경우에만** 되돌린다.

시험 3,096건(4건 늘었다).


### 2026-09-17 (8) — 회색 막, 그리고 단추 하나

**창을 닫았는데 회색 막이 남아 화면이 멈췄다**(owner). 아무것도 안 눌리고
새로고침 말고는 할 수 있는 것이 없는 상태다.

까닭은 `openUploadModal` 이 부를 때마다 `new bootstrap.Modal(...)` 을 하고
있던 것이다. 이 함수는 **네 자리에서 불린다**(업로드 단추 · 슬롯의 + ·
끌어다 놓기 · 영양성분 탭의 성적서 첨부). 한 요소에 인스턴스가 여럿 붙으면
각자 배경막을 들고 있다가 하나만 걷힌다. `getOrCreateInstance` 로 바꿨다.

같은 증상으로 가는 길이 하나 더 있다 — **창이 닫히는 도중에 다음 창을
여는 것.** 성적서 고르기에서 "새로 올리기" 로 넘어가는 자리가 그렇다.
닫힌 뒤(`hidden.bs.modal`)에 연다.

그리고 이 화면에는 창이 열 개가 넘고 새로 붙을 때마다 같은 함정이 있으므로,
**마지막 창이 닫혔는데 막이 남아 있으면 걷는 그물**을 하나 뒀다.

**첨부와 불러오기를 한 단추로 합쳤다**(owner). 둘을 나란히 두면 누르기 전에
고르게 하는 꼴이다 — 문서함에 그 성적서가 있는지를 사용자가 먼저 기억해 내야
한다. 이제 「성적서에서 가져오기」 하나를 누르면 창이 둘을 함께 보여 준다:
있는 것 목록과 「새 성적서 올리기」. 하나도 없어도 창은 연다 — 거기서 바로
올리면 되므로 문서함 탭으로 보낼 까닭이 없다.

단추 모양도 오른쪽 미리보기의 스타일·표시기준 단추와 같게 맞췄다
(`.style-btn`). 「배합에서 계산」 도 같이 바꿨다 — 한 줄에 서는 것들이
서로 다르게 생길 까닭이 없다.

시험 3,092건(6건 늘었다).


### 2026-09-17 (7) — 기준을 재 보지 않고 정했다

(6)에서 넣은 `@container (min-width: 1000px)` 이 **아무 데도 안 걸렸다.**
owner 가 화면을 보고 "아직 7:3 같다" 고 했다.

까닭은 단순하다 — 이 설정칸이 실제로 받는 폭은 **800px 안팎**이다. 오른쪽
미리보기가 절반을 가져가기 때문이다. 창이 1650px 이어도 여기는 800px 다.
기준을 창이 아니라 이 칸으로 잡은 것까지는 맞았는데, **그 값을 재 보지 않고
1000 이라고 적었다.**

그리고 폭을 재 보니 **자리를 넓히는 것만으로는 안 됐다.** 한 줄에 서려면 ①
이 ~580px, ② 가 ~400px 인데 둘이 쓸 수 있는 것은 800px 다. 내용을 줄였다 —

    ① 입력칸 74→64px · 단위 62→54px · "1회 섭취참고량"→"섭취참고량"
    ② "이 값은" 제거(묶음 이름이 이미 그 말이다) · 고르는 상자 140→124px
       · 「성적서 첨부」→「첨부」

기준은 760px 로 내렸다. 그리고 모자랄 때 **접지 말고 조금 줄어들게** 했다 —
접히면 줄이 하나 늘고 그만큼 아래 표가 밀리는데, 입력칸이 몇 px 좁아지는
것은 티가 안 난다.

> 배운 것 — **화면을 못 보면 재기라도 해야 한다.** 스크린샷의 픽셀을 세면
> 800px 이 나왔고, 그것이면 첫 판에 맞출 수 있었다. 눈으로 볼 수 없다는 것이
> 값을 지어내도 된다는 뜻은 아니다.

시험 3,086건(2건 늘었다).

### 2026-09-17 (6) — 넓은 자리를 놀리지 않고, 올리는 것부터 그 자리에서

**① 은 한 줄, ② 는 두 줄.** 7:3 비율은 좁은 화면을 보고 정한 값인데 넓어져도
그대로라, 자리는 남는데 1회 섭취참고량이 둘째 줄로 떨어지고 단추가 셋째 줄로
떨어졌다. 창 크기가 아니라 **이 묶음이 실제로 받은 너비**로 판단한다
(`@container`) — 이 화면은 iframe 안이고 오른쪽 미리보기가 폭을 가져가므로
창 크기로 재면 "넓은 창 + 좁은 설정칸" 을 넓다고 잘못 본다. 받쳐 주지 않는
브라우저에서는 그 규칙만 무시되고 7:3 이 그대로 산다.

**성적서를 올리는 것부터 영양성분 탭에서 된다.** 어제 붙인 것은 읽기뿐이라,
문서함에 성적서가 없으면 "먼저 올려 주세요" 를 보고 문서함 탭으로 가서 올리고
다시 돌아와야 했다 — 값을 넣으러 온 사람에게 왕복을 시키는 셈이다.

올리는 창은 문서함 것을 그대로 쓴다(두 벌로 두지 않는다). 종류만 미리 골라
둔다 — 영양성분 성적서를 올리러 온 것이 분명하기 때문이다. 그리고 올린 뒤에는
**그 문서를 바로 읽는다.** 방금 올린 것을 다시 고르게 할 까닭이 없다.

창을 그냥 닫으면 그 표시를 거둔다. 남겨 두면 다음에 아무 문서나 올렸을 때
엉뚱하게 판독 창이 뜬다.

> 걸린 것 — 시험이 `window.readSpecNutrition` 을 이름으로 찾다가 **부르는
> 쪽**에 먼저 걸렸다. 새 코드가 정의보다 위에 들어갔기 때문이다. 정의 자리를
> 잡도록 고쳤다(`= async function` 까지 붙여서).

시험 3,084건(7건 늘었다).


### 2026-09-17 (5) — 성적서를 영양성분 탭에서도 부른다

성적서 판독이 붙고 나니 자리가 어긋나 있었다. **값을 넣는 자리는 영양성분
탭인데 판독은 문서함 화면에만 있었다.** 그래서 성적서를 열어 놓고 아홉 칸을
손으로 옮겨 적고 있었다 — 그 종이는 이미 문서함에 있는데도.

「값의 출처」 에 **공인기관 성적서**라는 선택지가 이미 있었다. 그것을 고르면
「성적서에서 불러오기」 가 나온다 — 「이론치 계산」 을 골랐을 때 「배합에서
계산」 이 나오는 것과 짝이다. 두 단추가 같이 있으면 어느 값이 표에 들어간
것인지 헷갈리므로, 고른 출처에 맞는 것만 보인다.

**판독은 베끼지 않는다.** 영양성분 탭은 iframe 이고 판독은 바깥 문서에 있다.
문 하나만 두드린다(`pickSpecNutritionDoc`) — 미리보기의 2차 검증이 바깥의
`ltStartCompare` 를 부르는 것과 같은 꼴이다. 옮기거나 베끼면 같은 것이 두
벌이 되고 어느 날 한쪽만 고쳐진다. 이 파일이 이미 그 일을 겪었다(검증 모달
두 벌). 시험으로 잠갔다 — 영양성분 탭에 판독 주소나 기준량 낱말이 나오면
실패한다.

고를 것이 하나면 묻지 않는다. 묻는 창은 둘 이상일 때만 뜻이 있고, 아니면 한
번 더 누르게 하는 일일 뿐이다. 하나도 없으면 문서함 탭을 열어 준다.

> 걸린 것 — 안내를 `alert` 으로 적었더니 시험이 잡았다. 이 화면에도 스낵바가
> 있다. `alert` 은 화면을 멈춰 세우고 생김새도 다른 알림과 따로 논다.

시험 3,077건(4건 늘었다).


### 2026-09-17 (4) — 성적서 표는 칸이 셋이다

기준량은 읽혔는데 **값 아홉이 전부 「못 읽음」** 이었다(화면에서 확인).

까닭은 표의 생김새다. 시험성적서는 칸이 셋이다 — 시험 항목 · 시험 기준 ·
시험 결과. 판독기가 칸마다 줄을 나누면 이름과 값 사이에 **"기준없음" 이
끼어든다.**

    열량(kcal/100g)
    기준없음          ← 숫자가 없는 가운데 칸
    574.38

파서는 이름만 있는 줄을 만나면 **다음 한 줄만** 보고 포기했다. 그래서
"기준없음" 에 걸려 값을 통째로 놓쳤다. 세 줄까지 보되 건너뛰는 것은 숫자가
하나도 없는 줄뿐이고, **다른 성분 이름이 나오면 거기서 멈춘다** — 판독기가
칸을 세로로 읽어 값이 한데 모여 있을 때 남의 값을 집으면 조용히 틀린 표가
된다. 그 경우를 시험으로 함께 잠갔다.

**그리고 「성적서에서 읽은 글자 보기」 를 달았다.** 값이 안 채워졌을 때
무엇을 읽었는지 볼 수 없으면 **파일 탓인지 파서 탓인지 알 수 없다** — 이번에
내가 그 자리에서 추측으로 고쳤다. 다음에는 화면이 답을 들고 있다.

> 걸린 것 하나 — 원문을 그대로 보여 주려고 `escapeHtml` 을 새로 만들었더니
> 바깥 문서의 같은 이름과 겹쳤다(시험이 잡았다). 이 파일에 이미 있는
> `escapeAttr` 을 쓴다. **같은 일을 하는 이름을 새로 만들지 않는다.**

시험 3,073건(4건 늘었다).


### 2026-09-17 (3) — 대조 화면 넷을 한 틀로

owner 요청: "파일로 데이터를 불러오고 대조하는 화면들의 디자인·배치를 공통
모듈로 통일하라." 살펴보니 **공통 모듈은 이미 있었다** — `photo_viewer.js` 의
`photoViewerLayout`(왼쪽 원본, 오른쪽 값 + 회전·확대·이동). 품목보고번호
확인·원료 사진·디자인시안·OCR 확인이 그것을 쓰고 있었다.

**빠져 있던 것은 바로 어제 내가 만든 성적서 화면이었다.** PDF 를 걸 수
없다는 이유로 제 틀을 따로 짰다. 그러면 안 되는 이유가 그대로 드러난다 —
같은 일을 하는 화면이 서로 다르게 생기고, 뷰어를 고칠 때 두 곳을 고쳐야
한다. 뷰어가 PDF 를 받게 고치고(그 자리에 `<embed>`, 우리 회전·확대는
걷어낸다 — 브라우저 뷰어와 두 벌이 겹치면 어느 쪽이 먹히는지 모른다),
성적서를 그 틀로 옮겼다.

표의 모양(읽음/못 읽음 이름표, 줄, 단위)도 화면 안 `<style>` 에서 공통
CSS 로 올렸다(`.cmp-*`). 넷이 같은 낱말로 말한다.

**업로드 창은 고른 파일을 보여 주지 않고 있었다.** 이름과 크기만 적혀 있는데
그 창에서 문서 종류와 유효기간을 정한다 — 종이를 못 보고 정하는 셈이고,
파일 이름이 "scan_0007.pdf" 인 일은 흔하다. 같은 뷰어를 그 자리에 붙였다.
두 칸 틀이 필요 없는 자리라 뷰어만 따로 쓸 수 있게 `photoViewerElement` 를
열었다(놓아 주는 `photoViewerRelease` 도 함께 — 정리를 부르는 쪽마다 다시
적게 하면 언젠가 한 곳이 빠진다).

시험으로 잠근 것: **두 칸 틀을 제 손으로 짜는 화면이 없어야 한다.**
`photo-compare`·`photo-viewer-slot` 을 뷰어 밖에서 쓰면 실패한다.

> 배운 것 — **새 화면을 만들기 전에 그 일을 하는 화면이 이미 있는지 본다.**
> 어제 성적서 화면을 만들며 "PDF 라서 안 된다" 로 결론 내고 새로 짰는데,
> 정작 필요한 것은 뷰어에 스무 줄을 더하는 일이었다.

시험 3,069건(3건 늘었다).


### 2026-09-17 (2) — 성적서를 원본 옆에 놓고 견준다

**기준량을 항목 이름 안에 적은 성적서를 못 읽었다.** 「열량(kcal/100g)」 처럼
'당' 도 '기준' 도 없는 꼴인데, 시험성적서는 대개 이렇게 적는다. 글자는 멀쩡히
읽고도 「기준량을 읽지 못했습니다」 로 끝났고, **읽어 둔 값 아홉 개가 함께
버려졌다** — 사용자는 판독이 통째로 실패한 줄 안다.

패턴을 더하자 **곧바로 두 번째 것이 드러났다**(시험이 잡았다). "열량(kcal/
100g) 574.38" 에서 첫 숫자는 574.38 이 아니라 **100** 이다 — 기준량 표기를
걷어내지 않으면 모든 성분이 기준량 숫자로 채워진다. 값을 읽기 전에 그 표기를
먼저 지운다.

**화면을 디자인시안 2차 검증과 같은 모양으로 바꿨다**(owner 요청). 왼쪽에
원본(PDF 는 그대로 끼워 넣는다), 오른쪽에 아홉 칸. 읽은 칸과 못 읽은 칸을
「읽음」·「못 읽음」 으로 갈라 놓는다 — 비어 있는 칸만으로는 "0 인가 못 읽은
것인가" 를 알 수 없고 그 둘은 뜻이 완전히 다르다. 기준량을 못 읽었으면 환산
전 값을 곁에 적어 **읽긴 읽었다**는 것을 보여 주고, 사람이 기준량을 넣으면
다시 셈한다. 그때 원문을 되돌려 보내 **판독을 다시 하지 않는다** — 같은 종이를
두 번 읽으면 한도가 그만큼 깎인다.

**불러오기에서 찾아 놓고 넣을 자리가 없었다.** 등록 단추가 사진 칸 아래에만
있어 번호로 찾은 사람 눈에는 안 띈다. 코드 주석이 이미 그 증상을 적어 두고도
(「다 골라 놓고 아무 일도 안 일어나는 것처럼 보인다」) 홈에서 들어온 경우에만
길을 열어 두었다. 찾은 결과 바로 밑에 「이 제품으로 등록 · 원료로 등록」 을
둔다.

> 배운 것 — **파서를 고치면 그 옆이 흔들린다.** 기준량을 읽게 만든 순간 값
> 파서가 같은 숫자를 물었다. 시험이 없었으면 "이제 읽는다" 로 끝내고 모든
> 성분이 100 으로 저장됐을 것이다.

시험 3,066건(12건 늘었다).


### 2026-09-17 — 화면에서 올라온 다섯, 그리고 딸려 나온 하나

owner 가 실제로 써 보고 짚은 것들이다. 다섯 다 **원인이 서로 달랐다.**

**① 원재료명이 적은 것부터 늘어섰다.** 식약처 API 는 원재료명과 순서를
따로 준다. 수집할 때 둘을 맞춰 다시 엮어 `FoodItem.rawmtrl_nm_sorted` 에
넣어 두는데, **품목보고번호 불러오기 입구만 원본(`rawmtrl_nm`)을 넘기고
있었다** — API 가 보낸 차례 그대로다. 검색(`label/utils.py:228`)과 라벨
만들기(`label/views.py:3864`)는 이미 정렬본을 먼저 본다. 이 한 곳만 빠져
있었다. owner 가 "서버에서 재조합해 보여 주고 있었다" 고 짚어 준 덕에 찾았다.

**② BOM 표가 배합비 순으로 정렬되지 않는다.** 정렬하는 기능이 **없었다**.
요약은 늘 내림차순으로 만드는데 표의 줄은 넣은 순서 그대로라, 둘이 뒤집힌
순서로 보인다. 「배합비 순 정렬」 단추를 뒀다 — 자동으로 옮기지는 않는다.
줄 끌기가 켜져 있어 사람이 맞춰 둔 순서를 말없이 흐트러뜨리면 안 된다.

**③ 올린 문서가 필수 문서 칸에 안 꽂혔다.** 슬롯의 `+` 로 올렸을 때만
연결하고 있었다. 문서함에 그냥 끌어다 놓으면 파일명으로 「품목제조보고서」
로 분류까지 해 놓고도 0/5 그대로다 — 목록에는 보이는데 칩은 "없음" 이라고
말한다. 없는 슬롯을 새로 만들지는 않는다(그 목록은 사람이 정한다).

> 여기서 하나가 딸려 나왔다. 그 자리 주석이 "save() 에서 update_status()
> 호출" 이라는데 **`DocumentSlot` 에는 save() 가 없다.** 문서를 꽂아도 DB 의
> status 는 EMPTY 로 남았다. 문서함 화면은 그릴 때 다시 계산해 멀쩡해
> 보였지만, 저장된 값을 읽는 쪽(확정 전 검증의 만료 판정)은 낡은 값을 봤다.

**④ 불러오기 창이 계속 떴다.** 주소의 `import=1` 을 지우지 않았다. 이 화면은
문서를 올릴 때마다 `location.reload()` 를 하므로 그때마다 다시 열렸다. 뒤로
가기로 돌아와도 마찬가지다. 연 뒤에 표시를 지운다.

**⑤ PDF 성적서에서 글자를 못 읽었다.** Vision 의 `images:annotate` 는 사진만
받는다. PDF 를 그대로 보내면 거절당하고, 그것이 빈 문자열이 되어 화면에는
「사진에서 글자를 찾지 못했습니다」 로 나온다 — 파일이 잘못된 줄 알게 된다.
성적서·품목제조보고서는 대개 PDF 다. `files:annotate` 로 보내면 같은 키·같은
판독기가 읽는다. 여러 쪽이면 앞 다섯 쪽을 이어 붙인다.

> 배운 것 — **"안 된다" 는 다섯 가지가 다섯 가지 까닭을 갖는다.** 한 화면에서
> 같이 올라왔다고 한 덩어리가 아니다. ①은 입구 한 곳, ②는 없던 기능,
> ③은 조건 하나, ④는 주소 한 글자, ⑤는 API 문이 달랐다.

시험 3,054건(13건 늘었다).


### 2026-09-16 (4) — 가장 많이 눌러야 하는 것이 가장 안 보였다

탭으로 옮기고 나니 「검증」 이 설정 탭 넷과 **똑같이 생겼다.** 회색 글씨
하나가 늘어난 것으로만 보인다. 이 화면에서 가장 많이 눌러야 하는 것인데
그렇다. 옮기면서 이 화면 자신의 기록을 놓쳤다 — `label_preview.html:44` 에
"규정 검증은 이 화면에서 가장 많이 누르는 단추다. 회색 테두리로 두면 보조
도구처럼 보여서, 정작 돌려 보지 않고 확정하는 일이 생긴다" 고 적혀 있다.

모양으로 가른다. 설정 넷은 밑줄형이고 검증만 **알약형**이다. 색으로만
가르지 않는 까닭은 활성 표시도 파란색이라 "지금 열려 있는 탭" 과 헷갈리기
때문이다.

그리고 **재검토 수를 탭 머리에 단다.** 검증을 돌린 뒤 표 설정으로 옮기면
결과가 눈앞에서 사라지는데, 고칠 것이 남았다는 사실까지 사라지면 안 된다.
권고는 세지 않는다 — 확정을 막는 것만 숫자로 말한다.

시험 3,043건(2건 늘었다).

### 2026-09-16 (3) — 검증을 탭으로, 그리고 늘 실패하던 단추

**한 칸을 위아래로 나눠 쓰는 것이 문제였다.** 검증 결과를 설정 위에 얹어
두고 높이를 두 번 만졌는데, 자르지 않으면 설정이 화면 밖으로 밀리고 자르면
넘친 것이 설정 위에 겹쳐 찍혔다. **높이 계산으로 풀 문제가 아니었다** —
owner 가 "탭으로 왔다갔다 하자" 고 했고 그것이 답이었다.

검증을 「표 설정 · 항목 순서 · 텍스트 변환 · 분리배출마크」 와 같은 탭 줄로
들였다(맨 앞, 탭 안에서만). 한 번에 하나만 보이고 각자 패널 전체를 쓴다.
그래서 `lt-verify` 의 높이 상한을 **없앴다** — 있으면 그 상한이 다시 겹침을
만든다. 탭 복원은 이름을 박아 두지 않고 "있는 탭 중 첫째" 로 바꿨다(창으로
열면 검증 탭이 아예 없다).

**「필수 입력 항목」 의 「수정하러 가기」 는 늘 실패하고 있었다.** 가장 자주
나오는 지적인데 가장 안 되는 단추였다. 까닭은 이름 하나다 — 표시 항목
이름은 「내용량(열량)」(`weight_calorie`)이고 칸 이름은 내용량
(`field-content-weight`)이라, 규칙대로 만든 id 가 화면에 없었다.

전수로 세어 보니 **갈 곳 없는 표시 항목은 셋뿐**이었고 둘은 이미 지도에
있었다(원재료명(표시) · 영양성분 표시). 빠진 하나를 넣고, 그래도 못 찾으면
**기본 정보 탭이라도 열고** 정확히 못 찾았다고 말하게 했다. 아무 데도 안
보내는 것이 제일 나쁘다.

그리고 표시 항목 전부가 갈 곳을 갖는지 **모델에서 훑어 잠갔다**
(`ValidationEditTargetTests`). 표시 항목은 `chckd_*` 로 늘어나므로, 한 번
고치고 끝낼 일이 아니다.

> 배운 것 — **두 번 고쳐도 안 되면 계산이 아니라 배치를 의심한다.** 높이
> 상한을 48%·60% 로 만지는 동안 문제는 "둘을 한 칸에 두었다" 는 데 있었다.
> 화면을 보는 사람이 그것을 한 줄로 짚었다.

시험 3,041건(3건 늘었다).


### 2026-09-16 (2) — 화면에서 본 것 셋

어제 옮긴 검증 결과 패널을 실제로 눌러 보고 나온 것들이다. **셋 다 옮기기
전에는 드러나지 않던 종류**다.

**① 「수정하러 가기」가 조용히 아무 일도 안 했다.** `gotoLabelField` 는 그
칸을 못 찾으면 `false` 를 돌려주는데, 받는 쪽(`_tab_label.html`)이 그 값을
버리고 있었다. 사용자에게는 단추가 고장 난 것으로 보인다. 못 갔으면 말하게
했고, 어느 칸이었는지는 콘솔에 남긴다 — 규칙을 넓힐 단서가 거기 쌓인다.

**② 탭을 다녀오면 결과가 사라졌다.** 사라지는 길이 둘이었다.

- 표를 다시 그리면 `tbody` 가 통째로 갈리며 **번호 배지가 지워진다.** 기본
  정보에서 값을 고치고 돌아오면 바깥 화면이 새 데이터를 밀어 주고, 그때
  일어난다. 왼쪽 목록의 "③" 이 표의 어디를 가리키는지 알 수 없게 된다.
- 미리보기가 다시 뜨면 JS 변수에 있던 결과가 없어진다.

표를 그린 끝에서 배지를 다시 얹고, 결과는 `sessionStorage` 에 제품별로
갈무리해 되살린다. 되살린 것은 **낡았다고 말한다** — 고치고 돌아온 사람이
옛 지적을 지금 판정으로 읽으면 안 된다.

**③ 결과가 설정 항목을 아래로 밀어냈다.** 이 패널은 검증 아래로 표 설정·
항목 순서·글자 서식이 이어지는 세로 한 줄인데, 결과를 그대로 쌓으니 그것들이
화면 밖으로 나갔다. 검증 덩어리에 높이 상한(48%)을 주고 그 안에서 스크롤하는
것은 결과 본문 하나뿐이게 했다(`min-height:0` 사슬). 그리고 `<details>` 로
바꿔 접을 수 있다 — 접어도 머리줄의 개수는 남는다.

**③ 을 고치다 화면이 겹쳤다 — 같은 날 다시 고쳤다.** 높이 상한만 주고
`overflow` 를 안 줬다. 넘친 내용은 상자 밖에 **그대로 그려져** 아래 설정
위에 겹쳐 찍힌다. 그리고 스크롤을 `flex` + `min-height:0` 사슬에 맡겼는데,
그 사슬은 고리 하나만 어긋나도 통째로 안 걸린다(`<details>` 를 flex 컨테이너로
쓴 것이 의심스러운 고리였다). 지금은 결과 본문 높이를 `30vh` 로 못 박고,
검증 덩어리에도 `overflow-y` 를 함께 준다 — **자른 상자는 넘침도 가둔다.**

> 배운 것 — **자리를 옮기면 그 자리의 제약이 따라온다.** 모달은 남는 공간을
> 걱정할 필요가 없었다. 세로 한 줄짜리 패널로 내려오자 "얼마나 차지해도
> 되는가" 가 새 문제로 생겼다. 옮기는 설계에서 이것을 미리 못 봤다.
>
> 그리고 **높이를 자르는 것과 넘침을 가두는 것은 한 쌍이다.** 하나만 하면
> 밀리던 것이 겹치는 것으로 바뀔 뿐이다. 시험도 둘을 함께 잠갔다.

시험 3,038건(6건 늘었다).


### 2026-09-16 — 검증 결과를 표 옆에 둔다, 그리고 죽은 검증 엔진 408줄

**검증을 미리보기에서 떼어내지 않기로 했다(owner).** 떼는 설계를 그리다가
지금 구조의 까닭을 찾았다 — 1차 결과는 **미리보기 표 위에 번호 배지로
달리고**(`.pv-issue-badge`), 2차는 그 표 전체를 시안과 대조한다. 표와 떨어지면
"어느 줄 이야기인지" 를 보여 줄 자리가 없어진다. 앞사람도 가로 띠에서 세로
패널로 옮기며 "한 절차, 한 자리" 라고 적어 두었다. ④ 의 실제 불편(영양정보
표)은 어제 풀렸으므로, 남은 둘만 고쳤다.

**(a) 결과를 모달에서 1차 검증 탭 안으로 내렸다.** 모달이 바로 그 표를 덮고
떴다 — 지적한 줄에 번호를 달아 놓고 그 번호를 가리는 꼴이라, 하나 볼 때마다
열고·읽고·닫고 표에서 찾기를 되풀이해야 했다. 지적이 열몇이면 왕복도 열몇
번이다. 이제 왼쪽에 목록, 오른쪽에 번호가 함께 보인다. 배지를 누르면 아무
것도 열지 않고 그 지적으로 데려간다. 창으로 따로 열었을 때는 그 자리가 없어
모달을 쓴다 — 제목·본문 조립은 **한 벌**로 뽑아 두 자리가 나눠 쓴다.

곁들여 고친 것: 탭 머리의 「끝」 표시(초록 번호)를 **단추 누른 순간**에
켜고 있었다. 서버가 답하기 전에 "검사했습니다" 가 떠 있었다는 뜻이다.
결과가 왔을 때 켜도록 옮겼고, 기다리는 동안은 "검사하는 중…" 이다.

**(b) 죽은 검증 엔진 408줄을 걷어냈다.** 호출자 0:

| 무엇 | 줄 |
|---|---|
| `window.validateSettings` (옛 클라이언트 검증) | 130 |
| `showValidationModal` — **두 벌** (77 + 45) | 122 |
| `window.validateRegulations` | 61 |
| 그것만 부르던 검사 함수 다섯 | 88 |

지우는 까닭은 줄 수가 아니다. **이름이 더 그럴듯한 쪽이 죽어 있었다.**
"검증 결과 표를 고쳐 달라" 를 받은 사람은 `showValidationModal` 을 먼저
찾는데, 고쳐도 아무 일이 일어나지 않는다. 게다가 같은 이름이 한 파일에 두
벌이라 뒤에 선언된 것이 이긴다 — 이 저장소는 그 함정에 이미 한 번 빠졌고
(`label_preview.html` 의 같은 자리 주석), 그때 HTML 쪽만 치우고 JS 쪽을
남겨 두었다.

> 배운 것 — **떼어내기 전에 왜 붙어 있는지부터 찾는다.** "성격이 섞였다" 는
> 진단이 절반은 틀렸다. 붙어 있어야 해서 붙어 있었고, 실제로 아팠던 곳은
> 다른 데였다. 설계를 그리는 데 쓴 시간이 구현보다 길었지만, 그리지 않았으면
> 나흘짜리 분리 작업을 하고도 같은 왕복이 남았을 것이다.

시험 3,032건(6건 늘었다). 앞선 시험 셋은 구조가 바뀌어 새 자리에 맞췄다 —
잠그던 뜻은 그대로다.


### 2026-09-15 — 만료 문서 묶음, 그리고 낡은 문서 셋

**「알림 설정」 이 처음으로 무언가를 한다.** 문서 타입마다 "30일 전" 을 적어
두고 화면도 그렇게 보여 주는데 보내는 쪽이 없었다 — `needs_alert()` 호출자 0.
`alert_expiring_documents` 를 만들어 그 함수의 첫 호출자로 삼았다. 창 안이라고
매일 보내지 않는다(설정한 날 + 7·3·1·0일 전). **예약 등록은 사람이 한다.**

같이 고친 것:

- 홈의 「N건 만료 임박」 칩이 **문서를 세어 놓고 제품 목록으로** 보냈다.
  문서 목록(`expiring_documents`)으로 돌렸다.
- 「N건 만료됨」 칩을 새로 뒀다. `expired_documents` 는 **어디에서도 링크되지
  않아** 만료된 문서를 볼 길이 없었다 — 만료일이 지나면 세 화면에서 동시에
  사라진다.
- 탐색기의 「만료 임박」 이 `[:5]` 에서 멈췄다. 숫자도 5에서 굳고 거르기도
  다섯 제품만 남았다(같은 목록을 `data-has-expiring` 이 쓴다).

**시간대에서 둘이 나왔다.** 예약은 UTC 로 돌고 사이트는 `Asia/Seoul` 이다.

- `days_until_expiry()` 가 `timezone.now().date()`(=UTC 날짜)로 셌다. 한국
  자정~오전 9시에는 남은 날을 하루 더 많게 말했다 → `localdate()`.
- 중복 방지를 `created_at__date=today` 로 물으려다 말았다. **MySQL 에 시간대
  표가 없어 `CONVERT_TZ` 가 NULL 을 준다**(로컬에서 확인). 조건이 아무 행에도
  안 맞아 같은 메일이 예약마다 나갔을 것이다. SQLite 로 도는 시험은 이것을
  못 잡는다 — 하루 경계를 파이썬에서 계산해 범위로 묻는다.

**낡은 문서 셋을 고쳤다.** 「마이그레이션 그래프가 깨져 있다」 는 `96c1b08`
(2026-08-30)에서 이미 풀렸는데, `settings_test` 주석 · `IMPROVEMENT_PLAN 4-9`
· 이 원장의 인계 메모가 그대로 남아 **그 뒤 두 주 동안 작업을 막고 있었다**
(`product_recent` 삭제가 여기 걸려 있었다). 고친 자리에 무엇이 사실인지와
어디서 확인하는지를 적었다.

> 배운 것 — **막고 있는 제약은 그 문장이 쓰인 날짜와 함께 읽는다.** 세 문서가
> 서로를 가리키며 같은 말을 해서 아무도 다시 확인하지 않았다. 확인은
> `migrate --plan` 한 번이었다.

**예약 자리가 없다.** PythonAnywhere Tasks 가 상한에 걸려 새 줄을 못 만든다
(2026-09-15 확인). 그래서 `daily_reminders` 로 묶었다 — 예약에는 이것 하나만
걸고, 앞으로 늘어나는 알림은 그 안의 목록에 더한다. **예약을 건드리지 않고
알림을 늘릴 수 있다.** 하나가 실패해도 나머지는 나간다(시험으로 잠갔다).

걸 자리는 기존 줄 끝이다. 시각은 **UTC** 다(owner 확인) — 한국은 아홉 시간
앞선다. `00:10` 줄(= 한국 09:10)에 `;` 로 붙이는 것을 권했다. `&&` 가 아니라
`;` 인 까닭은, 앞 커맨드가 실패해도 알림은 나가야 하기 때문이다.

**`applyIngredientSelection` 은 의도로 확정했다**(owner). 위 「아직 안 고친
것」 참고.

**미리보기에서 영양정보 표를 접을 수 있다**(④ 의 실제 불편). 접는 것은 화면
뿐이고 PDF·워드·문서함 도안에는 그대로 들어간다. 설정은 **계정에 붙는다** —
가로·글꼴은 제품마다지만(`MyLabel.prv_*`) 이것은 인쇄물을 바꾸지 않는 화면
취향이라 제품이 이백 개면 이백 번 켜야 하는 설정이 된다. 같은 자리를 쓰는
선례가 둘 있다(원료 목록 칸 순서, 디자인 의뢰서 규정 메모).

시험 3,026건(25건 늘었다).


### 2026-09-13 — 모바일 앱 전수 감사와 반영 (검증 환경을 갖췄다)

앱(`labeldata-mobile`)은 **한 번도 감사한 적이 없었다.** 디자인·UI/UX ·
앱↔웹 기능 격차와 절차 · 구조·품질·배포 세 갈래로 동시에 훑었다.

**먼저 검증 환경을 만들었다.** 그전까지 이 장비에는 Flutter·Dart·Java 가
없어서, 앱은 "코드를 읽어 확정한 것만" 고칠 수 있었다 — 저장소가 지켜 온
"재현 시험을 먼저 쓴다" 규칙을 앱에는 적용할 수 없었다는 뜻이다. Flutter
3.29.3(프로젝트 기준)·JDK 17·Android SDK 36 을 설치했고 이제 이렇게 검증된다:

    flutter analyze   → 문제 0
    flutter test      → 48건 (처음 만든 것)
    flutter build apk → 돈다

**설치하자마자 내가 낸 것 둘이 잡혔다.** 앞선 커밋에 컴파일 오류(import
누락)가 있었고, 서버에 `match_type_display` 를 추가해 놓고 앱 모델이 그 키를
그대로 버리고 있었다. 도구 없이 코드만 읽어서는 둘 다 못 잡았다.

**되돌릴 수 없는 것 하나.** 수거검사 알림을 스와이프하면 서버가
`InspectionMatch` **행 자체**를 지운다 — 웹의 수거검사 탭에서도 영구히
사라진다. 부적합 알림을 미는 것은 이 기기의 기록 한 줄이 지워질 뿐인데
**같은 손동작에 파급이 다르고 확인창이 없었다.**

**조용히 죽어 있던 것들.** 강제 업데이트 대화상자가 한 번도 뜬 적이 없다
(`MaterialApp.router` 위쪽 context 로 `showDialog` 를 불러 던지고 `catch (_)`
가 삼켰다 — 구버전을 끄는 유일한 장치다). Crashlytics 가 동기 위젯 예외만
받아 **async 예외를 하나도 안 잡았다.** 세 provider 가 `ref.read` 라 기기
등록이 끝나도 다시 읽지 않아 `/devices//rules/` 로 요청이 나갔다 — 푸시 →
콜드 스타트 → 상세가 이 앱의 주 동선이다.

**서버가 보내는데 앱이 버리던 것.** 키워드 등록 직후의 소급 결과 다섯 값을
통째로 버려서, 90일치에서 12건이 걸렸든 0건이든 **소급이 예외로 실패했든**
같은 문구가 떴다. 웹은 이미 그 문제를 겪고 고쳤는데 앱이 반복하고 있었다.

**앱만 쓰는 사용자가 못 켜던 기능.** 수거검사 매칭 세 규칙 중 둘은
`UserProfile` 의 회사명·인허가번호가 있어야 도는데, 그 값을 넣는 자리가
웹에만 있고 앱 API 도 없었다 — 앱에 '수거검사' 탭이 버젓이 있는데 영영 비어
있을 수 있었다. `GET/PUT /devices/<id>/inspection-profile/` 을 열었다.

**서버 쪽에서 함께 고친 것** — 내 제품 알림이 키워드 알림에 가려져 아예 안
만들어지던 것(이 앱이 파는 것이 그 알림이다), 앱 읽음이 웹 배지를 안 내리던
것, 미인증 계정 로그인이 "비밀번호가 틀렸다" 로 나가던 것, 승격이 한도를
안 보던 것, 소급이 알림함 상한을 두 배로 넘기던 것, PATCH 로 키워드를 바꿔도
소급이 안 돌던 것.

**디자인·접근성** — 테마 파일이 "옅은 배경 위의 글자는 *OnBg 를 쓴다" 고
적어 두었는데 `warningOnBg` 사용 0회였다. `risk_level` 의 DB 기본값이 MED 라
**가장 흔한 등급이 하필 2.8:1 로 가장 안 보였다.** 빈 화면은 전부 막다른
길이었다 — 알림 0건인 신규 사용자는 당겨도 아무 일이 없고 단추도 없었다.
위험도는 3px 색 띠 하나로만 말했고, 저장소 전체에 `Semantics`·`tooltip` 이
0회였다.

**정리** — 안 쓰는 의존성 여덟(빌드 크기와 Play 데이터 안전성 신고에 그대로
얹혀 있었다), 죽은 코드 셋, 22MB `symbols.zip` 추적 해제, `firebase_analytics`
(Dart 0줄인데 Android SDK 는 자동 수집), 영원히 비어 있던 지표 admin 메뉴.

**빌드를 막던 것.** `key.properties` 가 없으면 release 뿐 아니라 **debug 도**
Gradle 설정 단계에서 죽었다(새 클론은 언제나 그 상태다). 그리고
`gradle-daemon-jvm.properties` 가 데몬 JVM 을 `vendor=jetbrains, 21` 로
고정해 Android Studio 밖에서는 빌드가 아예 안 됐다 — 프로젝트가 요구하는
것은 Java 17 인데도. 둘 다 풀었다.

### 2026-09-13 — 전수 감사 3회차 (로컬, 웹·모바일 병렬)

다섯 갈래를 동시에 돌렸다 — Flutter 앱(첫 감사) · 모바일 서버 API · 문서·업로드·
제품 상세 · 라벨·미리보기 · 규제·게시판·공통. 커밋 여덟 개, 시험 51건 추가,
2,764건 통과, 마이그레이션 없음.

**두 감사가 서로 모르는 채 같은 결론에 닿은 것이 둘 있었다** — `/token/refresh/`
부재와 알림 번호 충돌. 병렬로 돌린 값이 거기서 나왔다.

**먼저 적어야 할 것 — 지난 라운드가 낸 회귀.**

`3dd9690` 이 `smart_upload.js` 에 확장자 검사를 넣으면서 `const ext` 를 **같은
블록에 두 번** 선언했다. 실행 오류가 아니라 **파싱 오류**라 파일 전체가 로드되지
않는다 — 그 사이 **문서 업로드가 통째로 불가능**했다(업로드 단추·필수 문서 칩·
드래그앤드롭·유효기간 계산 전부). 파이썬 시험은 이것을 볼 수 없고 눈으로도 안
잡힌다. `manage.py check` 에 `static.E001` 을 붙여 같은 종류를 막는다. 저장소
전체를 훑어 이 하나뿐이었다.

**모바일 — 플래그를 켤 수 없던 진짜 까닭**

앱 인터셉터는 401 을 받으면 **반드시** `POST /token/refresh/` 를 치는데
(`api_client.dart:80-96`) **서버에 그 라우트가 없었다.** 404 가 오면 앱의
`catch (_)` 가 리프레시 토큰까지 지운다. 액세스 토큰 수명이 7일이므로 8일째
앱을 켜는 사용자가 조용히 로그아웃됐다. `MOBILE_REQUIRE_AUTH` 를 켰다면 그
사용자들이 전부 영구 잠금됐을 것이다 — **앞 라운드에서 "앱이 헤더를 보내니 켜도
된다" 고 적은 것은 틀렸다.** 이제 그 경로가 있으므로 켤 수 있다.

`/account/delete/` 도 없었다. 개인정보처리방침이 "[내 정보/설정 → 회원 탈퇴] 로
계정 삭제 및 정보 파기를 요청할 수 있다" 고 약속해 두었는데 버튼이 늘 "알 수 없는
오류" 로 끝났다. 만들었다 — 비밀번호 재확인 → 기기·보관함·알림 내역·키워드 즉시
파기 → 계정 비활성. **표시사항·제품은 남긴다**(아래 '판단이 필요한 것' 참고).

그 밖에 `register_device` 가 안 보낸 칸을 지우던 것(iPhone 이 DB 에서 영구히
android 가 되고, 앱을 켤 때마다 fcm_token 이 한 번 NULL 이 됐다), 알림 목록의
번호가 두 표에서 겹쳐 **엉뚱한 알림이 지워지던 것**, 상세 응답에 세 칸이 빠져
앱 UI 80여 줄이 한 번도 렌더된 적 없던 것, 레이트리밋이 HTML 404 로 나가던 것.

**인쇄물이 표시기준을 어기고 있었다**

반올림·"미만" 규칙이 세 곳에 있었고 셋이 서로 달랐다. 미리보기 것은 성분이 아니라
단위('kcal'/'mg'/'g')로만 갈라서 성분별 규칙을 애초에 표현할 수 없었다.
**트랜스지방 0.3g 을 `0` 으로 인쇄했다.** 콜레스테롤 3mg 도 `0`(옳게는
"5mg 미만"), 당류 3.7g 은 `3.7`(옳게는 `4`), 나트륨 경계는 140(옳게는 120).
계산기에서 확인한 표와 실제로 인쇄되는 표가 달랐고, 두 화면을 나란히 놓고
숫자를 대조하지 않으면 보이지 않는다. JS 두 벌을 `nutrition_display.js` 하나로
모았다.

세로 길이를 재는 곳도 둘이었고 cm 환산 상수가 달랐다(`/28.35` vs `/37.795`, 33%
차이). 그 값이 DB(prv_length)와 PDF 페이지 높이가 되므로 **인쇄물이 세로로
늘어났다.** 미리보기 설정 JSON 은 템플릿이 손으로 조립해서, 분리배출마크 문구에
역슬래시나 줄바꿈이 하나만 들어가면 `JSON.parse` 가 던지고 **가로·글자 크기·
자간·줄간격·글꼴이 전부 기본값으로 돌아갔다.**

**500 과 유출**

`quota.consume()` 은 존재하지 않는 함수이고 `quota` 라는 이름조차 그 스코프에
없었다 — 성적서 영양성분 판독 두 경로가 NameError 500 이었다(A등급을 만드는
유일한 길이다). `DocumentSlot.SlotStatus.ACTIVE` 도 없는 값이라 고정 서류
불러오기가 **문서를 만든 뒤** 500 이 났고, 실패로 알고 다시 누르면 중복이 쌓였다.
`?insp_id=abc`·`?date_from=abc`·`?days=1000000000` 도 각각 500.

게시판 답변 작성자가 `username` 원문으로 나갔다 — 가입이 `username=email` 이고
상세는 로그인 없이 열리므로 **아무나 남의 이메일을 읽었다.** `ocr_truth/` 는
ACCESS_RULES 에 없어 로그인만 하면 받아졌다(사용자 제품의 표시사항 실물 사진이다).

자료 요청 제출(로그인 경로)에는 크기·확장자·할당량 검사가 하나도 없었고
(`.html` 이 요청자 오리진에서 실행된다), 협력사 업로드는 2.5MB 넘는 파일에서
500 이 났다(같은 UploadedFile 을 두 모델에 잇달아 저장 — 첫 저장 때 임시 파일이
옮겨진다).

**돈이 나가던 자리**

「둘러보기」가 누를 때마다 새 계정을 만들어 흐름 한도(규정 검증·사진 읽기)가
그때마다 리셋됐다. 같은 브라우저는 같은 게스트로 돌아가게 했다.

### 2026-09-13 — 전수 감사 2회차 (로컬, 심각도 순)

바뀐 방식대로 **처음부터 다시 전수로** 훑었다. 웹 일곱 갈래와 모바일을
병렬로 돌려 130건 남짓 나왔고, 그중 **심각 17건**을 재현 시험으로 확인한
뒤 고쳤다. 커밋 여섯 개, 시험 36건 추가, 마이그레이션 없음.

한 번 본 화면에서 또 나왔다. 그리고 **한 번도 안 본 화면이 또 있었다** —
인증 화면 전체가 그랬고, 거기서 이번 감사 최대 건이 나왔다.

**심각 — 계정과 데이터**

1. **계정 탈취.** `?uid=1&token=`(토큰만 빈 값)으로 아무 계정의 비밀번호나
   바꿀 수 있었다. `password_reset_token` 기본값이 `''` 인데 판정이
   `stored != token` 하나뿐이라 `'' != ''` 가 거짓이 된다. uid 를 1부터
   훑으면 관리자도 열린다. 바로 옆 이메일 인증에는 있던 `and token` 이
   재설정에만 없었다. 만료도 없었다 — `password_reset_sent_at` 을 쓰기만
   하고 읽는 곳이 없어 반년 전 링크가 지금도 열렸다. TTL 1시간을 걸고
   `validate_password()` 를 통과시키게 했다.
2. **저장형 XSS 두 곳.** 미리보기가 라벨 값 11개를 `|safe` 로 `<script>`
   안에 박았다(공유받은 사람 브라우저에서 실행). 알림 드롭다운은 남이 친
   반려 사유·이름을 `innerHTML` 로 그렸다 — **모든 V2 화면**의 상단바다.
3. **배합표가 화면 좌표와 데이터 좌표를 섞었다.** `getSourceDataAtRow` 는
   물리 좌표를 받는데 `setDataAtRowProp`·`getCell`·`afterChange` 는 화면
   좌표를 준다. `manualRowMove` 가 켜져 있어 줄을 옮기면 갈라진다. 표 값은
   옳은 줄로, `_meta`(bom_id)는 다른 줄로 갔고 — 저장이 그 bom_id 를 쓰므로
   **엉뚱한 DB 행을 덮고**, 서버가 그 행으로 원료 마스터를 역동기화해
   **같은 원료를 쓰는 다른 제품의 BOM 까지** 고쳤다.
4. **협력사 업로드.** `request.FILES.items()` 는 키마다 마지막 하나만 준다 —
   여러 개를 붙이면 하나만 저장되고 화면은 "제출 완료"라고 했다. 크기·확장자·
   할당량 검사가 서버에 하나도 없어 로그인 안 한 협력사가 요청자의 저장
   한도를 넘길 수 있었고 `.html`·`.svg` 도 받았다.
5. **모바일에 인증이 없었다.** `login` 이 JWT 를 발급하는데 어느 뷰도
   검사하지 않았다 — `request.user` 가 `v1/mobile/views.py` 전체에 한 번도
   없었고, 소유자 판정이 URL 의 `device_id` 하나였다.
6. **게시판 빈 흰 화면 + 비밀글 첨부 유출.** `HttpResponseForbidden` 을
   return 해서 `handler403` 을 안 탔다(본문 0바이트). 첨부는 `/media/`
   직행으로 아무 로그인 사용자에게나 갔다.

**심각 — 기능이 통째로 죽어 있던 것**

7. **문서 유효기간이 한 번도 적용되지 않았다.** `data-validity`(성적서 180일,
   원산지증명 365일, HACCP 1095일) 분기가 `if (selectedExpiry) return;` 뒤에
   있었는데 그 조건이 늘 참이었다. 그 문서들의 **D-day·만료 뱃지·만료 알림·
   준수율이 전부 죽어 있었다.**
8. **"성적서에서 읽기" 가 아무 일도 안 했다.** 파일 고르개를 만들자마자
   `load()` 의 `innerHTML` 이 지웠다. 영양성분 A등급을 만드는 유일한 경로다.
9. **원재료 팝업이 식품유형 자리에 식품구분을 보냈다.** `.food-type-select`
   를 붙이는 함수의 호출이 0회라 대체 셀렉터가 문서 순서 첫 match(식품구분)
   를 집었다. 그 값이 `get_or_create` 키에 들어가 **중복 원료가 쌓였다.**
   원산지는 화면이 `origin`, 서버가 `notes` 를 읽어 한 번도 저장된 적이 없다.
10. **원료 등록 한도를 만드는 길 다섯이 전부 안 봤다.** 그 다섯이 모두
    지나는 `get_or_create_my_ingredient` 한 곳으로 모았다.
11. **영양 계산기 팝업이 고른 표시기준을 부모가 기본값으로 덮었고**, 단위를
    안 보내 ml 제품이 저장 한 번에 g 이 됐다. `REQUIRED_IDS` 가 영영 안
    차서 100ms 타이머가 무한히 돌던 것도 함께 고쳤다.

**모바일 — 서버는 닫았고 앱 배포가 남았다**

토큰을 보낸 요청은 이제 검사한다. 전면 차단은 `MOBILE_REQUIRE_AUTH`
(기본 꺼짐)이다. 앱(`labeldata-mobile`)을 받아 확인한 결과
`lib/core/network/api_client.dart` 의 인터셉터가 **모든 요청에
`Authorization: Bearer` 를 싣는다** — 켤 수 있다. 다만 앱은 **401 에서만**
토큰을 갱신하므로 자격증명 없음에 403 이 아니라 **401** 을 주도록 고쳤다.

> **남은 판단 하나.** 옛 설치본 중 `device_id` 를 Android Build.ID 로 쓰는
> 것이 있다(지금 앱은 UUID v4 — `lib/core/services/device_service.dart:25`).
> 같은 기종끼리 그 값이 겹치면 `AppDevice` 한 줄을 여럿이 나눠 쓰고 있고,
> 플래그를 켜는 순간 한 명만 통과하고 나머지는 403 으로 잠긴다. 그 사용자들이
> 최신 앱으로 넘어간 뒤에 켜는 것이 안전하다.

**앱 코드가 확정해 준 것 두 가지.** `version_check` 의 `x.y.z+build` 파싱은
지금은 안 깨진다 — 앱이 `info.version`(3자리)만 보낸다. 다만 관리자가
`min_version` 에 그 꼴을 넣으면 그 자리에서 죽으므로 파서는 견고하게 했다.
`AnalyticsEvent` 는 **양쪽 다 죽어 있다** — 앱에 `logEvent()` 가 있으나
호출자가 0이고, 서버에 `/devices/<id>/events/` 라우트가 없다.

### 2026-09-13 — 남은 것을 한 번에 (로컬)

원장에 쌓여 있던 **미수정 40건**과 **안 본 화면 4개**를 함께 마쳤다.
새 화면 감사에서 28건이 더 나와, 모두 68건을 검증하고 고쳤다.
시험 132건 추가. 마이그레이션 없음.

커밋 여덟 개로 나눴다 — 부적합 알림 / 기본정보·문서타입 / 미리보기 /
조건패널·의뢰서 / 원료·게시판 / 원료 마무리 / 문서·배합·영양 / BOM 적용.

**고치기 전에 두 번 확인한 것들.** 원장에 올라 있었지만 재현되지 않아
고치지 않은 것도 있다 — 그 판단도 함께 적는다.

- **'모두 읽음' 칩 숫자와 실제 처리 건수** — 이미 맞다. 칩은
  `tab_admin_unread_all`(조건과 무관한 수)을 쓰고 `mark_tab_read` 가 그만큼
  처리한다. 시험으로 확인(5 = 5). 원장이 낡았던 것.
- **제품 조회 '페이지당 25' 인데 10건** — 재현되지 않는다. explorer 는
  `per_page`(25·50·100·200), 원료 목록은 `items_per_page`(10·25·50·100)로
  각각 옳게 돈다. `?per_page=abc` 도 500 이 아니다. 500 이 나던 것은
  **게시판**이었고 그쪽을 고쳤다.
- **BOM 뱃지 td[3]** — V1 목록(`my_label_list.html`, base.html)은 감사
  범위 밖이라 손대지 않았다. 같은 모양이 **V2 원료 목록**에 있었고 그쪽은
  사용자가 칸을 고르므로 실제로 어긋난다 — 그것을 고쳤다.
- **게스트 일괄 복사** — 게스트는 지금 방문마다 격리된 계정을 받고 24시간
  뒤 정리된다(`common/guest.py`). 복사를 막을 이유가 사라졌으므로 막지 않고,
  대신 **등록 한도**를 모든 생성 경로에 걸었다. 게스트도 같은 한도를 받는다.

**감사하다 새로 찾은 것 두 가지.** 원장에 없던 것이다.

1. `bulk_copy_my_ingredients` 가 `pk=None` 으로 save() 해서 한도 검사를
   통째로 비켜 갔다. "objects.create 만 찾으면 놓친다" — 시험이 이제 그
   꼴도 센다.
2. 편집 권한 규칙이 **세 벌**이었다(화면 역할표·서버 역할표·그 위 주석).
   모델의 `can_edit_label` 플래그가 답이라, 그것으로 모았다.

### 2026-09-13 — 스케줄 1회차 (클라우드)

`product_explorer` · `_tab_label` · `product_starred`. 2건 발견·수정.

1. **공유 편집자가 [2차 검증] 을 못 썼다** — `_resolve_editable_label` 이
   이름·docstring 과 달리 오너만 봤다. 화면은 `can_edit` 이면 단추를 보여 주는데
   서버가 조용히 404. 같은 함수를 `discard_if_untouched` 도 쓰지만, 공유가 있는
   제품은 `has_children` 이 '손댄 것' 으로 보므로 지워지지 않는다(시험으로 잠금).
2. **BOM 엑셀에 비활성 행이 섞였다** — `bom_save_api` 가 저장할 때마다 옛 행을
   `active_yn=False` 로 눕히는데 엑셀에 그 조건이 없었다. 화면의 BOM% 칸과
   엑셀이 서로 다른 말을 했다.

`product_starred` 는 코드는 정상이나 **저장소 어디서도 링크되지 않는 도달 불가
화면**이다(`doc_requests.html` 과 같은 유형). 손대지 않았다.

> **클라우드 실행은 PR 을 올리지 못한다.** 설치된 GitHub 앱(`fam-soo`)에
> `labeldata/labeldata` 쓰기 권한이 없어 git push·REST API·MCP 세 경로가 모두
> 403 이다. 위 두 건은 로컬에서 다시 검증해 직접 반영했다. 권한이 열릴 때까지
> 클라우드 실행은 **발견만 하고 작업은 버려진다** — AUDIT_GUIDE.md 참고.

### 2026-09-12 — 제품 관리 5개 화면

제품 관리의 협업 기능 전체. 발견 46건, 전부 시험으로 재현 확인 후 수정.
자세한 내용은 `IMPROVEMENT_PLAN.md` 11장.

**감사 방법을 바꾼 날이다.** 그전까지는 서버 권한 판정만 봤고, 그래서
"화면이 부르는 함수가 없다"·"보내는 키와 읽는 키가 다르다"·"화면이 그리는
데이터가 context 에 없다" 는 종류를 하나도 못 잡았다. 11-4 참고.

## 링크 없는 화면 — 2026-09-14 조사

저장소 어디에서도 링크되지 않는 여섯을 하나씩 봤다. **판단은 owner 몫이고
아직 아무것도 손대지 않았다.**

| 화면 | 도나 | 다른 데서 되나 | 판단 |
|---|---|---|---|
| `product_recent` | **아니다** | 없다(미구현) | 지운다 |
| `product_starred` / `product_favorite` | 돈다 | **완전히 중복** | 지운다 |
| `doc_requests` (템플릿만, 라우트 없음) | 렌더된 적 없다 | **완전히 중복** | 지운다 |
| `expiring_documents` | 돈다 | 부분적 | **살린다** |
| `expired_documents` | 돈다 | **어디에도 없다** | **살린다** |
| `doc_requests_dashboard` | 돈다(리다이렉트) | 그 자체가 중복 | 지운다 |

### 지운다

**`product_recent`** — 기능이 안 붙은 것이 아니라 **만들다 만 것**이다.
기록을 남기는 `ProductAccessLog.log_access()`(`v1/products/models.py:198`)의
호출자가 **0** 이라 표가 영원히 비어 있고, 한 줄이라도 생기면 템플릿이
`MyLabel` 에 없는 `product_id` 를 읽어 `NoReverseMatch` 로 죽는다
(`v1/templates/products/product_recent.html:29`). 링크를 걸면 500 을 배포하는
셈이다. 함께 죽는 것: `ProductAccessLog` 모델 전체(테이블 드롭 마이그레이션
필요), `views.py:481-483` 의 `recent_product_ids`(어느 템플릿도 안 쓴다).

**`product_starred`** — 홈 대시보드가 이미
`product_explorer?filter=ALL&starred=1` 로 보내고 그것은 **서버 필터**다
(`v1/templates/main/home_v2_dashboard.html:491`, `v1/products/views.py:243`).
이 화면이 더 주는 것은 `starred_datetime` 정렬뿐이다. `urls.py:44`(starred)와
`urls.py:45`(favorite)가 같은 뷰를 가리키는데 **둘 다 안 쓰인다.**

**`doc_requests`** — 라우트도 뷰도 없다. 템플릿만 남은 고아다
(`git log -S"doc_requests.html"` 로 확인 — `views.py` 에 그 이름이 한 번도
들어간 적이 없다). 기능은 연락처 관리로 옮겨졌다: 받은 요청은
`contacts.html:111-130`, 보낸 요청은 `:357-373`, 둘 다 사이드바
`base_v2.html:114` 에서 닿는다. 지울 때 `v1/products/tests.py` 의 세 자리
(`:6526`, `:6571`, `:6606`)에서 파일 이름을 함께 빼야 한다 — 색·스낵바 검사
목록에 들어 있다.

**`doc_requests_dashboard`** — 본문이 `redirect('products:contacts')` 세
줄이고, 그 목적지는 사이드바에 이미 있다. 자기 docstring 이 "연락처 관리로
통합" 이라고 적어 두었다. `base_v2.html:115` 의 활성 표시 조건에 이름이
남아 있는데, 아무도 그 주소로 가지 않으므로 켜질 일이 없다.

### 살린다

**`expired_documents`** — **제품을 이미 알고 있지 않으면 볼 방법이 없다.**
만료일이 지나는 순간 세 자리에서 동시에 사라진다 — 대시보드 칩
(`v1/main/views.py:426`), 탐색기 카드(`views.py:263`), 만료 임박 목록
(`views.py:4570`). 셋 다 `expiry_date__gte` 로 거른다. 그리고 **만료 알림을
보내는 코드가 없다**(위 「남은 것」 참고). 이 화면은 한 번 500 이 나던 것을
`fbf6145` 에서 고쳐 두었다 — 아무도 못 여는 화면에 들인 수고다.

**`expiring_documents`** — 홈 칩이 "N건 만료 임박" 이라고 **문서 수**를 세어
놓고 **제품 목록**으로 보낸다(`home_v2_dashboard.html:330`). 숫자와 도착지가
어긋난다.

**제안한 자리** — 새 UI 없이 링크 둘이다.

1. `home_v2_dashboard.html:330` 의 만료 임박 칩을
   `{% url 'products:expiring_documents' %}` 로 돌린다. 그 칩이 이미 문서를
   세고 있으므로 이름과 목적지가 맞게 된다.
2. 그 옆에 「N건 만료됨」 칩을 하나 더 둔다. `expired_count` 는
   `v1/main/views.py:414-427` 의 `expiring_count` 옆에 `expiry_date__lt` 로
   세 줄이면 된다.

이 저장소에 선례가 있다 — 「문서 타입 관리」도 만들어 놓고 링크를 안 걸어
아무도 만나지 못했는데, 그때 고른 답이 **삭제가 아니라 링크 추가**였다
(`base_v2.html:114` 주석).

## 2026-09-18 — 영양성분 이상 판정과 그 판정이 막는 것

「영양성분 등록 요령」의 검토 규칙 셋을 우리 코드와 대조하는 데서 시작해,
규칙을 넣고, 두 번의 감사로 그 규칙의 결함을 걷어내고, 판정이 실제로 무엇을
막는지까지 갔다. 커밋 `491e93c`..`3ece04c`.

**요령과 대조해서 나온 것**

- 당류 ≤ 탄수화물 · 지방 ≥ 트랜스+포화+콜레스테롤 을 **남의 데이터에만**
  대고 있었다(nutrition_anomaly A1·A3). 사용자가 손으로 넣은 표는 아무도 안
  봤다 — `check_nutrition_internal_consistency` 로 같은 잣대를 댄다.
- 허용오차가 두 곳에서 서로 달랐다(라벨 30 % · 적재 15 %). 규정은 ±20 % 다.
- 에리스리톨을 당알콜로 묶으면 2.4 로 세어진다. 요령은 **0** 이다.

**열량 규칙(C)을 넣으며 배운 것** — 세 번 갈아엎었다

1. 표기값을 분모로 쓰면 **허용오차를 쓴 제품이 모두 경계 위에** 놓인다.
   제조사는 계산값/1.2 를 합법적으로 적는다(표시기준 120 % 규정).
2. 하나의 값과 견주는 것 자체가 틀렸다. 탄수화물 1 g 은 0~4 kcal 사이라
   알 수 있는 것은 **범위**뿐이다. 그 뒤로 감미료 제품 오탐이 사라졌다.
3. 상한은 '열량을 내는 것이 탄단지뿐' 일 때만 상한이다. 유기산·알코올은
   컬럼 자체가 없어(NOT_IN_SOURCE) **주류가 전량 오탐**이었다.

    3,538 -> 2,506 -> 319 -> 275 건

**감사가 짚은 것** (37 + 69 에이전트, 주장마다 반증 시도를 붙였다)

- 확정 58 · 반증 40. **반증 단계가 40 건을 걸렀다** — 조사 결과를 그대로
  믿었으면 틀린 것을 그만큼 보고할 뻔했다. 이 두 단계는 다음에도 나눈다.
- **NutritionAnomaly 표를 읽는 선택 경로가 하나도 없었다.** 판정을 쌓아도
  아무것도 막지 못한다. 지금도 그렇다 — 막는 것은 `unusable_reason()` 이고,
  그것은 표가 아니라 행 자신을 본다.
- 탄단지가 전부 0 인 행은 `verify_row` 의 빈 칸 검사가 열량 검산보다 앞에
  있어 **fail 이 아니라 skip 으로 앉는다.** 모든 필터가 fail 만 걸러서 전
  경로를 통과했고, 라벨 검증마저 '확인 불가' 로 넘겼다.
- `usable_for_recipe` 는 호출부가 **0 건**이었다. 정작 그 일을 하라고 만든
  게이트다.

**운영 수치 (2026-09-18)**

    적재 340,555 · 보고번호 237,485 · 원재료 붙은 행 200,855
    식이섬유 0 초과 16,190 (4.8 %) · **당알콜 0 초과 457 (0.1 %)**
    이상 판정 2,431 (A 1,149 · B 1,007 · C 275)
    recheck_food_nutrition 으로 fail -> pass 423 건

## 2026-09-19 — 원료 사진을 원료에 붙이다

문서함에 올리고 판독해 BOM 으로 보내던 원료 사진 흐름을 뜯어 **원료 중심**으로
다시 세웠다. 커밋 `a7ab2c7`..(이 기록).

**왜 뜯었나**

- `ProductDocument.label` 이 필수라 사진이 늘 어느 한 제품에 매였다. 같은
  크림치즈를 다른 제품에서 쓰면 같은 사진을 또 올려야 했다.
- 증빙 서류와 원료 사진이 같은 창의 같은 목록에 나란히 있어 "문서를 올리러
  왔는데 왜 배합표가 나오지" 가 됐다.
- 봉지 사진 전체를 판독에 보내 조리법의 '소금' 을 원재료로 읽는 식의 오독이
  났고, 2.4 MB 짜리가 문서함에 쌓였다.

**세운 것**

- `MyIngredient.label_photo` / `label_photo_fields` (0034). 옛 원료 표시사항
  문서는 같은 마이그레이션이 눕힌다(active_yn=False, 파일은 남김).
- 자르기(image_crop.js, 브라우저 canvas). 원본은 남기지 않는다. [전체 쓰기]
  로 건너뛴다. 툭 누른 것은 고른 것이 아니다(MIN_SIZE).
- 진입점 둘: 배합 탭 [사진으로 등록](여러 장 → 줄 세워 확인 → BOM 줄 + 원료
  사진) · 원료 관리 [사진 등록](한 장 → 읽기 → **빈 칸만** 채우기).
  문서함에서는 뺐다 — 안내도 남기지 않았다(쓴 사람이 없었다).
- 배합표 행 머리 📷 → 그 자리에서 사진 펼침 + [원료 관리로 이동]. 원료 관리는
  `?ingredient_id=` 로 그 원료를 펼친 채 연다.

**이 갈래에서 시험이 잡아 준 것** — 사람 눈으로는 못 봤을 것들

- rowHeaders 는 화면 좌표인데 getSourceDataAtRow 에 그대로 넣었다(줄을 끌어
  옮기면 사진 표가 엉뚱한 줄에). '데이터를 직접 읽는 곳은 한 곳뿐' 시험.
- `except IngredientQuotaExceeded` 가 그 이름을 import 하지 않고 있었다 —
  예외가 안 나는 동안엔 안 보이는 구멍. 파일 없는 시험이 예외를 내면서 드러남.
- 예비색이 토큰 값과 달랐다(#34a853 vs #1e8e3e). 색 토큰 시험.
- 단추 크기 체계(products.W001/W002). 자체 크기가 필요한 것은 checks.py 에
  까닭을 적어 등록한다 — 조용히 빠져나가지 않는다.

**같은 날 이어서 (Fable 5.1 로 바꾼 뒤)**

- 자르기에 **회전·확대**를 넣었다. 회전은 캔버스에 구워 넣는다 — CSS
  transform 으로 돌리면 화면→원본 좌표 셈이 각도마다 달라져, 잡은 곳과 다른
  곳이 잘리는데 화면에서는 멀쩡해 보인다. 확대는 표시 폭을 키우고 스크롤한다
  (transform scale 이면 옮기는 끌기와 고르는 끌기가 같은 동작이 된다).
- 원료 관리: **새 원료에도** 사진 자리. read 는 번호가 오면 그 자리에서 붙이고,
  새 원료는 화면이 들고 있다가 저장 뒤 attach 로 붙인다(같은 파일을 두 번
  올리지 않는다). 빈 칸은 **화면이** 채운다 — 서버 apply 는 걷어냈다.
- 사진을 누르면 새 탭이 아니라 **왼쪽(목록 자리)** 에 띄운다. 오른쪽 폼과
  나란히 놓고 고친다.
- '업로드·다운로드' → '엑셀 올리기·엑셀 내려받기'. 사진인지 엑셀인지 못 가렸다.
- 배합 탭에서 시작한 줄이 끝나면 **배합표 iframe 만** 다시 그린다. 페이지
  전체 새로고침은 첫 탭(기본정보)을 스치게 했다.
- 이관 뒤 남은 자리 정리: 필수문서 관리 팝업·문서 종류 목록에서 '여러 건'
  구분을 뺐고(hidden 옵션으로 배합 탭만 코드로 고른다), 불러오기 모달의
  '원료로 등록' **사진** 갈래와 `ingredient_photo_upload` 뷰·주소를 걷어냈다.
  품목보고번호로 원료를 만드는 갈래(useLookup)는 사진이 아니라 조회라 남겼다.

**빠진 것 / 다음**

- 배합 탭 줄세우기는 아직 사진을 **문서로** 올려 판독한 뒤 확정 때 원료로
  옮긴다(한 걸음에 갈아엎지 않았다). `document_ingredient_photo_preview` ·
  `_to_bom` 과 문서함 탭의 판독 창이 그 길이다. 원료 관리 쪽은 문서를 거치지
  않는다(`my_ingredient_photo_read`). 둘을 한 길로 합치는 것이 다음 정리다 —
  그때 문서함 탭의 원료 판독 코드(줄세우기·확인 창)가 통째로 옮겨 간다.
- 원료 관리에서 여러 장은 안 받는다(원료 하나 = 사진 하나).

**같은 날 이어서 — 운영에서 난 500 과 그 뒤**

- 배합 탭 사진 → BOM 확정(`document_ingredient_photo_to_bom`)이 운영에서만
  HTML 500. 미리보기는 됐다. SQLite 시험은 통과했다. **MySQL 은 칸보다 긴
  값을 거절한다** — `MyIngredient.prdlst_report_no` 가 16자, `ProductBOM`
  의 제조사 200·식품유형 100. 사진에서 읽은 값은 길이를 모른다('제…호' 로
  감싼 번호, 주소 딸린 제조사). `fit_to_model` / `clean_report_no` 를
  `get_or_create_my_ingredient` (원료를 만드는 다섯 길이 다 지난다) 와
  `register_ingredient_bom` 에 넣었다. 뜻밖의 예외는 JSON 500 으로 — HTML 이
  가면 화면은 "Unexpected token '<'" 만 보여 준다. 서버 로그로 확정할 것.
- 원료 관리: 고른 사진을 **왼쪽 패널에서** 자른다(무대 #ingPhotoStage 를 폼에서
  패널로 옮겼다가 돌려놓는다). 폼 안에 두면 오른쪽 아래에 작게 뜨고 스크롤이
  생겨 잘 보이지 않았다. 옛 사진은 갈아 끼울 때 지운다(`replace_photo`).
- 세 관점 검토 + 반박 검증(18 에이전트): 15건 중 **13건 확인**, 전부 고쳤다.
  사람 눈으로 못 봤을 것들 —
  · 옛 파일을 트랜잭션 **안에서** 지웠다. 되돌려지면 DB 는 옛 이름을 가리키는데
    파일이 없다 → `transaction.on_commit`. 시험은 `captureOnCommitCallbacks`.
  · `bulk_copy` 로 복사한 원료는 **같은 파일**을 본다 → 다른 행이 보는 파일은
    두다.
  · 점 많은 파일 이름('a.b.c….jpg')은 저장소가 뒤쪽을 전부 확장자로 보아
    SuspiciousFileOperation → 이름을 다듬는다(뿌리 40자).
  · 패널로 옮긴 무대가 목록 패널(overflow:hidden) 을 넘쳐 **단추 줄이 안
    보였다**(headless Chrome 1920×950 으로 확인) → 패널이 스크롤을 맡는다.
  · 새 원료에서 고른 사진(`pendingPhoto`)이 폼을 갈아도 살아남아 **다음에
    저장하는 엉뚱한 원료에 붙었다** → 폼이 바뀌면 버리고, 방금 만든 원료에만
    붙인다. V2 는 저장 뒤 주소를 바꾸므로 붙이기가 끝난 뒤에 움직인다.
  · 단독 상세 페이지는 `initLabelPhoto` 를 한 번도 부르지 않았다(기능이 죽어
    있었다). 목록·문자열 JSON 은 try 앞에서 터졌다. 늦게 온 PDF 결과가 새로
    고른 사진을 덮었다(세대 번호). 폼에서 누른 Esc 가 무대를 허물었다.
- 배합 탭 사진 → BOM 확정 창: **비슷한 원료는 추천하되 정하는 것은 사람.**
  후보(번호·제조사·유사도)를 라디오로 늘어놓고 [기존 원료에 연결] / [새 원료로
  등록]. 새로 등록은 같은 정보가 있어도 만든다(`allow_duplicate`) — 중복은
  나중에 원료 관리에서 정리하기로 사용자가 정했다. 아무것도 안 보내면(품목보고
  번호 길, 옛 화면) 옛 규칙(90점이면 붙임) 그대로.

## 2026-09-20 — 화면 중심 도움말과 단추 정리 (세 관점 감사 · 검증 · 종합)

사용자 말: "버튼이 많아지면서 사용자들이 기능을 제대로 이해 못할 것 같아 …
도움말의 '이 화면 사용법'을 현재 보고 있는 페이지 중심으로." 네 화면(기본정보·
문서함·권한 / 배합 / 영양성분 / 검증·원료 관리)을 감사하고 반박 검증한 뒤 종합
(9 에이전트). 보고서 원문은 스크래치패드 `ux_report.md`, 요지는 아래.

**넣은 것** (`95c4da0`..`57a1083`)
- 도움말 엔진이 **iframe 안**을 가리킨다(`data-frame`, 같은 출처라 contentDocument
  직접). 지금 열린 탭의 걸음이 앞에 선다. 같은 갈래의 묶음을 모두 잇는다(원료
  관리 목록+상세). 검증 틀은 서브탭이 접혀 있으면 검증 서브탭을 먼저 연다.
- 걸음 전면 교체: 기본 정보 8·배합 8·영양성분 7·검증 8·문서함 4·권한 3, 원료
  관리 6+6. 모두 파일에서 선택자를 확인했고 시험이 대조한다.
- 칸 고르기 단추를 표 왼쪽 위 모서리 칸으로(afterGetColHeader col -1). id 는
  달지 않는다 — 본체·겹침판이 모서리를 둘 다 그려 같은 id 가 둘이 된다.
- [저장] 라벨이 탭을 따른다(기본 정보 저장/BOM 저장/…), 문서함·권한에서는 잠금.
  건너뛰기는 confirm. '불러오기' 둘을 '번호·사진으로 채우기'/'고정 서류 가져오기'
  로. 배합표 칸 [적용] 전에 saveIfEdited. 검증 탭 안 저장 안내 한 줄.
- **브라우저 시험**(`v1/common/browser_checks.py`, DevTools 로 헤드리스 크롬을
  몬다; `tests_browser.py` 가 파일 SQLite 설정으로 따로 띄운다): 배합표 줄 번호
  정렬, 모서리 단추, 배합 탭 도움말이 iframe 안 표를 감싸는가.

**아직 안 한 것 — 사용자 판단 대기** (보고서 1-A~1-E)
- 권한 탭 역할 이름 통일(네 곳이 다름), 헤더 [공유]→'권한 설정'(outline).
- BOM: ①~④ 순서 안내가 iframe 에서 숨겨짐 → 빈 표 카드로; 닿지 못하는 두 단추
  (라벨 정보 불러오기·붙여넣기 양식) 자리; markBomEdited 누락 다섯 곳; 읽기 전용
  정리; 죽은 `#context-view`.
- 영양성분: '적용값·계산값·인쇄될 값' 이름 통일, 출처 고르기 전 힌트, 배합 계산
  뒤 저장 안내 문구, ③ 번호, 개발자 문구.
- 검증: ↶ 초기화 자리, 항목 순서 동작 줄 이름, 내보내기 메뉴 설명 이식.
- 원료 관리: 엑셀 입구 통합(붙여넣기+올리기), 선택 동작 줄 이름, [저장만]→
  '사진만 붙이기', 신규 폼의 연결 표시사항 단추.
- 죽은 모달 넷(`#roleAssignModal` 등) 제거, 댓글 단추 아이콘.

## 남은 것

> 이 목록은 **화면 목록이 아니다.** 화면 목록은 매번 `extends base_v2` 를
> 훑어 새로 만든다(`AUDIT_GUIDE.md`). 여기에는 "봤고, 고칠 값이 있다고
> 판단했으나 아직 안 고친 것" 만 적는다.
>
> **2026-09-14 에 전건 재확인했다.** 앞서 여기 있던 문서함 여섯 · 미리보기
> 넷 · 모바일 서버 일곱 · 모바일 앱 열둘은 그 사이에 고쳐졌다 — 커밋
> `713c766`..`a297442`. 지금 열려 있는 것만 남긴다. 자세한 것은 맨 위
> 「인계」 절.

**아직 안 고친 것**

- 만료 알림·자료 요청 독촉이 **예약에 걸려 있지 않다.** 커맨드는 둘 다 있고
  `daily_reminders` 로 묶여 있다. PythonAnywhere Tasks 가 **개수 상한에 걸려
  새 자리를 못 만든다** — 기존 줄 끝에 `;` 로 붙이는 것이 답이다.
- 칸 순서 도우미가 9열을 가정한다(감춘 칸이 있으면 `null` 이 섞인다).
- **당알콜 칸이 MyLabel 에 없다.** 적재·성적서·배합·원료는 이미 값을 만드는데
  라벨에 받을 칸이 없어 화면의 아홉 목록에서 버려진다. 감사 결과 손볼 자리가
  최소 11 곳이고, 정작 **식약처 DB 적재율이 0.1 %** 라 칸을 만들어도 자동으로
  채워질 일이 거의 없다 — 채우는 곳은 성적서 판독과 사람 손뿐이다.
  사용자 판단으로 **대기**(2026-09-18).
- 타가토스·알룰로오스·에리스리톨은 **어떤 모델에도 칸이 없다.** 계수만 있다.
  값이 올라올 경로가 아예 없으므로 당알콜과 작업 범위가 다르다.
- 이상 판정 목록에서 **사람이 할 수 있는 행동이 코드에 없다.** 읽기 전용이고,
  본 뒤에 그 행을 쓰지 않게 표시하는 길이 없다.
- `label_preview.js` 의 죽은 검증 덩어리. 참조 0인 함수 여섯(131줄)은
  걷어냈다. 원장에 적혀 있던 1,446줄 범위는 **줄 번호로 적혀 있어 지금은
  못 믿는다** — 지울 것이면 참조를 다시 세어 고른다.

**판단이 필요한 것** — 맨 위 「인계」 절의 ①~④ 를 본다.

**다음 감사가 볼 것** — 이미 본 화면도 다시 본다. 한 번 본 화면에서 또
나온다는 것을 두 번 확인했다(권한 탭은 두 번째 감사에서 더 나왔고, 미리보기·
배합표도 그랬다).
