# V2 화면 감사 지침

> 매일 한 번 도는 클라우드 에이전트가 이 문서를 읽고 그대로 따른다.
> 사람이 직접 감사할 때도 같은 기준을 쓴다.

## 왜 이 감사를 하는가

앞선 감사는 **"서버가 막아야 할 것을 막는가"** 만 봤다. 권한 플래그가
검사되는지, 권한 없는 계정이 자료를 받을 수 있는지 — 전부 문을 지키는지였다.

**사용자가 그 문에 닿을 수 있는지는 한 번도 묻지 않았다.** 그래서 한 종류의
버그를 통째로 놓쳤다 — 권한을 줄 사람을 검색할 수 없어 권한 설정 화면이 아무
일도 못 하는 상태였는데, 서버 시험은 이메일을 코드로 직접 넣어 공유를
만들었으므로 늘 통과했다.

자세한 경위는 `IMPROVEMENT_PLAN.md` 11-4 에 있다.

## 감사 기준 — 화면의 모든 상호작용 요소에 대해

단추·입력칸·select·체크박스·끌어다 놓기·탭·카드 클릭·링크·모달·파일 선택을
**하나도 빼지 않고** 열거한 뒤, 각각에 대해 여섯 가지를 본다.

| # | 묻는 것 | 서버 시험으로 잡히는가 |
|---|---|---|
| 1 | 부르는 JS 함수가 실제로 존재하는가 | **안 잡힌다** |
| 2 | 같은 이름의 function 이 한 페이지에 둘 있지 않은가 | **안 잡힌다** |
| 3 | 화면이 보내는 POST/JSON 키와 뷰가 읽는 키가 같은가 | **안 잡힌다** |
| 4 | 화면이 그리는 데이터가 서버 context 에 실제로 담기는가 | **안 잡힌다** |
| 5 | 성공 후 화면을 갱신하는가 | 안 잡힌다 |
| 6 | 실패했을 때 사용자에게 무엇이라도 말하는가 | 안 잡힌다 |

2번은 **뒤에 파싱된 것이 이긴다.** 어느 쪽이 이기는지는 include 위치에만
달려 있어 아무도 의도하지 않는다. 실제로 `deletePerson` 이 그랬고, 공유를
지우는 길이 아예 없었다.

## 이 저장소에서 실제로 났던 유형

찾을 때 이 목록을 옆에 두라. 같은 모양이 다른 화면에 또 있다.

- **행 번호를 원본 배열 인덱스로 쓴다** — 필터를 켜면 격자는 부분집합이고
  삭제(splice)·행이동 뒤에는 번호가 밀린다. 엉뚱한 행이 수정·삭제된다
- **안 보낸 칸을 빈 값으로 덮는다** — 화면이 한 칸을 빠뜨리면 그 데이터가
  사라진다. 서버가 "안 온 것"과 "빈 것"을 구분해야 한다
- **GET 의 렌더 조건과 POST 의 검증이 두 벌** — 한쪽만 고쳐진다. 서버는
  허용하는데 단추가 안 그려지거나, 화면은 열어 주고 서버가 막는다
- **선언만 있고 호출 0회인 상수·함수·URL** — 고쳐도 화면이 안 바뀐다.
  `contacts_api_list` 가 그랬다
- **스타일이 마크업 뒤에 있다** — 브라우저가 스타일 없이 한 번 그린 뒤 다시
  그려, 화면 열 때 깜빡인다
- **조작마다 알림이 발송된다** — 드래그로 자리를 몇 번 옮기면 이메일이
  그만큼 나간다. 모아서 확정하는 단계가 필요하다
- **예외 원문이 사용자에게 간다** — `No ProductDocument matches the given
  query.` 가 스낵바에 떴다
- **권한 게이트가 일부 경로에만 걸려 있다** — 툴바 단추는 감췄는데 칩
  onclick 에는 조건이 없었다
- **가시성 규칙이 목록에만 적용되고 통계·슬롯에는 빠졌다** — 자기가 못 보는
  문서까지 세어 카운터가 목록과 어긋났다

## 범위

**대상**: `base_v2.html` 을 (직접 또는 상속으로) 타는 템플릿, 그에 include
되는 부분 템플릿, 그것들이 부르는 뷰·URL·정적 JS.

**제외**:
- `base.html` 을 타는 것 (V1 전용)
- 파일명이 `_v1.html` 로 끝나는 것
- `label/label_creation.html`, `main/home.html` (V1 모드 화면)

### 화면 목록은 **그때그때 만든다**

원장에 "안 본 화면" 목록을 적어 두고 지워 나가던 시절이 있었다. 그 방식이
무너졌다 — 2026-09-13 에 "다 봤다" 고 적어 두고 실제로 훑어 보니 **한 번도
감사한 적 없는 화면이 열다섯 개 넘게** 있었다. 로그인·가입 같은 **인증 화면
전체**가 그중에 있었다. 목록을 누가 처음 만들었는지 아무도 확인하지 않은
탓이다.

그러니 목록을 믿지 말고 **매번 새로 만든다.**

```bash
# V2 페이지 — base_v2 를 타는 것
grep -rl 'extends ["'"'"']base_v2.html' v1/templates/

# 그 페이지들이 include 하는 조각
grep -rho '{% include ["'"'"'][^"'"'"']*' v1/templates/ | sort -u

# 어디에도 extends 가 없는 것 — 팝업·부분 화면이 여기 숨는다
for f in $(find v1/templates -name '*.html'); do
  head -3 "$f" | grep -q extends || echo "$f"
done
```

세 목록을 합친 것이 오늘의 대상이다. 거기서 위 **제외** 규칙에 걸리는 것만
뺀다.

## 순서

1. 위 세 명령으로 **오늘의 대상 목록을 새로 만든다.** 원장의 표는 참고만
   한다 — 무엇을 언제 봤는지 알려 주지만, 무엇이 남았는지는 알려 주지 못한다.
2. **그 목록을 하나도 빼지 않고 훑는다.**

   화면 수가 많으면 나눠서 돌리되, **나눈 조각의 합이 목록 전체여야 한다.**
   "오늘은 두세 개만" 은 더 이상 하지 않는다 — 그렇게 고르다 보면 아무도
   안 고르는 화면이 생기고, 그것이 인증 화면 전체가 감사 밖에 있던 이유다.

   한 번에 다 못 보면 **영역별로 갈라 병렬로 돌린다.** 2026-09-13 에는 일곱
   갈래로 나눠 한 번에 훑었다(인증 / 제품 상세 조각 / 배합·목록 / 공유·협력사 /
   부적합 조각 / 표시사항 팝업 / 게시판·오류·공용).

   **이미 본 화면도 다시 본다.** 권한 설정 탭은 첫 감사를 통과한 뒤 두 번째
   감사에서 더 나왔다. 한 번 봤다고 건너뛰지 마라.
3. 위 기준으로 감사한다. **코드로 확인한 것만 단정하고, 추측은 추측이라고
   적는다.**
4. 발견한 것이 있으면 **고치기 전에 먼저 그 증상을 재현하는 시험을 써서
   실패를 확인**하고, 고친 뒤 통과를 확인한다. 시험은 `v1/<app>/tests.py` 에
   넣는다. 재현되지 않으면 고치지 않는다 — 감사가 틀린 것이다.
5. 검사를 돌린다.

   ```bash
   python manage.py check
   python manage.py test v1 --settings=v1.config.settings_test
   ```

   `settings_test` 를 써야 한다 — 운영 DB 계정에 테스트 DB 생성 권한이 없다.
   필요하면 `pip install -r requirements.txt` 를 먼저 한다.
6. `.audit-ledger.md` 에 오늘 본 화면과 결과를 덧붙인다. 발견이 0건이어도
   "봤고 문제 없었다" 를 적는다.

   **"남은 화면 없음" 이라고 적지 마라.** 그 문장이 다음 감사를 멈춰 세운다.
   적을 것은 "오늘 이 목록을 훑었다" 이지 "이제 볼 것이 없다" 가 아니다.

   재현되지 않아 **고치지 않은 것**도 적는다. 그 판단이 왜 그랬는지 남겨야
   다음 사람이 같은 것을 다시 파고들지 않는다.
7. **PR 을 올린다.** `main` 에 직접 올리지 않는다 — 운영 서버가 `main` 을
   그대로 받아 가므로, 사람이 보지 않은 변경이 바로 배포된다.

   > **지금은 클라우드에서 푸시가 막혀 있다.** 설치된 GitHub 앱에
   > `labeldata/labeldata` 쓰기 권한이 없어 git push·REST API·MCP 세 경로가
   > 모두 403 이다. 푸시가 안 되면 **커밋까지만 하고, 무엇을 어디에서 찾아
   > 어떻게 고쳤는지를 보고에 그대로 적어라** — 그 보고만으로 사람이 다시
   > 재현하고 반영할 수 있어야 한다. 파일:줄과 수정 내용을 빠뜨리지 마라.
8. 발견이 0건이면 PR 없이 원장만 갱신하고, 남은 화면 수를 보고한다.

## 커밋·PR 메시지

문장으로 길게 적지 않는다. 제목 한 줄에 주요 내용을 요약하고 본문은 짧은
항목으로. **마이그레이션 유무를 반드시 적는다.**

```
문서함 감사 — 성적서 저장 경로 없음, 권한 게이트 누락

- openSpecNutritionPicker 를 부르는데 그 함수가 저장소에 없었다
- 읽기 전용인데 슬롯 칩 onclick 에 권한 조건이 없었다

시험 8건 추가. 마이그레이션 없음
```

## 언제 끝나는가

**전수 목록을 한 바퀴 훑어 발견이 0건일 때.** 그때 `.audit-ledger.md` 맨
위에 그렇게 적고 스케줄을 멈춰도 된다고 보고한다.

"안 본 화면이 없다" 는 끝났다는 뜻이 아니다 — 그 둘을 한 번 헷갈려서
감사가 멈출 뻔했다. 끝은 **훑었는데 나오지 않는 것**이지 **볼 것이 없는
것**이 아니다.
