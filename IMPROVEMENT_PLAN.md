# 기능 개선 — 하나뿐인 작업 문서

> 마지막 손질 2026-09-11. **같은 주제의 문서를 둘로 두지 않는다** — 그러면 어느
> 날 한쪽만 고쳐지고, 늙은 쪽이 현재 기준인 척한다. 실제로 그랬다(아래 3장).
>
> 이 문서는 셋을 합친 것이다.
>
> | 원래 문서 | 어디로 |
> |---|---|
> | `NUTRITION_UX_PLAN.md` | 1장 · 2장 · 5장 · 6장 |
> | `REGULATORY_ALERT_PIPELINE.md` | 4장 |
> | `FOOD_CATEGORY_REFACTORING_DESIGN.md` | 3장 — **채택되지 않았다.** 왜 안 옮겼는지만 남긴다 |

## 목차

1. [영양성분 표와 검증 화면](#1-영양성분-표와-검증-화면)
2. [끝난 것과 남은 것](#2-끝난-것과-남은-것)
3. [식품구분 4분류 — 채택하지 않았다](#3-식품구분-4분류--채택하지-않았다)
4. [부적합·처분 알림 — 흐름과 남은 것](#4-부적합처분-알림--흐름과-남은-것)
5. [게스트와 튜토리얼](#5-게스트와-튜토리얼)
6. [무료 도구 + 광고 모델 접목](#6-무료-도구--광고-모델-접목)
7. [데이터가 모이게 만든다 — 영양성분 · 시안 대조 · 원료 공용](#7-데이터가-모이게-만든다--영양성분--시안-대조--원료-공용)
8. [사용자를 늘리는 길과, 그 전에 무너질 자리](#8-사용자를-늘리는-길과-그-전에-무너질-자리)
9. [실행 계획 — 무엇을 · 어떻게 · 왜](#9-실행-계획--무엇을--어떻게--왜)

---

# 1. 영양성분 표와 검증 화면

> **여기 적힌 것은 2026-09-11 에 전부 고쳤다.** 무엇이 왜 잘못이었는지를
> 남겨 두는 것은, 같은 모양의 문제가 다른 화면에서 또 나오기 때문이다.

## 1-1. 영양성분 표가 세 가지 일을 한 표에서 한다

`templates/products/nutrition_editor.html` 의 Handsontable 5열 구조.

| 열 | 성격 | 지금 화면에서 |
|---|---|---|
| 영양성분 | 이름표 | readOnly |
| 계산값 (100g당) | **사람이 넣는다** | 편집 가능 |
| 단위 | **사람이 고른다** | dropdown |
| 적용값 | **기계가 낸다** | readOnly |
| 강조표시 판정 | **기계가 낸다** | readOnly |

다섯 열이 *이름표 · 입력 · 결과* 세 가지 성격인데, 화면에서는 다섯 칸이 똑같이
생겼다. Handsontable 의 readOnly 는 회색조 정도의 차이라 **어느 칸을 고칠 수
있는지 눌러 봐야 안다.**

### ① 열 이름이 거짓말을 한다

"계산값"이라고 적혀 있지만 **사람이 직접 치는 칸**이다. 배합에서 가져오거나
성적서를 보고 넣는다. "계산"이라는 말은 *누르면 자동으로 채워진다* 는 뜻으로
읽히고, 그런 단추를 찾다가 못 찾고 비워 둔다.

### ② 안 쓰는 줄이 늘 펼쳐져 있다

25행이 언제나 다 그려진다. 표시 의무는 9개이고 나머지는 대개 빈 줄이다.
**원료 상세에서 알레르기 19칩·GMO 6칩을 접었던 것과 같은 문제** — 고를 일이
드문 것이 가장 넓은 자리를 쓴다.

### ③ 넣는 기준과 인쇄되는 기준이 다르다

입력은 **100g당** 고정인데, 인쇄 기준(총내용량당·단위내용량당·100g당)은
**오른쪽 패널에서** 고른다. 화면 양쪽이 서로 다른 기준으로 말하니, 사용자는
"내가 넣은 숫자가 그대로 인쇄되나"를 표만 봐서는 알 수 없다.

### ④ 단위를 거의 안 바꾸는데 매번 묻는다

열량은 kcal, 나트륨은 mg 로 정해져 있다. dropdown 이 한 열을 통째로 쓰지만
실제로 고르는 일은 드물다.

---

## 1-2. 검증 결과는 모달 안에서만 산다

검증은 `tab-label`(4단계 "검증")에 있지만, 결과를 보여 주는 곳은
**확정·승인을 누를 때 뜨는 모달** 하나다.

```
showValidationBlock(newStatus, result)      product_detail.html:1599
  ├ required_missing  →  "비어 있는 필수 입력 항목 N건"
  └ 나머지            →  "그 밖에 확인이 필요한 항목 N건"
         <li> 메시지 </li> + <div> 제안 </div>
```

### ① 고치려면 닫아야 하고, 닫으면 사라진다

모달은 **결정을 막는 도구**지 **고치는 도구**가 아니다. 지적을 읽고 → 닫고 →
칸을 찾아 고치고 → 다시 확정을 눌러야 결과를 본다. 지적이 열 건이면 이 왕복을
열 번 한다.

### ② 어디를 고쳐야 하는지 화면이 안 데려간다

**서버는 이미 답을 보내고 있다.** `_issue(..., fields=(field,))` 로 *어느
칸인지* 가 함께 온다. 그런데 모달은 그걸 안 쓴다.

같은 화면의 우측 표시항목 패널에는 **이미 이동 기능이 있다**
(`.di-name` → `scrollIntoView` + `di-highlight`, `product_detail.html:1670`).
만들어 둔 길을 검증만 안 쓰고 있다.

### ③ 심각도가 평면이다

서버는 `advisory=True` 로 *"근거가 있으면 써도 되는 말"* 과 *"규정 위반"* 을
가른다. 화면은 둘을 같은 `<li>` 로 그린다. 그러면 지적 스무 건 중 진짜 막는
것이 세 건이어도 **스무 건 다 무겁게 보이고**, 곧 아무도 안 읽는다.

### ④ "검사 못 함"이 통과처럼 보인다

`_unchecked(...)` 는 **자료가 없어 판정 자체를 못 한** 항목이다. 식품유형
소분류가 비면 그 유형의 의무 표시사항이 통째로 빠진다. 이건 *통과가 아니라 안
본 것* 인데, 지금은 지적 목록에 섞여 그냥 한 줄로 보인다.

---

---

# 2. 끝난 것과 남은 것

## 2-1. 계획했던 일곱 가지 — 전부 끝났다

고치는 값이 큰 순서. 서로 독립이라 따로 낼 수 있다.

| 순 | 할 일 | 왜 | 크기 |
|---|---|---|---|
| 1 | **검증 결과를 모달에서 꺼내 패널로** — 검증 탭에 상주, 확정 모달은 요약만 | 닫지 않고 고칠 수 있게. 왕복 열 번이 한 번이 된다 | 중 |
| 2 | **지적 → 그 칸으로 이동** — `fields` 는 이미 온다. `.di-name` 패턴 재사용 | 이미 있는 두 조각을 잇는 일. 가장 싸고 효과가 크다 | 소 |
| 3 | **심각도 3단** — 막음 / 확인 / 조언 | `advisory` 플래그가 이미 있다. 화면만 안 쓴다 | 소 |
| 4 | **"검사 못 함"을 따로 묶기** | 안 본 것을 통과로 읽으면 검증이 있으나 마나다 | 소 |
| 5 | **영양 표: 입력칸과 결과칸을 눈으로 가르기** — 결과열 옅은 바탕 + 열 머리에 자물쇠 | 다섯 칸이 똑같이 생겨 눌러 봐야 안다 | 소 |
| 6 | **영양 표: 열 이름 고치기** — 계산값 → **입력값**, 적용값 → **인쇄될 값** | "계산"은 자동으로 채워진다는 뜻으로 읽힌다 | 소 |
| 7 | **영양 표: 안 쓰는 줄 접기** | 25행 중 대개 9행만 쓴다 | 중 |

> **2·3·4 는 하루치다.** 셋 다 *서버가 이미 보내는 것을 화면이 안 쓰는* 문제다.
> 새로 계산할 것도, 마이그레이션도 없다. 1번(패널화)이 제일 크지만 나머지를
> 담을 그릇이라 먼저 하는 편이 낫다.

---

모두 2026-09-11 에 들어갔다(커밋 `1bf063c` ~ `6c2de98`).

## 2-2. 하다가 걷은 것 — 영양성분 표 접기

7 번(안 쓰는 줄 접기)은 **넣었다가 걷었다.** 표시 의무 9 줄만 남기고 16 줄을
접었더니 마지막 줄(나트륨)의 글자가 잘렸고, **네 번 고쳐서 네 번 다 잘렸다.**

    ① refreshDimensions() 를 부른다         -> 잘림
    ② 줄수 × 22 + 머리글 로 칸을 못 박는다   -> 잘림
    ③ 그려진 높이를 재서 못 박는다           -> 잘림
    ④ height:'auto' 로 맡기고 자르지 않는다  -> 잘림

네 번 **다르게** 고쳐서 네 번 다 같은 곳이 잘렸으면, 고치는 방법이 아니라
**접는다는 생각 자체가 이 표에 안 맞는 것**이다. Handsontable 이 아는 높이와
담는 칸의 높이를 두 곳에서 정하는 한 어긋날 자리가 남는다.

자리는 다른 길로 아꼈다 — 위쪽 설정 두 묶음의 폭을 7:3 으로 나눠 한 줄 줄였다
(`.cfg-group--spec`). 정말 더 아쉬우면 **안 쓰는 성분을 표에서 빼는 것이 아니라
처음부터 넣지 않는 것**(고른 성분만 줄로 만든다)으로 간다. 그건 높이가 아니라
데이터를 다루는 일이라 이 함정에 안 빠진다.

## 2-3. 다음

| 할 일 | 크기 | 비고 |
|---|---|---|
| **튜토리얼** | 중 | 5 장. 기반 둘(`[?]` 12 곳, `_WORKFLOW_STEPS`)은 이미 있다 |
| 무료 도구 `/tools/nutrition-table/` | 중 | 6 장. OCR 비용에 IP 한도 필수 |
| 시안 판독 정확도 | — | 함유 상자를 못 읽는다. 잘라 올리면 낫다는 안내는 이미 있다 |
| 부적합·처분 화면 정리 | 중 | 4 장 10 절 |


---

# 3. 식품구분 4분류 — 채택하지 않았다

`FOOD_CATEGORY_REFACTORING_DESIGN.md`(2025-12-25, 971 줄)는 식품구분을 **네
가지**로 나누는 설계였다.

    domestic_processed · import_processed · 식품첨가물 · 농수축산물

**구현되지 않았다.** 지금 코드는 **세 가지**다.

    processed · additive · agricultural        (v1/label/views.py, 원료 상세 화면)

`domestic_processed` 와 `import_processed` 는 저장소 어디에도 없다. 국내/수입은
식품구분이 아니라 **수입 여부**(`is_imported`)로 다루고 있고, 원산지·품목보고번호
검사도 그 값을 본다(`validation_service._applies_to`).

## 왜 이 문서를 통째로 옮기지 않았나

971 줄 중 대부분이 **그 네 분류를 전제로 한 화면·검증·모델 설계**다. 전제가
달라졌으므로 그대로 옮기면 "현재 기준인 척하는 옛 설계" 가 된다 — README 가
경고한 바로 그 사례이고, 예전에 판독 문서에서 한 번 겪었다(1,068 줄짜리
미채택 설계안).

원문이 필요하면 이력에서 꺼낸다.

    git log --follow -- FOOD_CATEGORY_REFACTORING_DESIGN.md

## 살아남은 생각

설계안 전체는 버리되, 지금도 유효한 것 둘은 여기 적어 둔다.

- **식품구분이 화면을 바꾼다.** 식품첨가물이면 표시규정 카드가 뜨고 식품유형이
  고정된다 — 지금 원료 상세가 그렇게 동작한다.
- **구분마다 필수값이 다르다.** 수입 제품은 품목보고번호가 없는 것이 정상이라
  검증이 지적하지 않는다. 이건 구현돼 있다.


---

# 4. 부적합·처분 알림 — 흐름과 남은 것

> 한 화면(`/regulatory/`)의 세 탭이 모두 "알림" 이다. 그런데 그동안 셋이
> 서로 다른 말과 서로 다른 주소를 썼다. 지금의 흐름을 한 줄로 세우고,
> 어디를 고치면 세 탭이 같이 바뀌는지를 적는다.

## 4-1. 네 단계와 그 경계

| 단계 | 하는 일 | 코드 |
|---|---|---|
| **수집** | 식약처 OpenAPI·새올에서 원본을 받아 저장 | `services/collector.py` |
| **매칭** | 원본 ↔ 내 제품·원료·업체를 맞춰 "내 알림"을 만든다 | `services/matcher.py`, `collector._trigger_inspection_match` |
| **표시** | 목록·배지·상세로 보여 준다 | `views.news_list`, `selectors.py` |
| **처리** | 읽음 / 조치 / 끄기 | `views.mark_tab_read`, `views.save_match_action`, `services/mute.py` |

경계 규칙 하나만 지키면 화면이 어긋나지 않는다.

> **건수를 세는 규칙은 `selectors.py` 한 곳에만 둔다.**
> 사이드바 배지·홈 대시보드·탭 배지·목록 헤더·카운트 API 가 모두 여기를 본다.
> 예전에 이 규칙이 네 곳에 복제돼 화면마다 숫자가 달랐다.

---

## 4-2. 알림은 네 갈래로 들어온다

| 갈래 | 무엇이 걸리나 | 저장되는 표 | 끄는 법 |
|---|---|---|---|
| 내 제품 매칭 | 뉴스 키워드 ↔ 내 BOM·원료·제품명 | `NewsProductMatch` | 키워드/원료 **끄기** |
| 원료 보관함 매칭 | 뉴스 키워드 ↔ BOM 미연결 원료 | `NewsIngredientMatch` | 키워드/원료 **끄기** |
| 키워드 알림 | 내가 등록한 키워드 | `NewsKeywordMatch` (+ 기기별 `PushNotificationLog`) | 그 **키워드 삭제** |
| 수거검사 | 품목보고번호·인허가번호·업소명 | `InspectionMatch` | 업소명 **끄기** |

네 갈래 모두 **사용자**에 붙는 표에 남는다. 키워드 매칭만 예외로 기기별
`PushNotificationLog` 에만 남던 시절이 있었는데, 그 표는 `AppDevice` 마다 행을
만들기 때문에 **앱을 깐 적 없는 웹 사용자에게는 행이 하나도 안 생겼다.**
등록해도 "일치하는 정보가 없습니다" 가 뜨고, 목록 배지도 상세 구역도 나오지
않았다 — 웹에서는 기능이 통째로 없는 것과 같았다.

지금은 판정 한 번에서 두 가지가 나온다.

| | 무엇 | 누가 보는가 | 기기가 없으면 |
|---|---|---|---|
| `NewsKeywordMatch` | 사용자 ↔ 뉴스 ↔ 규칙 | 웹 목록·상세·배지 | **생긴다** |
| `PushNotificationLog` | 기기 ↔ 뉴스 | 앱 알림함·FCM 발송 줄 | 안 생긴다(보낼 곳이 없다) |

규칙을 지우면 `NewsKeywordMatch` 도 함께 사라지고(CASCADE), 규칙을 끄기만 하면
남되 읽음으로 내려간다. 규칙이 사라져도 "무엇 때문에 걸렸는지" 는 보여야 하므로
키워드·분류는 매칭 행에 베껴 둔다.

**받겠다(`AlertRule`) 와 안 받겠다(`AlertMute`) 는 서로 다른 표다.**
같은 말을 양쪽에 넣으면 어느 쪽이 이겼는지 화면에서 설명할 수 없으므로,
직접 등록한 키워드를 끄려 하면 409 로 되돌려 보내 키워드를 지우게 한다
(`views.alert_mutes_api`).

---

## 4-3. 처리는 세 가지뿐이고, 서로 다른 일이다

세 탭 어디서든 같은 말과 같은 단추를 쓴다.

| 처리 | 뜻 | 되돌릴 수 있나 | 주소 |
|---|---|---|---|
| **모두 읽음** | 봤다 | (다시 안 읽음으로 못 돌림) | `POST api/mark-tab-read/` |
| **조치** (진행 중·완료·해당없음·메모) | 처리했다 | 이력이 쌓임 | `POST api/save-action/` |
| **끄기** | 앞으로 안 받겠다 | 받지 않기 목록에서 해제 | `POST api/alert-mutes/` |

**읽음과 조치 완료를 섞지 않는다.** 예전에는 부적합·행정처분 탭에서 배지를
지울 방법이 상세 하단의 "전체 알림 일괄 처리"(= 되돌릴 수 없는 조치 완료
기록) 뿐이라, 그냥 훑고 넘기려던 사람이 조치 이력을 남겨야 했다.
지금은 세 탭 모두 툴바에 **모두 읽음**이 있고, 그 단추는 조치 이력을 남기지
않는다.

---

## 4-4. "왜 나한테 왔는가" 는 상세의 표준 항목이다

세 탭의 상세가 모두 **알림 사유** 라는 같은 제목·같은 자리를 쓴다.
좌변(원본 쪽 값)과 우변(내 쪽 값)을 화살표로 잇는다 — 어느 쪽을 꺼야 하는지는
사람마다 달라서, 양쪽을 다 보여 줘야 고를 수 있다.

```
알림 사유   [대장균]  ↔  [정제수]        위해도 50pt · 유사도 90
🔕 이것 때문에 알림이 잦다면  [키워드 '대장균' 끄기] [내 원료 '정제수' 끄기]
```

- 행정처분·지자체처분은 업체명 하나로 걸린다 → 좌변이 업체명이고, 끄는 것도 업체다.
- 수거검사는 업소명이 오탐의 주범이다(회사명이 짧으면 남의 업체까지 걸린다)
  → 업소명을 끈다. 내 제품 번호가 맞아 걸린 건은 끄는 것이 아니라
  "해당 없음(이 건만 삭제)" 이 맞다.
- 키워드 알림은 **내가 등록한 것** 때문에 왔다 → 그 자리에서 바로 지운다.
  예전에는 설정 모달을 열어 등록해 둔 목록에서 같은 말을 눈으로 찾아야 했다.

---

## 4-5. 끄면 두 가지가 함께 일어난다 (`services/mute.py`)

1. **앞으로 안 생긴다** — `matcher` 와 `collector` 가 `AlertMute` 를 보고 매칭을
   만들지 않는다.
2. **이미 쌓인 것을 치운다** — 그러지 않으면 껐는데도 목록이 그대로라
   "안 먹었다" 고 판단하고 같은 것을 또 누른다.

치우는 표시는 기존의 **오탐지/해당 없음과 같은 것**을 쓴다
(`false_positive_yn` / `dismissed_yn`, 수거검사는 삭제).
상태를 새로 만들면 배지·필터·오탐 학습이 저마다 다른 규칙을 갖게 된다.

되돌리면(해제) **앞으로 오는 것부터** 다시 온다. 치운 지난 알림은 되살리지
않는다 — 되살리면 정리한 목록이 갑자기 불어나 무엇이 새 알림인지 알 수 없다.

**한 가지 예외를 둔다.** 뮤트로 한꺼번에 치워진 건은 오탐 학습(Approach C)의
모수에서 뺀다. 한 번에 수백 건이 오탐으로 표시되므로 그대로 태우면 그 키워드의
FP 누적이 폭증해, 다른 원료에 걸린 정상 매칭까지 등급이 깎인다
(`matcher._load_fp_patterns_for_user`).

---

## 4-6. 앱(식품 안심 알리미) 푸시 — 웹과 같은 규칙을 따른다

푸시는 **수집 즉시 나가지 않는다.** 로그만 먼저 쌓고
`send_pending_alerts` 커맨드가 일 3회(10·14·17시 KST) 배치로 내보낸다.
수거검사 판정 변동(`PHASE_JUDGMENT`)만 예외로 즉시 나간다.

```
수집 03:00 ─→ 매칭 ─→ PushNotificationLog(sent_at=NULL) / InspectionMatch(fcm_sent_at=NULL)
                                   │
                        (여기 사이에 사용자가 끄면?)
                                   │
배치 10:00 ─→ 사용자별로 묶어 FCM 1건
```

이 시차가 "껐는데 또 온다" 의 원인이었다. 03시에 걸린 알림을 09시에 꺼도,
로그는 발송 대기에 그대로 남아 10시에 울렸다. 키워드 삭제는 더 나빴다 —
`PushNotificationLog.rule_triggered` 가 `SET_NULL` 이라, 규칙을 지우면 로그는
"누가 부른 알림인지"만 잃은 채 그대로 나갔다.

그래서 **끄는 모든 길이 예약된 푸시를 함께 거둔다**
(`push_service.cancel_pending_logs`).

| 어디서 | 무엇을 거두나 |
|---|---|
| 웹 — 알림 끄기 | 치운 매칭의 뉴스로 걸린 제품·원료 푸시 |
| 웹 — 키워드 삭제 | 그 규칙으로 걸린 키워드 푸시 |
| 앱 — 키워드 삭제·끄기 (`mobile.views.rule_detail`) | 같음 (비회원은 그 기기만) |
| 웹 — 업체 끄기 | `InspectionMatch` 자체를 지우므로 수거검사 푸시도 함께 사라진다 |

거두는 방법도 갈라 둔다.

- **아직 안 나간 로그**(`sent_at IS NULL`) → 삭제. 보낸 적이 없으니 이력도 아니다.
- **이미 나간 로그** → 읽음 처리만. 앱 알림 탭에 남기되 배지는 내린다.
  지우면 "어제 받은 그 알림" 을 다시 찾을 수 없다.

앱 알림 탭(`mobile.views.notifications_list`)은 `PushNotificationLog` +
`InspectionMatch` 를 병합해 보여 준다. 둘 다 끄기가 정리하는 대상이므로,
웹에서 끈 것은 앱 목록에서도 함께 사라진다 — 따로 맞출 것이 없다.

---

### 4-6-1. 숫자와 점은 같은 것을 말한다

탭 배지에는 두 가지가 붙는다. 둘이 서로 다른 질문에 답하면 어느 쪽을 믿어야
할지 알 수 없다 — 실제로 "부적합 탭은 0건인데 빨간 점이 있다" 는 신고가 나왔다.

| | 무엇을 말하나 | 세는 범위 |
|---|---|---|
| 배지 숫자 | 지금 조건으로 이 탭에 보이는 건수 | 조건 **안** |
| 빨간 점 | 그 숫자 안에 안 읽은 것이 있다 | 조건 **안** |
| '모두 읽음' 단추의 숫자 | 누르면 처리될 건수 | 조건 **밖** |

단추만 조건 밖인 이유: 그 단추는 조건과 무관하게 탭을 통째로 읽음 처리한다.
조건 안에서 센 값을 쓰면 "0건" 이라 해 놓고 여러 건을 처리하게 된다.

조건에 가려 안 보이는 미확인은 네비바 배지(`regAlertBadge`)가 따로 말하므로,
점을 조건 안으로 좁혀도 "안 읽은 게 있다" 는 사실을 잃지 않는다.

---

## 4-7. 한 곳만 고치면 세 탭이 같이 바뀌는 자리

| 무엇 | 어디 |
|---|---|
| 어떤 출처가 '행정처분' 탭인가 | `views.ADMIN_API_SOURCES` |
| 탭 키 이름 | `views.TAB_INSP_NEWS / TAB_ADMIN / TAB_INSPECTION` |
| 건수 산출 규칙 | `selectors.py` |
| 수거검사 판정 상태 | `models.InspectionResult.PENDING_JUDGMENTS / FINAL_JUDGMENTS`, `judgment_status_of()` |
| 끄기 값의 정규화 | `models.normalize_mute_value()` |
| 끄기 단추의 동작 | `static/js/regulatory_alert_mute.js` (세 화면이 함께 쓴다) |
| 모두 읽음 | `views.mark_tab_read` (옛 주소 `inspection_mark_all_read` 도 같은 처리) |
| 예약된 푸시 거두기 | `mobile/services/push_service.cancel_pending_logs` |

---

## 4-8. 화면이 스크립트에 기대지 않게 한다

이 화면은 **탭 전환도, 걸러 보기도, 페이지 넘김도 전부 페이지 이동**이다.
서버가 이미 아는 것을 화면에 그리지 않고 브라우저에서 고쳐 주는 구조를 두면,
스크립트가 한 번 멈출 때 그 모든 것이 함께 멈춘다.

실제로 그렇게 됐다. 인라인 `<script>` 한 블록에서 문자열 한 줄이 끊겨
(따옴표가 닫히기 전에 줄바꿈) 그 블록의 함수가 **전부** 정의되지 않았고,
증상은 "행정처분·수거검사 탭이 클릭되지 않는다" 하나로만 나타났다.
탭 단추는 멀쩡히 보이고 화면에 오류도 안 뜬다.

그래서 규칙을 둔다.

> **그 자리에 있는지 없는지, 어느 것이 켜져 있는지는 서버가 그린다.**
> 스크립트는 이미 그려진 것을 바꾸는 일만 한다.

| 무엇 | 서버가 그리는 것 | 스크립트가 하는 것 |
|---|---|---|
| 지금 탭 | `#regSidebar[data-view]`, `#regPage` 클래스, `.rs-vtab--active` | 없음(초기화는 확인만) |
| 모두 읽음 단추 | 지금 탭에 미확인이 있을 때만 그린다 | 눌러서 다 읽으면 거둔다 |
| 수거검사 진행/완료 필터 | `insp_status` 로 걸러 그린다 | 주소를 바꿔 다시 요청 |
| 상세 조건 잠금 | 수거검사 탭이면 `disabled` | 없음 |

`manage.py check` 의 **templates.E006** 이 끊긴 문자열을 배포 전에 잡는다.
정규식 리터럴과 백틱은 건너뛴다.

---

## 4-9. 아직 남은 것

> 해결됨(2026-09) — **웹 전용 사용자의 키워드 알림.** 매칭을 기기가 아니라
> 사용자에 붙는 표(`NewsKeywordMatch`)로 옮겼다. 2절 참고.
> 읽음을 웹에서 만질 수 있게 되어 **미확인 배지에도 함께 센다**(`selectors`).
> 조치(`RegulatoryMatchAction`)는 붙일 자리가 없으므로 '미조치' 모수에는
> 넣지 않는다 — 넣으면 영원히 미조치로 남는다.

- **정적 파일 캐시 무효화가 저장소 전반에 빠져 있다.**
  이 화면 것(`regulatory.css`, `regulatory_list.js`)은 `?v={{ STATIC_BUILD_DATE }}`
  를 붙였지만, 저장소에는 아직 CSS 16곳·JS 5곳이 버전 없이 걸려 있다.
  `check_static_cache_busting` 은 **고정값**(`?v=3`)만 잡고 **빠진 것**은 안 잡는다.
  검사를 넓히면 한 번에 21건이 뜨므로, 고칠 결심과 함께 넓히는 게 맞다.

- **목록 줄이 키보드로 안 열린다.** `<tr onclick=…>` 이라 Tab 으로 닿지 않고
  Enter 로도 안 열린다. 상세를 여는 유일한 길이라 키보드만 쓰는 사용자는
  이 화면을 못 쓴다. `<tr tabindex="0" role="button">` + keydown 으로 열거나,
  제품명 칸을 실제 `<a href="?id=…">` 로 두면 된다(주소가 이미 그 모양이다).

- **모바일 앱**에는 아직 '받지 않기' 화면이 없다. 웹에서 끈 것은 앱 푸시·앱
  알림 탭에 그대로 반영되지만, 앱에서 새로 끄려면 웹으로 와야 한다.
  서버 쪽은 `GET/POST /regulatory/api/alert-mutes/` 로 이미 열려 있으므로,
  앱에 화면만 붙이면 된다 (앱 API 는 `/mobile/` 아래에 같은 모양으로 추가).
- **마이그레이션 그래프가 갈라져 있다** — 앱 전체를 도는 `manage.py migrate` 는
  `InconsistentMigrationHistory` 로 실패한다. 테스트도 그래서 `settings_test` 로
  마이그레이션을 건너뛰고 돈다(`v1/config/settings_test.py` 참고).
  이번에 넣은 `regulatory/0005_alertmute` 는 앱을 지정하면 깨끗이 돈다 —
  본서버에서도 `manage.py migrate regulatory` 로 넣는다.
  (CREATE TABLE 한 줄뿐이라 기존 표는 건드리지 않는다)

---

## 4-10. 화면 정리 계획

기능은 다 있는데 어디를 눌러야 할지가 어렵다. 이 화면만 제 컴포넌트를 따로
들고 있어서, 다른 메뉴에서 익힌 조작이 여기서는 통하지 않는다.
새 기능을 더하는 계획이 아니라 **같은 일을 하는 자리를 하나로 줄이는** 계획이다.

### 4-10-1. 세어 본 값

| | 부적합·처분 | 제품 조회 |
|---|---|---|
| 한눈에 보이는 조작 | **20** (카드 5·탭 3·기간 4·나머지 8) | 8 |
| 한 화면 안의 페이지네이션 | **3벌** | 1벌 |
| 템플릿 줄 수 | **1,717** | 697 |

조작이 많은 것 자체가 문제는 아니다. 문제는 **같은 일을 하는 자리가 둘씩 있고,
성격이 다른 것이 한 줄에 섞여 있다는 것**이다.

### 4-10-2. 왜 이 화면만 다른가

`list_common.css` 머리말은 "제품 조회 · 식품첨가물 · 부적합·처분 알림이 같은
모양으로 보이도록" 이라고 적혀 있다. 실제로는 셋 중 하나만 다른 길로 갔다.

| 무엇 | 제품 조회 | 식품첨가물 | 원료 보관함 | 부적합·처분 |
|---|---|---|---|---|
| 탭 마크업 | `<a href>` | — | — | **`<button onclick>`** |
| 탭 클래스 | `pv-tab` | — | — | **`rs-vtab`** |
| 상세 조건 | `pv-cond-toggle` | `pv-cond-toggle` | — | **`reg-cond-toggle` + 별도 서랍** |
| 칩 | `pv-chip` | `pv-chip` | — | **`rs-day-btn`·`rs-risk-chip`·`rs-quick-chip`** |
| 상시 통계 카드 | 없음 | 없음 | 없음 | **5장** |
| 페이지네이션 | 1벌 | 1벌 | 1벌 | **3벌** |
| 좌 목록 / 우 상세 | — | — | 있음 | 있음 |

마지막 줄이 중요하다. 분할 화면은 **원료 보관함과 이미 같다** — 고칠 것이 아니라
지킬 것이다. 어긋난 것은 그 위에 얹힌 조작 층뿐이다.

### 4-10-3. 네 가지 원칙

1. **이 화면만의 컴포넌트를 새로 만들지 않는다.** 같은 뜻의 컨트롤은 `pv-*`
   공용 이름으로 옮긴다. 이름이 같으면 모양도 저절로 같아진다.
2. **상태는 서버가 그린다.** (8절과 같은 규칙)
3. **한 가지 일에는 한 자리만 둔다.** 같은 곳으로 가는 링크가 둘이면, 어느 쪽을
   눌러야 하는지를 사용자가 고민한다.
4. **자주 안 쓰는 것은 상세 조건으로 접는다.** 툴바에 상시로 남는 것은
   "거의 매번 쓰는 것" 만.

### 4-10-4. 무엇을 바꾸나

| | 무엇 | 성격 |
|---|---|---|
| A | 통계 카드 줄을 걷고, **목록 헤더 한 줄로 합친다** | 덜어냄 |
| B | 툴바에서 중복 지름길 제거 | 덜어냄 |
| C | 탭을 `<a href>` 링크로, 클래스를 `pv-tab` 으로 | 공용으로 |
| D | 페이지네이션을 활성 탭 한 벌만 그린다 | 덜어냄 |
| E | 상세 패널의 매칭 세 구역을 하나로 합친다 | 정리 |
| F | 목록 줄을 키보드로 열 수 있게 | 접근성 |

**A — 통계 카드 줄.** 다섯 장이 화면 맨 위 한 뼘(약 150px)을 차지하는데 하는 일은
목록을 거르는 것이다. 다른 어느 목록 화면에도 이런 줄이 없다.
목록 헤더는 이미 `전체 N건` 을 왼쪽에 들고 있다 — **거기에 합친다.**
그 한 줄이 지표이자 범위 스위치를 겸하므로, 줄도 늘지 않고 자리도 둘로 갈리지
않는다. 수거검사 미확인은 이미 그 탭의 배지와 빨간 점이 말한다.

```
지금   [카드 5장 · 세로 150px]
       [탭 · 검색 · 알림설정]
       [기간 · 모두읽음 · 상세조건]
       [전체 4,182건            정렬 페이지당]

바뀐 뒤 [탭 · 검색 · 상세조건 · 알림설정]
       [기간 · 모두읽음]
       [4,182건 · 내 알림 12 · 일반 4,170 · 미조치 5   정렬 페이지당]
```

**C — 탭을 링크로.** 가운데 클릭으로 새 탭에서 열리고, 주소를 복사할 수 있고,
스크립트가 죽어도 눌린다(8절 사고가 정확히 여기서 났다).

**E — 상세 패널.** 지금 여섯~여덟 덩어리로 쌓인다. 접혀 있는 두 개(원문·판정 기준)는
그대로 두되, 매칭 세 구역(제품·원료·키워드)을 하나로 합치고 그 안에서 종류를
배지로 가른다. 사용자가 묻는 것은 "왜 왔나 / 무엇을 하나" 두 가지뿐이다.

### 4-10-5. 순서와 진행

| 단계 | 무엇 | 항목 | 상태 |
|---|---|---|---|
| 1 | 이름부터 공용으로 | C | **완료** — pv-tab / pv-chip 을 list_common.css 로, 탭은 `<a href>` |
| 2 | 상단을 비운다 | A · B | **완료** — 카드 5장 → 목록 머리 한 줄 |
| 3 | 덩어리를 줄인다 | D · E | **완료** — 페이지네이션 한 벌 · 상세의 매칭 세 구역을 하나로 |
| 4 | 남은 빚 | F · 캐시 버전 | **완료** — 제품명이 진짜 링크 · 정적 파일 22곳에 ?v= |

**잰 값 (1~4단계 뒤)**

| 지표 | 시작 | 지금 | 목표 |
|---|---|---|---|
| 부적합 탭에서 보이는 조작 | 20 | **12** | 12 이하 ✔ |
| news_list.html 줄 수 | 1,717 | **1,061** | 1,200 ✔ |
| 페이지네이션 | 3벌 | **1벌** | 1벌 ✔ |
| 이 화면 전용 칩·탭 CSS | rs-* 4종 | **없음** (공용 pv-*) | 공용 ✔ |
| 스크립트 없이 탭 이동 | 안 됨 | **됨** | 됨 ✔ |

네 단계를 모두 마쳤고 지표도 모두 목표에 닿았다.

줄 수는 한 파일이 들고 있던 짐을 뜻이 있는 조각으로 나눠 줄였다 — 줄이
사라진 것이 아니다. 이 저장소가 이미 `_news_item.html` 처럼 쓰던 방식이다.

| 조각 | 무엇 |
|---|---|
| `_alert_settings_modal.html` | 키워드·수거검사·받지 않기 세 탭 |
| `_inspection_detail_panel.html` | 수거검사 상세 (내 매칭) |
| `_inspection_public_panel.html` | 수거검사 상세 (공개 목록) |
| `_empty_detail_panel.html` | 고르기 전 / 걸린 것 없을 때 |

**두 번 같은 사고를 냈다.** 정규식이나 범위로 CSS 규칙을 지웠다가, 그 사이에
있던 공용 규칙까지 함께 날렸다 — 한 번은 알약 칩이 전부 노래졌고, 한 번은
툴바 뼈대가 사라져 검색창이 아래로 밀렸다. 이제 시험이 지킨다:
템플릿이 쓰는 클래스 중 어디에도 규칙이 없는 것을 찾아 준다.

### 4-10-6. 안 하기로 한 것

- **좌 목록 / 우 상세 구조는 그대로.** 원료 보관함과 이미 같고, 알림은 훑다가
  그 자리에서 조치를 남기는 일이라 이 편이 맞다.
- **'알림 사유' 와 끄기 단추는 그대로.** 알림이 많다는 신고의 핵심을 푸는
  자리다. 오히려 세 탭 상세의 표준으로 굳힌다.
- **모델·API 변경 없음.** 전부 템플릿·CSS와 뷰의 컨텍스트 층이다.
- **수거검사 공개 목록은 유지.** 내 정보를 아직 안 넣은 사용자에게 이 화면이
  빈 껍데기로 보이지 않게 하는 장치다.

### 4-10-7. 됐는지 어떻게 아나

| 지표 | 지금 | 목표 | 어떻게 재나 |
|---|---|---|---|
| 한눈에 보이는 조작 | 20 | 12 이하 | 부적합 탭 기준 |
| news_list.html 줄 수 | 1,717 | 1,200 이하 | `wc -l` |
| 이 화면 전용 CSS 클래스 | `rs-*` 다수 | 공용 `pv-*` 로 대체 | regulatory.css 규칙 수 |
| 스크립트 없이 동작 | 탭·단추 일부 | 전부 | JS 끄고 탭 이동 |

회귀는 시험이 지킨다. 탭 상태를 서버가 그리는지, colspan 이 표 머리와 맞는지,
카드가 몇 장인지가 이미 시험에 있다 — 단계마다 그 조건을 새 모양으로 고쳐 쓴다.

---

# 5. 게스트와 튜토리얼

## 5-1. 게스트 로그인과 튜토리얼은 경쟁하지 않는다

"어느 게 나은가"를 물었는데, **둘은 다른 문제를 푼다.** 다만 게스트 쪽에 지금
그냥 두면 안 되는 것이 하나 있다.

### ⚠ 게스트는 공용 계정 하나다

`guest@labeasylabel.com` 을 **모든 방문자가 같이 쓴다**
(`common/management/commands/ensure_guest_user.py`).

- 게스트 A가 만든 제품이 게스트 B에게 보인다
- 서로 덮어쓴다
- 막혀 있는 건 **삭제뿐**이고 생성·수정은 열려 있다
- 2026-09-11 에 막은 소유권 구멍들도 게스트끼리는 **의미가 없다** — 같은 `user` 다

| | 게스트 로그인 | 튜토리얼 |
|---|---|---|
| 푸는 문제 | **가입 전** 둘러보기 | **내 데이터를 앞에 두고** 다음에 뭘 할지 |
| 대상 | 방문자 | 신규 + **기존** |
| 성격 | 영업·마케팅 도구 | 제품 기능 |

기존 사용자는 자기 데이터를 두고 로그아웃해서 게스트로 들어가지 않는다.
말한 문제 — *"기존 사용자도 활용법을 모른다"* — 는 **튜토리얼만** 풀 수 있다.

### 권하는 것

1. **게스트는 남기되 격리를 고친다.** 방문마다 임시 계정(`guest_<uuid>`, 24시간
   뒤 정리)을 내주거나, 최소한 게스트가 만든 것을 세션에 묶는다. 지금 상태로
   홍보하면 **남의 실습 데이터가 보이는 화면**을 보여 주게 된다.
2. **튜토리얼은 새로 만든다.** 게스트로 대체되지 않는다.

---

## 5-2. 튜토리얼은 이미 깔아 둔 것 위에 짓는다

### 기반이 두 개 이미 있다

**① 칸 옆의 `[?]`** — `templates/includes/_field_help.html`. 원료관리 6곳·
제품관리 6곳에 붙어 있고 마우스·키보드·손가락을 다 받는다.
**튜토리얼은 이 `[?]` 들을 순서대로 열어 주는 것**으로 만들 수 있다.

**② 4단계 흐름** — `products/views.py:_WORKFLOW_STEPS` (기본 정보 → BOM →
영양성분 → 검증)에 각 단계의 `hint` 와 `done` 판정이 이미 있다. **튜토리얼의
뼈대를 새로 정할 필요가 없다.**

### 왜 예전 튜토리얼이 버려졌을까

*(짐작이다)* **전체 투어**(모달을 연속으로 넘기는 방식)는 한 번 보고 끝이고
실제로 막힌 순간과 시점이 안 맞는다. 사람은 **지금 이 칸에서** 막히지, 가입
첫날 순서대로 막히지 않는다.

### 제안: 전체 투어 대신 화면별 코치마크

각 화면 오른쪽 위 `[튜토리얼]` 을 누르면 **그 화면만** 3~5걸음으로 짚어 준다.
언제든 다시 열 수 있고 끝까지 안 봐도 된다. 진행 상태는 계정에 남겨 **본 것은
다시 안 권한다.**

### 시나리오 — 운영에서 잰 막힘에 맞춘다

| 시나리오 | 화면 | 근거가 된 실측 | 핵심 한 줄 |
|---|---|---|---|
| 원료 하나 등록하기 | 원료 상세 | 품목보고번호 있는 원료 **3%** | 번호가 있으면 세 칸이 저절로 채워진다 |
| 영양성분 넣는 세 가지 길 | 원료 상세·영양성분 탭 | 영양성분 정해진 원료 **4%** (68중 3) | **안 넣어도 된다.** 성적서가 있으면 그 값을 쓴다 |
| 알레르기는 추천일 뿐 | 원료 상세·제품 기본정보 | 자동감지 일치율 **58%** (622개) | 향료·제제는 이름으로 알 수 없다 |
| BOM에서 원재료명 만들기 | BOM·기본정보 | 배합비 없는 줄 **11/107** | 배합비를 넣어야 순서대로 조립된다 |
| 검증 보고 고치기 | 검증 탭 | — | 지적을 누르면 그 칸으로 간다 *(3장 2번 뒤)* |

### 적용 대상과 띄우는 때

| 누구에게 | 언제 | 무엇을 |
|---|---|---|
| **신규** (첫 로그인) | 제품을 처음 열 때 한 번 | 4단계 흐름 → 이후 화면별로 |
| **기존** | 스스로 `[튜토리얼]` 을 누를 때 | 그 화면만 |
| **기존** | 새 기능이 붙은 화면에 처음 올 때 | 바뀐 것 한 걸음만 |
| **게스트** | 들어오자마자 | 4단계 흐름 *(격리를 고친 뒤)* |

> **붙였으면 효과를 재야 한다.** `UserActivityLog` 가 이미 있다. 튜토리얼을 본
> 계정과 안 본 계정의 *품목보고번호 입력률·영양성분 등록률·검증 통과율* 을
> 비교하면 이 일이 값을 했는지 수로 말할 수 있다. **안 재면 다음 튜토리얼도
> 짐작으로 만들게 된다.**

---

> **게스트 격리는 2026-09-11 에 끝냈다**(`v1/common/guest.py`). 방문마다
> `guest_<열두 글자>@labeasylabel.com` 을 새로 만들고, 지난 것은
> `manage.py purge_guests --apply` 로 치운다. 아래 5-1 의 "권하는 것" 중
> 1 번이 그것이다. 2 번(튜토리얼)이 남았다.


---

# 6. 무료 도구 + 광고 모델 접목

> 성적서를 올리면 영양정보 표를 만들어 주는 무료 사이트가 있고, 광고 시청으로
> 수익을 낸다. 우리에게 접목할 수 있는 것을 단기·장기로 나눠 본다.

### 6-1 저쪽과 우리는 파는 것이 다르다

| | 그 사이트 | 우리 |
|---|---|---|
| 범위 | **한 작업** (성적서 → 표) | 라벨 전체 워크플로 |
| 사용자 | 일회성 방문자 | 식품 제조사 (반복) |
| 진입 | 로그인 없이 바로 | 가입 → 제품 → 4단계 |
| 수익 | 광고 노출 | 구독 |

**저쪽의 강점은 기능이 아니라 마찰이 없다는 것이다.** 성적서 한 장을 표로
바꾸고 싶은 사람에게 우리 제품은 *제품을 만들고 → 탭을 찾고 → 값을 넣는* 길이다.
그 사람은 우리 고객이 될 수도 있는데 첫 화면에서 돌아선다.

### 6-2 결론부터 — 광고는 접목하지 않는다

**본 제품에 광고를 붙이면 안 된다.** 우리 사용자는 규제 문서를 만드는 사람이고,
그 화면에 광고가 뜨면 **도구의 신뢰가 깎인다.** 저쪽은 일회성 이용자를 상대하니
잃을 신뢰가 없어서 가능한 모델이다.

**접목할 것은 수익 모델이 아니라 "마찰 없는 입구"다.**

### 6-3 단기 — 로그인 없는 한 화면짜리 도구

목표: **가입 전 사람이 3분 안에 쓸 것을 얻고, 그 결과를 저장하려 할 때 가입한다.**

우리는 재료가 이미 다 있다. 없는 건 **입구 하나**다.

| 조각 | 우리에게 있나 |
|---|---|
| 영양성분 계산·표시기준 반올림 | ✅ `services/nutrition_calc.py` |
| 영양정보 표 그리기 | ✅ `nutrition_editor.html` 미리보기 |
| PDF 저장 | ✅ 표시사항 미리보기 |
| 이미지 OCR (영양정보표 포함) | ✅ `services/ocr_*.py` 14개 모듈 |
| **로그인 없이 들어오는 입구** | ❌ **이것만 없다** |

OCR 은 이미 `nutrition_basis` 까지 읽고 있다 — 다만 지금 읽는 대상이 **완제품
라벨 사진**이다. **성적서는 문서 종류만 다르고 기계는 같다.** 프롬프트 한 벌을
더 쓰면 된다.

**할 일 (단기)**

1. `/tools/nutrition-table/` — 로그인 없이 여는 한 화면.
   성적서 이미지/PDF 올리기 → OCR → 표 미리보기 → PNG·PDF 내려받기.
2. **아무것도 저장하지 않는다.** 세션에서만 산다.
   이것이 게스트 공용 계정 문제의 **더 나은 답**이다 — 계정이 없으니 섞일
   것도 없다.
3. 결과 아래 한 줄: *"이 표를 제품에 붙이고 배합·알레르기·검증까지 하려면
   가입하세요."* 광고 대신 **우리 제품이 광고 자리에 선다.**
4. 유입을 잰다. `UserActivityLog` 에 `tool_nutrition` 을 남기고, 그중 몇 명이
   가입하는지 본다.

**주의 — 공짜 OCR 은 공짜가 아니다.** OpenAI 호출 비용이 방문자 수만큼 나간다.
`services/ai_rate_limit.py` 가 이미 있으니 **IP 단위 한도**를 반드시 걸고
시작한다. 한도를 안 걸면 하루 만에 비용이 터진다.

### 6-4 장기 — 무료 도구를 깔때기로

| 단계 | 하는 일 | 전환 지점 |
|---|---|---|
| 1 | 성적서 → 표 (무료, 저장 없음) | — |
| 2 | 표를 **저장**하려 함 | **가입** |
| 3 | 성적서를 **보관**하려 함 | 문서함 (이미 있다) |
| 4 | 원료에 그 값을 **붙이려** 함 | 원료관리 (이미 있다) |
| 5 | 제품 라벨 전체 | 구독 |

무료 도구가 2~5를 **이미 만들어 둔 기능으로** 이어 준다. 새로 지을 것이 거의
없다 — 길만 놓으면 된다.

**장기에 볼 것**

1. **무료 한도 + 유료 전환.** 광고보다 우리 사업에 맞는 모델이다.
   월 N장까지 무료, 넘으면 가입. 광고는 신뢰를 팔지만 한도는 안 판다.
2. **성적서 보관함.** 문서함(`ProductDocument`)에 *시험성적서* 종류를 두고,
   원료의 영양성분 출처(`MyIngredientNutrition.source_kind = spec_ocr`)와
   잇는다. **이건 이미 설계에 있다** — `SOURCE_SPEC_OCR` 이 모델에 정의만 되고
   화면이 없다.
3. **이게 4장의 숙제를 같이 푼다.** 성적서에서 값을 읽어 원료에 붙일 수 있으면,
   영양성분 등록률 4% 가 오를 여지가 생긴다. 지금은 값을 넣는 길이
   *품목보고번호(3%)* 와 *손으로 치기* 뿐이다.

### 6-5 안 할 것

- **본 제품 광고** — 위에 적은 이유로.
- **무료 도구에 로그인 붙이기** — 그러면 저쪽과 같은 것을 만들면서 마찰만 더한다.
- **OCR 정확도 경쟁** — 성적서는 서식이 제각각이라 끝이 없다. 읽은 값을
  **사람이 고칠 수 있게** 보여 주는 것이 정확도보다 값이 크다. (알레르기
  자동감지에서 배운 것과 같다 — 기계가 못 찾은 것이 사람이 찾은 것을 이길 수 없다.)

### 6-6 단기·장기 요약

| | 단기 (1~2주) | 장기 (분기) |
|---|---|---|
| **입구** | 로그인 없는 `/tools/nutrition-table/` | 무료 한도 → 가입 → 구독 깔때기 |
| **OCR** | 성적서 프롬프트 한 벌 추가 | 읽은 값 → 원료에 붙이기 (`SOURCE_SPEC_OCR`) |
| **저장** | 안 한다 (세션만) | 성적서 보관함 ↔ 문서함 |
| **수익** | 없음 (유입만 잰다) | 무료 한도 + 유료 전환 |
| **광고** | **안 붙인다** | **안 붙인다** |
| **위험** | OCR 비용 — IP 한도 필수 | 무료 도구가 유료 기능을 잠식하지 않게 선 긋기 |

---

---

# 7. 데이터가 모이게 만든다 — 영양성분 · 시안 대조 · 원료 공용

> 2026-09-11 검토. 세 가지를 따로 물었지만 **같은 문제의 세 얼굴**이다.
> 우리 서비스는 사용자가 값을 넣어야 일하는 물건인데, 값을 넣는 일이 곧
> 사용자가 가장 하기 싫어하는 일이다. 그래서 **값을 대신 채워 주는 경로를
> 몇 개 갖고 있느냐**가 활성화를 정한다.

## 7-0. 먼저, 실측으로 본 지금

`link_ingredient_nutrition` 이 실서버에서 잰 숫자다. 추정이 아니다.

| | 값 |
|---|---|
| 적재한 식약처 영양성분 | **319,060 건** |
| 사용자 원료 | **884 개** |
| 그중 품목보고번호로 찾아지는 것 | **65 개 (7.4%)** |
| 실제로 영양성분이 정해진 원료 | **0 개** |
| 배합이 있는 제품 | 20 개 |
| 그중 지금 계산되는 제품 | **0 개** |

**읽는 법이 중요하다.** DB 가 모자라서 4% 인 것이 아니다. 319,060 건을 이미
갖고 있는데도 0 개다. 병목은 **확보**가 아니라 **연결**이고, 연결이 막힌
까닭은 원료의 92.6% 가 품목보고번호조차 없기 때문이다. 번호가 없는 원료에
DB 를 더 부어도 0 은 0 이다.

그래서 7-1 의 순서는 "더 받아 온다" 가 아니라 **"가진 것을 연결한다 → 번호
없는 원료에 다른 다리를 놓는다 → 그래도 없는 칸만 새로 받아 온다"** 다.

---

## 7-1. 영양성분 DB 확보 — 다섯 갈래, 싼 것부터

### ① 이미 붙일 수 있는 65 개를 지금 붙인다 (비용 0)

`link_ingredient_nutrition` 이 이미 있다. **한 번도 안 돌렸다.** 고를 것이
없는 건(같은 번호 = 같은 품목)인데 사람 손을 기다리는 구조라 잠들어 있었다.

- 배포 때 `--dry-run` 으로 세고, 이상 없으면 실행
- 그다음 **주 1 회 크론**으로 건다. 원료는 계속 늘어난다
- 새 원료를 저장하는 순간에도 한 번 시도한다 — 배치를 기다릴 이유가 없다

기대: 0 개 → 65 개. 작아 보이지만 **계산되는 제품이 0 에서 벗어나는** 첫 걸음이다.

### ② 성적서에서 읽는다 — 등급 A 를 만드는 유일한 길 (개발 小)

`MyIngredientNutrition.SOURCE_SPEC_OCR` 은 이미 정의돼 있고 등급이 **A** 다.
그런데 그 값을 넣는 화면이 없다. 한편 문서함에는 시험성적서가 이미 쌓인다.

- 문서 종류가 시험성적서·규격서인 파일에 **[영양성분 읽기]** 를 붙인다
- 판독 파이프라인(`ocr_service` → `ocr_ground` → 확인 창)을 그대로 탄다.
  새로 만들 것은 성적서용 프롬프트 한 벌과 확인 화면뿐이다
- 6-6 장기 계획에 이미 같은 말이 적혀 있다. **그 항목을 앞당기자는 것**이다

이것이 중요한 까닭: 공공 DB 로 채운 값은 영원히 C 등급이다("일반적인 밀가루"
이지 "우리가 쓰는 그 밀가루"가 아니다). **A 등급을 만들 수 있는 경로는
성적서뿐**이고, 그 종이는 이미 우리 문서함 안에 있다.

### ③ 번호가 없는 원료에 다리를 놓는다 (개발 中)

92.6% 가 여기 해당한다. 세 다리를 순서대로 시도한다.

| 다리 | 대상 | 출처등급 |
|---|---|---|
| 제조사명 + 원료명 2 단 매칭 | 국내 가공 원료 | C |
| 하위 원료(`rawmtrl_nm`)로 역산 | 복합원료 | C |
| 이름 후보 고르기 (`nutrition_candidates`) | 나머지 | C |

셋째는 이미 구현돼 있다. 문제는 **묻는 자리가 원료 상세 안쪽**이라 아무도
거기까지 안 간다는 것이다. 물어야 할 자리는 **영양성분 탭의 [계산하기] 를
누른 직후**다 — 그때가 사용자가 값을 원하는 유일한 순간이다.

`nutrition_contrib` 이 이미 "흔들 수 있는 원료만" 을 가려낸다. 원료 20 개
중 실제로 물을 것은 대여섯이다. 그 대여섯을 한 화면에서 연달아 묻는다.

### ④ 공공 DB 를 더 받아 온다 (비용 小, 효과는 특정 구간에만)

적재본의 출처 분포가 이미 말해 준다.

    수집 310,488 · 분석 5,355 · 산출 3,153
    식약처 314,852 · 수과원 1,094 · 농진청 약 1,400 · USDA/JAPAN 약 400

**'수집' 이 97%** 다. 수집은 업체 신고값을 모은 것이라 동명이 갈리고
(버터 12 건 · 164~761 kcal) 신뢰가 낮다. 보강할 곳은 **분석값**이다.

| 후보 | 채우는 구간 | 우선 |
|---|---|---|
| 농진청 **국가표준식품성분표** | 농·축·수산 원재료 — 지금 가장 비어 있다 | **높음** |
| 식약처 수입식품 품목 | 수입 원료 | 중 |
| USDA FoodData Central | 향료·유지·수입 base | 낮음 (단위·성분계 다름) |

단, ④ 는 ①②③ 뒤다. 연결이 안 되는 상태에서 DB 만 늘리면 **0 이 0 인 채로
표만 커진다.** 실제로 원문 JSON 이 표의 66%(1,106 MB)를 먹은 전례가 있다.

### ⑤ 사용자가 만든 값을 되돌린다 — 7-3 과 이어진다

같은 품목보고번호를 여러 회사가 등록한다. A 사가 성적서로 A 등급 값을 넣으면
같은 번호를 쓰는 B 사에게 제안할 수 있다. **가장 큰 잠재력이자 가장 조심할
것**이라 7-3 에서 따로 쓴다.

---

## 7-2. 디자인 시안 대조 정확도 — 오탐부터 줄인다

### 먼저 정하고 갈 것: 무엇이 "정확도" 인가

두 가지 실패가 있고 체감이 전혀 다르다.

    놓침(miss)   다른데 같다고 했다      → 인쇄 사고. 치명적이지만 안 보인다
    오탐(false)  같은데 다르다고 했다    → 매번 보인다. 기능을 안 쓰게 만든다

**오탐이 먼저다.** 놓침은 한 번 겪으면 기능을 더 믿게 되지만, 오탐은 세 번
겪으면 기능을 닫는다. 지금 "제대로 읽지 못함" 이라는 말이 반복해서 나온 것도
대부분 오탐 쪽이다.

### ① 견주는 규칙을 항목마다 다르게 (개발 小, 효과 大)

`value_match` 가 이미 `exact / prefix / contains / company` 를 갖고 있는데,
시안 대조는 이걸 안 쓰고 있다. 항목별로 배정한다.

| 항목 | 규칙 | 지금 나는 오탐 |
|---|---|---|
| 식품유형 | `prefix` | `빵류` ↔ `빵류 [가열하여 섭취하는 냉동식품]` |
| 제조원 | `company` | `(주)○○` ↔ `주식회사 ○○` |
| 내용량·영양성분 | **수치 비교** (단위 정규화 후) | `100g` ↔ `100 g` ↔ `100그램` |
| 주의·알레르기 문구 | **문장 단위 포함** | 줄바꿈 자리가 달라 통째로 다르다고 함 |
| 원재료명 | 토큰 집합 + 순서 | 괄호·중점 표기 차이 |

수치는 특히 명시해야 한다. 지금 문자열로 견주면 `12.5g` 과 `12.50g` 이
다르다고 나온다 — 이건 순수한 오탐이고, 사용자가 가장 자주 만난다.

### ② 좌표를 쓴다 (개발 中)

`ocr_boxes` 가 이미 있다. 표시사항 박스는 `항목명 : 값` 이 세로로 쌓인
정형이라, 텍스트 유사도보다 **위치로 끊는 편이 훨씬 정확**하다. 값이 다음
줄로 넘어간 경우(내용량이 두 줄)도 좌표로는 한 덩어리로 잡힌다.

### ③ 지어낸 값을 차단한다 — 이미 있는 것을 연결만 (개발 小)

`ocr_ground` 는 "모델이 낸 값이 OCR 원문에 실제로 있는 글자인가" 를 본다.
지금은 판독 경로에만 걸려 있고 **시안 대조에는 안 걸려 있다.** 같은 검사를
시안 쪽에도 건다. 시안에 없는 글자를 "시안에 이렇게 적혀 있다" 고 말하는
것이 가장 나쁜 오탐이다.

### ④ 숫자로 잰다 — "좋아졌다" 를 주장하지 않는다 (개발 小)

`ocr_benchmark` 가 이미 정답셋 채점을 한다. **시안 대조 전용 정답셋**을
20~30 건 만든다(내부 라벨 + 일부러 틀린 시안을 섞는다).

- 프롬프트·규칙을 고칠 때마다 회귀로 돌린다
- **놓침률과 오탐률을 따로** 낸다. 하나로 합치면 개선이 안 보인다
- 같은 사진도 매번 다르게 읽히므로 3 회 평균과 편차를 함께 본다

지금까지 "개선했다" 를 눈으로만 확인했고, 그래서 고칠 때마다 다른 곳이
나빠져도 몰랐다.

### ⑤ 무시한 지적을 배운다 (개발 中)

`ocr_learning` 이 교정 이력을 이미 쌓는다. 시안 대조에도 붙인다 — 사용자가
"이건 문제 아님" 으로 넘긴 지적을 기록하면, **오탐 패턴이 데이터로 드러난다.**
①의 규칙 표를 추측이 아니라 실측으로 채울 수 있게 된다.

### ⑥ "추가 표시사항" 을 한 겹 더 가른다 (개발 小)

지금은 라벨 데이터에 없는 글자를 모두 "근거 확인 필요" 로 모은다. 그중
상당수는 법정 문구(`label_phrases` 에 이미 있다)나 디자인 카피다. 셋으로 가른다.

    법정 문구인데 우리 데이터에 없음   → **이것이 진짜 지적**이다
    디자인 카피 (제품 설명·브랜드)      → 대조 대상 아님
    판독 잡음                           → 버린다

### 순서

    ① 항목별 비교 규칙   가장 싸고 오탐을 가장 많이 줄인다
    ③ 지어냄 차단        붙이기만 하면 된다
    ④ 정답셋 + 회귀      ①③ 이 실제로 나아졌는지 재기 위해 여기서 필요
    ⑥ 추가 표시사항 분류
    ⑤ 무시 이력
    ② 좌표               가장 크고, ①이 남긴 것만 남았을 때 의미가 있다

---

## 7-3. 원료 DB 공용 활용 — 가장 큰 기회이자 가장 큰 위험

### 전제: 같은 번호는 같은 품목이다

품목제조보고번호는 법적으로 하나의 품목을 가리킨다. 그러니 **그 원료의
표시명·식품유형·알레르기·영양성분은 어느 회사가 적어도 같아야 한다.**
지금은 회사 A 와 회사 B 가 같은 원료를 따로 조사해 따로 적고 있다. 같은
일을 인원수만큼 반복한다.

### 나눌 수 있는 것과 절대 안 되는 것

| 나눈다 | 나누지 않는다 |
|---|---|
| 품목보고번호 · 원료명 · 제조사 | **배합비** |
| 식품유형 · 표시명 후보 | **거래처 · 단가** |
| 알레르기 · GMO | **누가 등록했는지** |
| 영양성분 + 출처등급 | **어느 제품에 쓰는지** |

왼쪽은 전부 **품목보고 공시에 이미 공개된 정보**이거나 그것에서 파생된
것이다. 오른쪽은 영업비밀이고, 하나라도 새면 서비스가 끝난다. 이 선은
협상 대상이 아니다.

### 어떻게 보여 주나 — 덮어쓰지 않고 제안만

원료 상세에서, 같은 번호로 등록된 값이 있으면 이렇게만 말한다.

    이 번호로 등록된 값이 있습니다
      알레르기  우유, 대두        (7 곳이 같게 적음)
      영양성분  열량 380 kcal …   (출처: 업체 시험성적서 · 등급 A)
      [ 내 원료에 적용 ]  [ 무시 ]

- **자동으로 넣지 않는다.** 틀린 값이 퍼지는 것이 이 구조의 유일한 실패
  방식이고, 그 순간 우리 책임이 된다
- **몇 곳이 같게 적었는지**를 함께 보인다. 숫자가 근거를 대신한다
- **출처등급을 함께 보인다.** A(성적서) 하나가 C(이름으로 고름) 일곱보다 낫다
- 값이 갈리면 **갈렸다고 말한다.** 다수결로 하나를 고르지 않는다

### 닭과 달걀, 그리고 무임승차

기여해야 열람하게 하면 초기에 아무것도 안 보인다. 그래서:

1. **초기에는 기여 없이도 열람한다.** 풀이 채워지는 것이 먼저다
2. 채워지면 "내 원료 중 n 개가 다른 곳에 도움이 됐습니다" 를 보여 준다 —
   차단이 아니라 **기여했다는 사실을 보이는 것**이 동기가 된다
3. 그래도 필요해지면 그때 한도를 건다. 지금 걸 이유가 없다

### 만드는 법 — 새 표를 만들기 전에

가장 싼 판은 **새 모델 없이** `MyIngredient` 를 `prdlst_report_no` 로 묶어
집계하는 읽기 전용 질의다. 값이 실제로 모이는지, 얼마나 갈리는지를 먼저
본다. 그 뒤에 필요하면 집계 결과를 담을 표를 만든다.

순서를 뒤집으면 — 표부터 만들면 — **아무도 안 쓰는 표를 운영하게 된다.**
3 장(식품구분 4분류)에서 이미 한 번 배운 것이다.

### 법·개인정보에서 짚을 것

- 제조사명은 공시 정보라 노출해도 된다. **사용자의 회사명은 절대 아니다**
- 원료명·표시명은 사실 기재라 저작물성이 없다
- 이용약관에 "내 원료의 공개 가능 항목이 익명으로 집계될 수 있다" 를
  넣고, **끄는 스위치를 준다.** 기본값은 켬으로 하되 첫 진입에서 알린다

---

## 7-4. 최초 진입부터 활성화까지 — 어디서 새는가

### 깔때기와, 지금 아는 구멍

    방문 → 게스트/가입 → 첫 제품 → 첫 BOM → 첫 검증 → 재방문 → 유료
             ↑ ①            ↑ ②      ↑ ③              ↑ ④

**① 게스트가 만든 것이 버려진다 — 가장 큰 누수.**
게스트 계정 격리는 끝났지만 **승격 경로가 없다.** 게스트가 제품을 만들고
BOM 을 넣고 검증까지 돌린 뒤 가입하면, 그 전부가 남의 계정에 남는다.
`stale_guests` 가 지운다. 가장 열심히 써 본 사람의 결과물을 정확히 그
시점에 버리고 있다.

> 할 일: 게스트 → 회원 승격에서 `label.user` · `MyIngredient.user_id` 를
> 새 계정으로 옮긴다. 데이터 모델은 이미 FK 하나라 어렵지 않다.
> **7-4 에서 가장 먼저 할 것이다.**

**② 첫 제품이 빈 화면으로 시작한다.**
새 제품은 칸이 서른 개쯤 비어 있다. 사람은 빈 양식을 받으면 닫는다.
시작 방법을 세 가지로 제시한다 — 품목보고번호 / 라벨 사진 / 빈 양식.
앞의 둘은 이미 다 구현돼 있는데 **첫 화면에 안 보인다.**

**③ BOM 배합비가 없으면 그 뒤가 전부 안 된다.**
원재료명 조립·알레르기·영양성분이 모두 배합비에서 나온다. 여기서 멈추면
사용자가 보는 것은 "아무것도 안 되는 서비스" 다. 탭 번호(진행 표시)는
만들어 두었으니, **다음에 할 한 가지**를 화면이 콕 집어 말해야 한다.

**④ 돌아올 이유가 둘뿐이다.**
부적합 알림과 문서 유효기간 만료. 둘 다 이미 있고 둘 다 **서비스 안에서만**
뜬다. 안 들어오면 못 본다. 메일·푸시로 내보내야 재방문이 된다.

### 최초 진입 — 가입 전에 값을 보여 준다

6 장에 이미 적힌 로그인 없는 한 화면짜리 도구가 이 자리에 맞는다. 덧붙일 것
하나: **결과 화면의 끝에서 잇는다.**

    표가 나왔다 → "이 결과를 저장하고 검증까지 하려면"
                → 게스트로 계속 (가입 없음, 값이 그대로 넘어간다)
                → 가입 (①의 승격 경로를 탄다)

게스트로 이어 가면 ① 의 승격이 있어야 말이 된다. **①이 없으면 ②도 새는
깔때기일 뿐이다.** 그래서 ① 이 먼저다.

### 무엇을 재나 — 지금은 아무것도 안 재고 있다

`UserActivityLog` 가 이미 있다. 지표 넷만 정한다.

| 지표 | 왜 |
|---|---|
| 가입 후 **첫 검증 완료까지 걸린 시간** | 이 값이 곧 "쓸모를 느낀 시점" |
| 단계별 이탈률 (제품 → BOM → 검증) | 구멍이 ②인지 ③인지 가른다 |
| 7 일 / 28 일 재방문 | ④ 가 듣는지 |
| 원료당 영양성분 보유율 | 7-1 이 듣는지. 지금 **0%** |

넷 다 지금 데이터로 낼 수 있다. 새로 심을 것은 없다.

---

## 7-5. 순서 — 무엇부터

| 순 | 할 일 | 크기 | 왜 여기인가 |
|---|---|---|---|
| 1 | **게스트 → 회원 승격 시 데이터 이관** | 小 | 가장 열심히 쓴 사람을 버리고 있다 |
| 2 | `link_ingredient_nutrition` 실행 + 크론 | 0 | 만들어 두고 안 돌렸다. 0 → 65 |
| 3 | 시안 대조 **항목별 비교 규칙** | 小 | 오탐이 기능을 닫게 만든다 |
| 4 | 시안 대조에 **지어냄 차단** 연결 | 小 | `ocr_ground` 를 붙이기만 |
| 5 | **정답셋 + 회귀 측정** | 小 | 3·4 가 나아졌는지 재려면 필요 |
| 6 | [계산하기] 자리에서 **후보 묻기** | 中 | 물을 자리가 틀려서 기능이 잠들어 있다 |
| 7 | **성적서 → 영양성분** (등급 A) | 中 | A 등급을 만드는 유일한 경로 |
| 8 | **공용 원료 풀** (읽기 전용 집계부터) | 中 | 가장 큰 기회. 표부터 만들지 않는다 |
| 9 | 지표 넷 대시보드 | 小 | 8 까지의 효과를 재는 자리 |
| 10 | 농진청 표준성분표 적재 | 小 | 연결이 된 뒤라야 의미가 있다 |
| 11 | 좌표 기반 시안 대조 | 大 | 3 이 남긴 것만 남았을 때 |

### 안 할 것

- **영양성분 자동 확정** — '버터' 동명 12 건에 열량이 4.6 배 갈린다. 어떤
  유사도로도 못 가른다. 사람이 한 번 고르게 하는 지금 판단을 유지한다
- **공용 풀 다수결** — 일곱이 틀리고 하나가 맞는 경우가 있다. 갈리면 갈렸다고
  말하고 고르지 않는다
- **시안 대조 완전자동 판정** — 법적 표시물이다. 조용히 고치지 않고 짚는다
- **DB 부터 늘리기** — 연결이 0 인 상태에서 표만 커진 전례가 있다

---

# 8. 사용자를 늘리는 길과, 그 전에 무너질 자리

> 2026-09-11 검토. 7 장이 "들어온 사람을 붙잡는 법" 이라면 이 장은 **들어오게
> 하는 법**(8-1 ~ 8-3)과 **그 전에 무너지면 아무 소용이 없는 것**(8-4)이다.
>
> 순서를 이렇게 둔 까닭이 있다. 홍보가 먹히면 트래픽이 온다. 지금 구조로는
> 트래픽이 오는 것이 **위험**이다. 8-4 를 먼저 읽어도 좋다.

## 8-1. 우리가 파는 것이 무엇인지 먼저 정한다

홍보 문구를 정하기 전에 이것부터다. 6 장에서 이미 한 번 갈랐다 — 저쪽(무료
영양성분표 사이트)은 **표 한 장**을 팔고 우리는 **표시사항 한 벌과 그 근거**를
판다. 그러면 우리를 찾아야 하는 사람은 이렇게 좁혀진다.

| 우리 고객이 아닌 사람 | 우리 고객인 사람 |
|---|---|
| 표 한 장이 급한 사람 | 라벨을 **반복해서** 만드는 사람 |
| 제품이 하나인 사람 | 제품이 열 개 넘고 원료가 겹치는 사람 |
| 디자이너 | 품질관리·인허가 담당자 |
| 한 번 쓰고 떠날 사람 | 거래처에서 **서류를 받아야** 하는 사람 |

오른쪽 열의 사람은 검색으로 우리를 찾지 않는다. **이미 어딘가에 모여 있다.**
그래서 아래 순서는 검색광고가 아니라 그 모임에서 시작한다.

## 8-2. 유입 — 값이 싼 것부터

### ① 한 번 만든 라벨이 스스로 데려온다 (개발 小, 비용 0)

이미 있는 것 둘을 잇는 일이다.

- **공유 링크 (`public_share_view`)** — 거래처가 회원이 아니어도 열람한다.
  그 화면 아래에 "이 표시사항은 ○○에서 만들었습니다 · 직접 만들어 보기"
  한 줄. 광고가 아니라 **출처 표기**다
- **PDF·디자인 의뢰서** — 인쇄소와 디자이너에게 그대로 건네지는 종이다.
  발신 도구 이름을 작게 남긴다
- **자료 요청 메일** — 거래처가 규격서를 올리러 들어온다. **이미 우리
  화면을 쓰고 있는 비회원**이다. 그 흐름 끝에서 가입을 권한다

셋 다 **"우리 사용자의 거래처" 라는 정확한 표적**에 닿는다. 돈이 들지 않고,
사용자가 늘수록 저절로 커진다. 유일한 유입 경로 중 복리로 붙는 것이 이것뿐이다.

### ② 공개 도구로 검색에 걸린다 (6 장의 그것, 목적이 하나 더 있다)

6 장은 무료 도구를 **유입 깔때기**로 봤다. 여기에 하나를 덧댄다 —
**검색에 걸릴 페이지가 지금 하나도 없다.** 로그인 뒤 화면은 색인되지 않는다.

색인될 수 있고 우리가 이미 데이터를 가진 것:

| 페이지 | 데이터 | 검색어 |
|---|---|---|
| 식품첨가물 표시명 사전 | `FoodAdditive` (이미 있다) | "이산화규소 표시명", "L-글루탐산나트륨 간략명" |
| 식품유형별 의무 표시사항 | `food_type_settings` (이미 있다) | "과자류 표시사항", "빵류 의무표시" |
| 알레르기 19 품목 안내 | `allergen_names` (이미 있다) | "알레르기 표시 대상" |

**새로 쓸 글이 아니라 이미 가진 표를 공개하는 일**이다. 담당자가 실무 중에
가장 자주 검색하는 것이 정확히 이것들이고, 그 사람이 곧 우리 고객이다.

> 주의: 이것은 6-5 의 "무료 도구에 로그인 붙이기" 금지와 어긋나지 않는다.
> 여기서 여는 것은 **참조 데이터**이지 도구가 아니다.

### ③ 사람이 모여 있는 곳 (비용 0, 시간 中)

- **식품 인허가·품질관리 실무 커뮤니티** — 네이버 카페·오픈채팅에 담당자가
  모여 있다. 홍보 글이 아니라 **답변**으로 들어간다. 우리는 실제로 답을
  갖고 있다(원산지 3 순위 규칙, 표4·5·6 표시명, 열량 계수)
- **식품 관련 학과·교육기관** — 실습 계정. 학생이 곧 3 년 뒤 담당자다
- **컨설팅·인허가 대행사** — 한 곳이 수십 개 업체의 라벨을 만든다.
  **한 계정이 아니라 한 업종을 데려온다.** 8-3 으로 이어진다

### ④ 하지 말 것

- **검색광고 (CPC)** — 시장이 좁아 단가가 높고, 검색으로 오는 사람은
  대개 "표 한 장" 쪽이다. ②가 유기적으로 걸리는지 먼저 본다
- **일반 SNS 광고** — 표적이 전혀 안 맞는다
- **본 제품 광고 게재** — 6-5 에서 이미 결론 냈다

## 8-3. 협업 — 한 계정이 아니라 한 무리를 데려오는 길

### ① 인허가 대행사·컨설팅 (가장 값이 크다)

한 곳이 수십 업체의 라벨을 다룬다. 그 사람에게 필요한 것은 **여러 회사의
제품을 한 화면에서 관리하는 것**이고, 지금 그 기능이 없다. 있는 것은 제품별
권한 공유(`tab-share`)뿐이라 업체가 늘면 못 쓴다.

> 할 일: 계정 위에 **조직(거래처군)** 개념. 다만 8-4 를 먼저 읽고, 이 일에
> 손대기 전에 인프라를 옮길지 정해야 한다 — 사용자가 갑자기 배로 는다.

### ② 원료 공급사 (7-3 과 맞물린다)

원료를 파는 회사는 **같은 규격서를 고객사마다 반복해서 보낸다.** 그 회사가
자기 원료를 우리에 한 번 등록하면 고객사가 번호로 가져간다. 공급사는 일이
줄고, 우리는 **등급 A 데이터가 공급원에서 직접** 들어온다.

7-3 의 공용 풀이 사용자끼리 나누는 것이라면, 이것은 **원천에서 채우는 것**이다.
공용 풀보다 이쪽이 데이터 품질이 높다.

### ③ 인쇄소·패키지 디자인 회사

디자인 시안 대조는 **인쇄소가 가장 아쉬운 기능**이다. 잘못 인쇄하면 그 손해를
인쇄소가 진다. 8-2 ①의 PDF·의뢰서가 이미 그들 손에 들어가고 있다.

### ④ 협회·단체

식품산업협회, 지역 식품 관련 조합. 회원사 대상 교육에 실습 도구로 넣는 것이
가장 현실적인 형태다. 시간이 오래 걸리므로 ①②와 병행한다.

### 협업의 순서

    ② 원료 공급사   데이터가 들어온다 (7-1·7-3 과 같은 방향)
    ③ 인쇄소        이미 우리 산출물을 손에 쥐고 있다
    ① 대행사        값이 가장 크지만 8-4 를 정리한 뒤
    ④ 협회          병행, 오래 걸린다

---

## 8-4. 시스템 최약점 진단

> 코드는 잘 지켜져 있다 — 자체 검사(`checks.py`), 소유권 404, 게스트 격리,
> 한도(`quota.py`), 덤프 실패 감지까지 다른 곳에서 잘 안 하는 것들이 있다.
> **약한 곳은 코드가 아니라 그 바깥이다.**

### 순위 — 무너지면 얼마나 아픈가 × 얼마나 그럴듯한가

| 순 | 약점 | 터지면 | 지금 막는 것 |
|---|---|---|---|
| **1** | 백업이 사람 손에 달려 있다 | **서비스 종료** | 없음 |
| **2** | 업로드 파일은 백업 대상이 아니다 | 근거 서류 전부 소실 | 없음 |
| **3** | 단일 인스턴스·단일 DB, 대기 없음 | 전면 중단 | 없음 |
| **4** | DB 기본 비밀번호가 코드에 있다 | 유출 시 전량 | `.env` 가 있으면 안 쓰임 |
| **5** | 파일 기반 캐시 = 한도가 새는 구조 | AI 요금 폭증 | 일일 한도는 DB |
| **6** | OpenAI 계정에 지출 상한이 없다(추정) | 청구서 | 앱 한도만 |
| **7** | 취약 의존성 8 건 (sqlparse 高 3) | DoS | 없음 |
| **8** | 스테이징이 없다 — 배포가 곧 운영 | 전면 장애 | 테스트 2,026 건 |
| **9** | 표가 크다 · PA 용량은 벽이다 | 쓰기 실패 | 원문 JSON 을 걷은 적 있음 |
| **10** | 루트 `package.json` 이 남의 것 | 혼란·경보 소음 | 없음 |

### 1. 백업 — 가장 아픈 자리

`dump_db` 는 잘 만들어져 있다. mysqldump 가 죽어도 `gzip` 때문에 종료 코드가
0 이 되는 함정까지 막아 두었다. **그런데 자동으로 돌지 않는다.**

백업은 "받는 것" 이 아니라 **"복구되는 것"** 이라야 한다. 셋이 다 있어야 한다.

    받는다    PythonAnywhere 예약 작업으로 매일 dump_db
    옮긴다    같은 디스크에 두면 디스크가 죽을 때 같이 죽는다 — 밖으로
    되살린다  분기에 한 번, 빈 DB 에 실제로 복원해 본다

셋째가 빠지면 앞의 둘은 **백업이 있다는 믿음**일 뿐이다. `restore_db.py` 가
운영 호스트를 거부하도록 만들어 둔 것은 잘한 일이고, 그 덕에 복구 리허설을
안전하게 돌릴 수 있다.

> 지금 당장 할 일: PA 예약 작업 한 줄 + 외부 보관 한 곳. **반나절짜리다.**

### 2. 업로드 파일은 백업에 안 들어간다

`MEDIA_ROOT` 는 PA 디스크다. `dump_db` 는 **DB 만** 뜬다. 규격서·성적서·
시안은 전부 파일이고, DB 에는 경로만 있다. 디스크가 날아가면 DB 는 살아서
**파일 목록은 보이는데 열리지 않는 상태**가 된다. 이게 더 나쁘다 — 뭘 잃었는지
알면서 못 되찾는다.

> 선택지: (a) media 를 주기적으로 tar + 외부 보관, (b) 오브젝트 스토리지로
> 옮긴다(3 번과 함께 정리하면 한 번에 끝난다).

### 3. 단일 인스턴스 — "그날" 은 온다

웹 하나, DB 하나, 대기 없음. PA 가 멈추면 우리도 멈춘다. 지금 규모에서는
**허용 가능한 선택**이다. 다만 무엇을 받아들였는지는 적어 둬야 한다.

    지금 받아들인 것   PA 장애 = 우리 장애. 복구 시간은 PA 에 달려 있다
    선을 넘는 시점     ① 유료 고객이 생길 때 (SLA 를 말해야 한다)
                       ② 8-3 ①(대행사)를 붙일 때 (한 번에 수십 업체)

**홍보를 시작하기 전에 이 선을 정해야 한다.** 사람이 몰린 뒤에 옮기는 것이
가장 비싸다.

### 4. 비밀번호 기본값이 코드에 있다

```python
'PASSWORD': config('DB_PASSWORD', default='labeldata1!'),
```

`.env` 가 있으면 안 쓰인다. 그러나 **기본값이 있다는 것 자체가 위험**이다 —
`.env` 가 빠진 채 뜨면 오류로 멈추는 대신 그 값으로 **접속을 시도한다.**
운영 DB 가 그 비밀번호를 그대로 쓰고 있다면 소스만 보면 열린다.

`SECRET_KEY` 의 `'your-secret-key'` 도 같다. 세션·CSRF·비밀번호 재설정
토큰이 전부 이 값에 달려 있다.

> 할 일: 두 값의 `default=` 를 없앤다. 없으면 **뜨지 않는 것이 맞다.**
> 위의 `try/except UndefinedValueError` 가 바로 그러라고 있는 구조인데,
> `default=` 가 그 그물을 무력화하고 있다.
> 그리고 운영 DB 비밀번호가 실제로 `labeldata1!` 이면 지금 바꾼다.

### 5. 파일 캐시는 셈을 흘린다

주석에 이미 정직하게 적혀 있다 — "파일 캐시라 증가가 원자적이지 않다.
여러 일꾼이 같은 순간에 세면 몇 번은 흘린다." 분당 한도는 그래도 되지만,
**돈이 나가는 한도가 캐시에 있으면 안 된다.** 지금은 일일 한도를
`FeatureUsage`(DB)에 둬서 이 위험을 이미 피했다. 유지해야 할 판단이다.

남은 것: `MAX_ENTRIES: 500` 인 `default` 캐시에 **비용과 무관하지만 사라지면
곤란한 것**이 들어가지 않는지 한 번 훑는다.

### 6. OpenAI 지출 상한

앱 한도(`quota.py`)는 잘 돼 있다. 그러나 그것은 **우리 코드가 정상일 때만**
듣는다. 무한 루프 한 번, 재시도 설정 실수 한 번이면 청구서가 나간다.
(실제로 타임아웃·재시도 상한이 빠져 호출 하나가 30 분까지 늘어난 적이 있고,
그래서 그 시험이 있다.)

> 할 일: **OpenAI 계정 자체에 월 하드 캡**을 건다. 코드 바깥의 마지막 그물이라
> 5 분이면 되고, 코드로는 대신할 수 없다.

### 7. 취약 의존성 8 건

| 패키지 | 등급 | 실제 위험 | 조치 |
|---|---|---|---|
| `sqlparse` ≤ 0.5.5 | **高 3 · 中 1** | 낮다 — 사용자 입력 SQL 을 파싱하지 않는다 | 올린다 (Django 의존, 쉽다) |
| `djangorestframework` 3.17.1 | 中 2 | **업로드 크기 제한 우회** — 우리는 업로드를 받는다 | **3.17.2 로 올린다** |
| `fflate` (jspdf 안) | 中 | 낮다 — 우리가 만든 PDF 만 다룬다 | `npm audit fix` |

**DRF 것이 실질적**이다. `DATA_UPLOAD_MAX_MEMORY_SIZE` 우회는 곧 메모리
고갈이고, 단일 인스턴스(3 번)에서는 그게 곧 전면 중단이다.

> 셋 다 **버전만 올리면 되는 일**이다. 반나절 안에 끝난다.

### 8. 스테이징이 없다

배포가 곧 운영이다. 2,026 건의 테스트가 그물 역할을 하고 있고 실제로 잘
잡아 왔지만, 테스트가 못 보는 것이 둘 있다.

    마이그레이션이 **운영 데이터 위에서** 어떻게 도는가
    `collectstatic` 뒤 실제 화면 (실제로 CSS 를 빠뜨려 한 번 그대로 나갔다)

> 가장 싼 형태: PA 무료 앱 하나를 **운영 스키마 + 소량 데이터**로 띄우고
> 배포 전에 거기서 `migrate` 를 한 번 돌린다. `dump_db --schema-only` 가
> 이미 그 용도로 있다.

### 9. 표 크기와 PythonAnywhere 용량

`PublicFoodNutrition` 이 319,060 행이고, 원문 JSON 을 걷기 전에는 표가
1,670 MB 였다(그 칼럼 하나가 66%). 잘 처리했다. 다만 **용량은 PA 에서 벽이지
경고가 아니다** — 넘으면 쓰기가 실패하고, 그 순간 저장 버튼이 안 먹는다.

> 할 일: 남은 여유를 **숫자로** 확인하고, 다음 적재(농진청, 7-1 ④) 전에
> 늘어날 양을 먼저 계산한다. "해 보고 모자라면" 이 가장 나쁜 순서다.

### 10. 루트 `package.json` 이 남의 것이다

```json
{ "name": "demo-repo", "description": "A sample package.json",
  "dependencies": { "@primer/css": "17.0.1" } }
```

우리 것이 아니다. 실제 프런트엔드는 `frontend/` 에 따로 있다. 쓰지 않는
의존성이 보안 경보를 만들고, 경보가 늘면 **진짜 경보를 안 보게 된다.**
7 번의 DRF 건이 다섯 개의 소음에 묻혀 있던 것이 그 예다.

> 할 일: 지운다.

### 잘 되어 있는 것 — 건드리지 말 것

진단이 약점만 적으면 다음 사람이 멀쩡한 것을 고친다. 아래는 이미 답이 나온 자리다.

- **소유권 실패는 404**. 403 은 그 id 가 있다고 알려 준다
- **게스트는 비밀번호가 없다** (`set_unusable_password`). 공용 계정 하나를
  쓰던 것을 격리한 것이 그 뒤 모든 소유권 수정의 전제가 됐다
- **`restore_db.py` 가 운영 호스트를 거부한다**
- **자체 검사** — 단추 class, 색 토큰, 템플릿 주석, 끊어진 JS 문자열
- **한도의 두 축**(흐름/저량)을 나눠 둔 것. 요금제가 여기서 나온다
- **`.env` 만 비밀을 안다.** 코드·커밋에 키가 없다 (4 번의 `default=` 만 빼면)

### 지금 당장 (반나절 안)

    1. PA 예약 작업으로 dump_db 매일 + 외부 보관 한 곳       ← 1 번
    2. DB_PASSWORD · SECRET_KEY 의 default= 제거             ← 4 번
    3. DRF 3.17.2 · sqlparse 올림                            ← 7 번
    4. OpenAI 계정 월 하드 캡                                ← 6 번
    5. 루트 package.json 삭제                                ← 10 번
    6. DB·디스크 남은 용량 확인 (숫자로)                     ← 9 번

**이 여섯이 끝나기 전에는 홍보를 시작하지 않는다.** 사람이 오는 것이 지금은
위험이고, 위 여섯이 끝나면 그때부터는 기회다.

---

# 9. 실행 계획 — 무엇을 · 어떻게 · 왜

> 2026-09-12. 7·8 장이 "무엇이 문제인가" 라면 이 장은 **"그래서 무엇을 하는가"**
> 다. 각 일마다 **왜 지금인가 · 어떻게 하는가 · 무엇이 위험한가 · 끝났다는 것을
> 무엇으로 아는가**를 적는다. 판단에 필요한 것이 빠져 있으면 그 자리를 물어야
> 한다.

## 9-0. 먼저 — 앞 장에서 한 말 하나를 고친다

7 장과 8 장에서 **"시안 대조에 `value_match` 를 쓰면 된다"** 고 적었다.
코드를 열어 보니 **그대로는 안 된다.**

    value_match          Python (v1/label/services/value_match.py)
    시안 대조 비교 로직   JavaScript (v1/static/js/products/basic_info_ocr.js)

시안 대조는 화면에서 모달로 즉시 견주므로 **비교가 브라우저 안에서 끝난다.**
서버는 결과를 받아 문서함에 남기기만 한다(`design_compare_record`). 그래서
파이썬 모듈을 가져다 쓸 수가 없다.

무엇이 달라지나: **일의 크기는 그대로**(여전히 작다)지만 **방법이 다르다.**
아래 [작업 3] 에서 두 갈래를 놓고 고르게 적었다. 판단이 필요한 자리다.

확인한 것은 하나 더 있다. 지금 비교는 `compareGrade` **하나가 모든 항목에
같은 규칙**을 쓴다.

```js
function compareGrade(mine, theirs) {
  if (a === b) return 'spacing';
  if (a.length > 12 && b.length > 12 && (a.indexOf(b) >= 0 || ...)) return 'partial';
  return 'diff';          // ← 나머지는 전부 '다름'
}
```

그래서 앞서 든 예가 실제로 이렇게 떨어진다.

| 내 값 | 시안 | 지금 판정 | 옳은 판정 |
|---|---|---|---|
| `빵류` | `빵류 [가열하여…]` | **다름** (`빵류` 가 12자 미만이라 `partial` 도 못 탄다) | 같음(부기) |
| `(주)○○식품` | `주식회사 ○○식품` | **다름** | 같음(법인격 표기) |
| `12.5g` | `12.50g` | **다름** | 같음(수치) |
| `100 g` | `100그램` | **다름** | 같음(단위) |

셋 다 **순수한 오탐**이고, 사용자가 가장 자주 만나는 것들이다.

---

## 작업 1 — 게스트가 만든 것을 회원 계정으로 옮긴다

|  |  |
|---|---|
| 크기 | 小 (반나절~1일) |
| 위험 | 中 — 데이터를 옮기는 일이다. 되돌리기 설계가 필요 |
| 새 화면 | 없음 (가입 완료 흐름에 한 단계) |
| 마이그레이션 | **없음** |

### 왜 지금인가

게스트 격리는 끝냈다(`v1/common/guest.py`). 그런데 **승격 경로를 안 만들었다.**
지금 일어나는 일은 이렇다.

    게스트로 들어옴 → 제품 만듦 → BOM 넣음 → 검증 돌림 → "쓸 만하네" → 가입
    → **새 계정은 텅 비어 있다** → 게스트 계정은 stale_guests 가 지운다

**가장 열심히 써 본 사람의 결과물을, 정확히 그 사람이 돈을 낼 마음이 든
순간에 버린다.** 유입을 늘리는 어떤 일(8 장 전체, 6 장의 무료 도구)도 이것이
없으면 새는 깔때기에 물을 붓는 것이다. 그래서 1 번이다.

### 어떻게 하는가

**① 무엇을 옮길지 목록을 먼저 정한다.** `User` 를 가리키는 관계가 25 개쯤
있는데 **전부 옮기면 안 된다.** 옮길 것은 "그 사람이 만든 결과물" 이다.

| 옮긴다 | 안 옮긴다 | 왜 |
|---|---|---|
| `MyLabel` (제품) | `UserActivityLog` | 로그는 그때 그 계정의 사실이다 |
| `MyIngredient` (+ `MyIngredientNutrition`) | 게시판 글 | 게스트는 못 쓴다 |
| `ProductFolder` | 받은 공유 | 남이 그 게스트에게 준 것이다 |
| `ProductDocument` (문서함) | 알림 | 옮겨 봐야 의미 없다 |
| `UserPhrase` (자주 쓰는 문구) | | |

BOM 줄·영양성분은 제품·원료에 딸려 가므로 따로 옮기지 않는다 —
**FK 를 타고 자동으로 따라온다.** 이게 중요하다. 옮길 것은 다섯 개뿐이다.

**② 언제 옮기나.** 가입 직후가 아니라 **첫 로그인 직후**다. 가입은 이메일
인증이 걸려 있어(`email_verified_yn`) 가입 시점에는 아직 그 사람이 아니다.
세션에 "직전 게스트 id" 를 남겨 두고, 인증 후 첫 로그인에서 옮긴다.

**③ 물어보고 옮긴다.** 조용히 옮기면 안 된다. 두 가지 이유다.

- 남의 PC 에서 게스트로 써 본 것이 내 계정에 딸려 오면 곤란하다
- 무엇이 옮겨졌는지 모르면, 안 옮겨진 것이 있을 때 사용자가 알 수 없다

> "게스트로 만드신 **제품 3개 · 원료 12개**를 이 계정으로 가져올까요?"
> `[가져오기]` `[아니요]`

**④ 한 번만 옮긴다.** 옮긴 게스트 계정에 표시를 남기고 즉시 비활성화한다.
안 그러면 같은 게스트 세션이 살아 있는 동안 두 번 옮겨 갈 수 있다.

**⑤ 트랜잭션으로 묶는다.** 다섯 모델을 옮기다 가운데서 실패하면 절반만
옮겨진 채로 남는다. `transaction.atomic` 하나로 감싼다.

### 위험과 되돌리기

가장 나쁜 경우는 **남의 데이터를 잘못 붙이는 것**이다. 막는 법:

- 세션에 남긴 게스트 id 로만 옮긴다. 사용자가 고른 id 를 받지 않는다
  (받으면 남의 게스트 계정 id 를 넣어 데이터를 훔칠 수 있다)
- 옮기기 전에 `is_guest()` 로 그 계정이 정말 게스트인지 확인한다
- 옮긴 내역을 로그로 남긴다. 잘못되면 **되돌릴 근거가 그것뿐이다**

### 끝났다는 것을 무엇으로 아는가

    시험    게스트로 제품·원료를 만들고 승격하면 새 계정에서 보인다
            남의 게스트 id 를 넣으면 404 (403 아니다)
            두 번 부르면 두 번째는 아무 일도 없다
    운영    승격한 계정 수 · 옮겨진 제품 수를 로그로 센다

---

## 작업 2 — `link_ingredient_nutrition` 을 돌린다

|  |  |
|---|---|
| 크기 | **0** (실행만) + 小 (크론·저장 시점 연결) |
| 위험 | 小 — `--dry-run` 이 있고, 사람이 정한 값은 안 건드린다 |
| 새 화면 | 없음 |
| 마이그레이션 | **없음** |

### 왜 지금인가

**만들어 놓고 한 번도 안 돌렸다.** 지금 상태:

    원료 884 개 · 번호로 찾아지는 것 65 개 · 실제로 값이 정해진 것 **0 개**

65 개는 **사람이 고를 것이 없는 건**이다. 같은 품목보고번호는 법적으로 같은
품목이라 고민할 여지가 없다. 그런데도 지금 구조는 원료 상세를 하나씩 열어
눌러야 저장된다 — **고를 것이 없는데 손을 기다리는** 구조라 기능이 통째로
잠들어 있다.

비용이 0 이고 효과가 즉시 보이는 유일한 일이다.

### 어떻게 하는가

```bash
cd ~/mysite && source ~/.virtualenvs/mysite-env/bin/activate
python manage.py link_ingredient_nutrition --dry-run   # ① 먼저 센다
python manage.py link_ingredient_nutrition             # ② 실제로 붙인다
```

① 이 중요하다. 숫자가 65 근처가 아니면(0 이거나 800 이면) **뭔가 다르게
돌고 있는 것**이니 붙이기 전에 멈춘다.

그다음 두 가지를 코드로 잇는다.

- **주 1 회 크론** (PA 예약 작업). 원료는 계속 늘어난다
- **원료를 저장하는 순간에도 한 번 시도.** 배치를 기다릴 이유가 없다.
  번호가 있고 딱 맞는 행이 있으면 그 자리에서 붙인다

### 왜 안전한가 — 이미 그렇게 만들어져 있다

`MyIngredientNutrition` 이 이미 있는 원료는 **지나간다.** 성적서를 넣었거나
후보를 고른 것을 자동 판단으로 덮으면 사람이 한 일이 조용히 사라지기
때문이다. 덮으려면 `--force` 를 명시해야 한다. 이 판단은 유지한다.

### 끝났다는 것을 무엇으로 아는가

    원료당 영양성분 보유율   0% → 7% 대
    배합 20 개 중 계산되는 제품   0 개 → 몇 개 (원료가 겹치는 만큼)

두 번째가 진짜 지표다. 원료에 값이 붙어도 **한 제품의 원료가 전부 채워져야**
계산이 돈다. 그래서 7% 가 곧바로 "계산되는 제품 7%" 가 되지는 않는다.
그 간극이 [작업 6] 이 필요한 이유다.

---

## 작업 3 — 시안 대조: 항목마다 다른 자를 쓴다

|  |  |
|---|---|
| 크기 | 小 (1~2일) |
| 위험 | 中 — **느슨하게 만드는 일이라, 지나치면 놓침이 는다** |
| 새 화면 | 없음 |
| 마이그레이션 | 없음 |

### 왜 지금인가 — 오탐과 놓침은 값이 다르다

    놓침(miss)   다른데 같다고 했다    → 인쇄 사고. 치명적이지만 **안 보인다**
    오탐(false)  같은데 다르다고 했다  → 매번 보인다. **기능을 안 쓰게 만든다**

지금 이 기능이 "제대로 못 읽는다" 는 말을 반복해서 들은 것은 대부분 오탐
쪽이다. 사용자는 세 번 겪으면 이 기능을 닫고, 닫은 기능은 놓침도 못 막는다.

### 갈림길 — 여기서 판단이 필요하다

비교가 JavaScript 안에 있다(9-0). 두 길이 있다.

**갈래 A. 지금 자리에서 고친다** — `FIELD_MAP` 에 항목마다 규칙을 적는다

```js
prdlst_dcnm:    { id: '…', label: '식품유형(표시용)', match: 'prefix'  },
bssh_nm:        { id: '…', label: '제조원',          match: 'company' },
content_weight: { id: '…', label: '내용량',          match: 'number'  },
cautions:       { id: '…', label: '주의사항',        match: 'sentence'},
```

- 장점: **1~2 일.** 화면 반응이 지금처럼 즉시. 서버 부하 없음
- 단점: 같은 정책이 파이썬(`value_match`)과 JS 두 곳에 산다. 언젠가 한쪽만
  고쳐진다 — 이 저장소가 실제로 여러 번 당한 실패다
- 그래서 **두 곳이 같은지 보는 시험**을 함께 넣는다. 항목 이름과 규칙 이름이
  양쪽에서 같은지 대조하는 시험 하나면 어긋남이 조용히 지나가지 않는다

**갈래 B. 비교를 서버로 옮긴다** — JS 는 결과만 그린다

- 장점: 정책이 **한 곳**(`value_match`)에 산다. 검증·판독과 같은 자를 쓴다
- 단점: **3~5 일.** 모달이 왕복 한 번을 더 기다린다. 기존 화면을 크게 건드린다

> **권합니다: A.** 까닭은 지금 급한 것이 "구조가 아름다운 것" 이 아니라
> **오탐을 지금 줄이는 것**이고, B 로 가도 규칙 표 자체는 똑같이 새로 써야
> 하기 때문이다. A 로 규칙이 실제로 맞는지 확인한 뒤, [작업 11](좌표)을 할
> 때 B 로 합치는 편이 낫다. 다만 **시험으로 두 곳을 묶는 것은 필수**다.

### 규칙 표 — 무엇을 어떻게 견주나

| 항목 | 규칙 | 뜻 | 막는 오탐 |
|---|---|---|---|
| 식품유형(표시용) | `prefix` | 앞이 같고 뒤가 괄호 부기면 같다 | `빵류` ↔ `빵류 [가열…]` |
| 제조원·판매원·수입원 | `company` | 법인격 표기를 지우고 이름만 | `(주)○○` ↔ `주식회사 ○○` |
| 내용량 | `number` | 숫자+단위로 정규화해 **수치 비교** | `12.5g` ↔ `12.50g`, `100 g` ↔ `100그램` |
| 소비기한 | `date` | 날짜 표기 정규화 | `2027.03.01` ↔ `2027-03-01` |
| 주의사항·알레르기 | `sentence` | **문장 단위로 쪼개 집합 비교** | 줄바꿈 자리만 다른 것 |
| 원재료명 | `tokens` | 중점·괄호로 쪼개 집합 비교 | 표기 차이 |
| 나머지 | `exact` | 지금 그대로 | |

`sentence` 가 특히 크다. 주의사항은 여러 문장이고 시안은 줄바꿈 자리가
달라서, 지금은 **통째로 "다름"** 이 된다. 문장으로 쪼개면 **어느 문장이
빠졌는지**를 짚을 수 있다 — 오탐이 줄면서 동시에 지적이 정확해진다.

### 위험 — 느슨해지는 방향이라는 것

이 작업은 전부 **"같다고 보는 범위를 넓히는"** 일이다. 지나치면 놓침이 는다.
그래서 두 가지를 지킨다.

- **수치는 절대 느슨하게 하지 않는다.** `number` 는 표기만 정규화하고
  **값이 다르면 무조건 다르다.** `12.5` 와 `12.6` 은 다르다
- 규칙 때문에 "같음" 으로 내린 것은 **그 까닭을 표시한다** — "같음(부기)",
  "같음(법인격 표기)". 사람이 판단을 되짚을 수 있어야 한다

### 끝났다는 것을 무엇으로 아는가

**[작업 5] 없이는 모른다.** 그래서 5 가 바로 뒤에 온다. 우선은 위 표의 네
가지를 손으로 넣어 보고 "같음" 으로 떨어지는지 확인한다.

---

## 작업 4 — 시안에 없는 글자를 "시안에 있다" 고 말하지 않게

|  |  |
|---|---|
| 크기 | 小 (반나절~1일) |
| 위험 | 小 |

### 왜 지금인가

`ocr_ground` 는 **"모델이 낸 값이 OCR 원문에 실제로 있던 글자인가"** 를 본다.
VLM 은 값을 지어내지만 전통 OCR 은 글자를 보고 글자를 내므로 지어낼 수가
없다 — 그래서 원문을 **정답이 아니라 증인**으로 쓴다.

이것이 **판독 경로에만 걸려 있고 시안 대조에는 안 걸려 있다.** 그래서 지금
시안 대조는 "시안에 이렇게 적혀 있습니다" 라고 말하면서, 그 글자가 정말
시안에 있었는지는 확인하지 않는다.

**가장 나쁜 오탐이 이것이다.** 시안에 없는 글자를 있다고 말하면 사용자는
멀쩡한 시안을 고치러 간다.

### 어떻게 하는가

새로 만들 것이 없다. 시안 판독에도 같은 검사를 **끼워 넣기만** 한다.
`ocr_ground` 가 이미 정한 규칙을 그대로 따른다 — **값을 고치지 않고
confidence 를 내리고 표시만 한다.** OCR 도 틀리기 때문이다.

화면에서는 확신이 낮은 값을 **"다름" 으로 세우지 않고 "확인 필요"** 로
따로 모은다. 우리가 잘못 읽었을 가능성이 있는 것을 시안 탓으로 돌리지 않는다.

---

## 작업 5 — 잰다. "좋아졌다" 를 주장하지 않는다

|  |  |
|---|---|
| 크기 | 小 (1~2일) + 정답셋 만드는 시간 |
| 위험 | 없음 |
| **사용자 협조 필요** | **정답셋 20~30 건** — 내가 못 만든다 |

### 왜 지금인가

작업 3 과 4 는 **판정 기준을 바꾸는 일**이다. 기준을 바꾸면 어딘가는 좋아지고
어딘가는 나빠지는데, **지금은 나빠진 것을 알 방법이 없다.** 눈으로 몇 건
보고 "좋아졌다" 고 말해 왔고, 그래서 고칠 때마다 다른 곳이 나빠져도 몰랐다.

`ocr_benchmark` 가 이미 정답셋 채점을 한다. **시안 대조 전용 정답셋**만
없다.

### 어떻게 하는가

**① 정답셋을 모은다 (여기서 도움이 필요하다).** 실제 제품 라벨 20~30 건과,
그에 대응하는 시안 이미지. 그리고 **일부러 틀린 시안 몇 건** — 숫자 하나를
바꾼 것, 문구 하나를 뺀 것. 놓침을 재려면 틀린 것이 있어야 한다.

**② 놓침률과 오탐률을 따로 낸다.** 하나로 합치면 개선이 안 보인다.
작업 3 은 오탐을 줄이는 일이고 놓침을 늘릴 위험이 있어, **두 수를 나란히
봐야** 판단이 된다.

**③ 세 번 돌려 평균과 편차를 본다.** 같은 사진도 매번 다르게 읽힌다.
한 번 돌린 결과로 판단하면 안 된다.

### 이 작업이 진짜로 하는 일

이것 없이 작업 3·4 를 하면 **고쳤다는 믿음만 남는다.** 그래서 3·4·5 는
사실상 한 묶음이고, 5 를 빼면 3·4 의 값이 절반으로 준다.

---

## 작업 6 — 묻는 자리를 옮긴다

|  |  |
|---|---|
| 크기 | 中 (3~5일) |
| 위험 | 小 |
| 새 화면 | 영양성분 탭 안의 묻는 창 하나 |

### 왜 지금인가 — 기능이 없는 것이 아니라 자리가 틀렸다

원료에 영양성분을 붙이는 **후보 고르기는 이미 다 만들어져 있다**
(`nutrition_candidates`). 그런데 그 자리가 **원료 상세 안쪽**이다.
아무도 거기까지 가지 않는다.

사용자가 원료의 영양성분을 원하는 **유일한 순간**은 영양성분 탭에서
**[계산하기] 를 눌렀을 때**다. 그때 "원료 12 개 중 8 개에 값이 없습니다"
라고만 말하고 끝나면, 사용자는 원료를 하나씩 찾아 열어야 한다. 아무도 안 한다.

### 어떻게 하는가

[계산하기] 직후, **그 자리에서 연달아 묻는다.**

    영양성분을 계산하려면 원료 3 개의 값이 필요합니다. (나머지는 영향 없음)

      1/3  정제소금  2.0%
           ○ 소금, 천일염          Na 38,000mg  분석·2019
           ○ 소금, 정제소금        Na 39,100mg  분석·2021
           [ 이 원료는 값이 없어도 됩니다 ]     [ 건너뛰기 ]

**묻는 수를 줄이는 것이 핵심이다.** `nutrition_contrib` 이 이미 "표시값을
흔들 수 있는 원료" 를 가려낸다. 원료 20 개라도 실제로 물을 것은 대여섯이다.
**스무 번 물으면 아무도 안 끝내고, 다섯 번이면 끝낸다.**

### 왜 자동으로 정하지 않나 — 이건 바뀌지 않는다

'버터' 라는 이름으로 동명 항목이 **열두 건**인데 열량이 이렇게 갈린다.

    164 kcal · 지방  2.69 g · 수집 · 2025   ← 이걸 집으면 열량이 1/4.6 로 틀린다
    761 kcal · 지방 82.04 g · 분석 · 2014   ← 진짜 버터

**이름이 완전히 같다.** 어떤 유사도 알고리즘도 둘을 가르지 못한다. 이건
알고리즘을 정교하게 만들어 풀 문제가 아니다. 사람이 한 번 고르고, 그
선택을 남겨 **다시 묻지 않는다**(`MyIngredientNutrition.picked`).

---

## 작업 7 — 성적서에서 읽는다 (등급 A 를 만드는 유일한 길)

|  |  |
|---|---|
| 크기 | 中 (5~7일) |
| 위험 | 中 — 판독 오류가 A 등급으로 저장되면 가장 나쁘다 |
| 비용 | **OpenAI 호출이 는다** — 한도 설계 필요 |

### 왜 이 일이 특별한가

원료 영양성분의 출처에는 등급이 있다.

    A  spec_ocr    업체 시험성적서
    B  report_no   품목보고번호가 식약처 DB 와 정확히 일치
    C  picked      후보 중 사람이 고름 / manual 직접 입력

**공공 DB 로 채운 값은 영원히 C 다.** 식약처 DB 의 밀가루는 "일반적인
밀가루" 이지 "우리가 쓰는 그 밀가루" 가 아니기 때문이다. 이론치로 만든
영양성분표는 그 자체가 감사 대상이라, **"이 숫자가 어디서 왔는가" 가 값만큼
중요하다.**

`SOURCE_SPEC_OCR` 은 모델에 이미 정의돼 있다. **그 값을 넣는 화면만 없다.**
그리고 그 종이는 **이미 우리 문서함에 쌓이고 있다.**

### 어떻게 하는가

문서함에서 종류가 시험성적서·규격서인 파일에 **[영양성분 읽기]** 를 붙인다.
그 뒤는 기존 파이프라인을 그대로 탄다.

    판독 → ocr_ground(지어냄 차단) → 확인 창(사람이 고침) → 원료에 저장(등급 A)

새로 만들 것은 **성적서용 프롬프트 한 벌과 확인 창**뿐이다.

### 위험 — A 등급이라는 이름의 무게

판독이 틀린 값이 **A 등급**으로 저장되면 가장 나쁘다. 등급이 높다는 것은
"이 값을 믿어도 된다" 는 뜻인데, 그게 거짓이면 아래 모든 판단이 무너진다.
그래서:

- **사람이 확인 창에서 승인해야 저장한다.** 자동 저장 없음
- 원본 파일을 **그 값에 연결해 둔다.** 나중에 "이 숫자 어디서 왔죠" 에
  종이를 댈 수 있어야 A 등급이 의미가 있다
- 이 경로로 들어온 값도 **사람이 덮어쓸 수 있다**

---

## 작업 8 — 공용 원료 풀 (읽기 전용 집계부터)

|  |  |
|---|---|
| 크기 | 中 (1단계 2~3일 / 2단계 5~7일) |
| 위험 | **高 — 이 목록에서 가장 되돌리기 어려운 일** |
| 마이그레이션 | 1 단계 없음 · 2 단계 있음 |
| **사용자 결정 필요** | **약관·기본값·고지 방식** |

### 왜 나중인가

값이 크지만 **되돌릴 수 없는 일**이기 때문이다. 한 번 "다른 회사 값을
보여 준다" 를 켜면 끄기 어렵고, 틀린 값이 퍼지면 우리 책임이 된다.
앞의 일곱은 전부 우리 안에서 끝나는 일이라 실패해도 되돌릴 수 있다.

### 1 단계 — 표를 만들지 않고 먼저 잰다 (2~3일)

새 모델을 만들지 않는다. `MyIngredient` 를 `prdlst_report_no` 로 묶어
**읽기 전용으로 세기만** 한다. 알아야 할 것은 셋이다.

    같은 번호를 두 곳 이상이 등록한 원료가 몇 개인가?
    그 원료들의 알레르기·표시명이 **실제로 같은가, 갈리는가?**
    갈린다면 얼마나?

**이 답을 모르는 채로 만들면 안 된다.** 겹치는 원료가 열 개뿐이면 이 기능은
가치가 없고, 값이 심하게 갈리면 보여 주는 것 자체가 위험하다.

> 표부터 만들지 않는 까닭: 3 장(식품구분 4분류)에서 설계를 먼저 하고
> 데이터를 나중에 봤다가 통째로 버린 적이 있다. 순서를 뒤집으면
> **아무도 안 쓰는 표를 운영하게 된다.**

### 2 단계 — 보여 준다 (1단계 결과가 좋을 때만)

**선을 먼저 긋는다. 이건 협상 대상이 아니다.**

| 나눈다 (공시 정보와 그 파생) | 절대 안 된다 |
|---|---|
| 품목보고번호 · 원료명 · 제조사 | **배합비** |
| 식품유형 · 표시명 후보 | **거래처 · 단가** |
| 알레르기 · GMO | **누가 등록했는지** |
| 영양성분 + 출처등급 | **어느 제품에 쓰는지** |

왼쪽은 전부 품목보고 공시에 이미 공개된 정보이거나 그 파생이다. 오른쪽은
영업비밀이고, **하나라도 새면 서비스가 끝난다.**

보여 주는 방식 — **덮어쓰지 않고 제안만 한다.**

    이 번호로 등록된 값이 있습니다
      알레르기  우유, 대두          (7 곳이 같게 적음)
      영양성분  열량 380 kcal …     (출처: 업체 시험성적서 · 등급 A)
      [ 내 원료에 적용 ]   [ 무시 ]

- **자동으로 넣지 않는다.** 틀린 값이 퍼지는 것이 이 구조의 유일한 실패
  방식이고, 그 순간 우리 책임이 된다
- **값이 갈리면 다수결로 고르지 않는다.** 일곱이 틀리고 하나가 맞는 경우가
  있다. **갈렸다고 말하고 둘 다 보여 준다**
- 출처등급을 함께 보인다. **A(성적서) 하나가 C(이름으로 고름) 일곱보다 낫다**

### 사용자가 결정해야 할 것

- 약관에 "공개 가능 항목이 익명으로 집계될 수 있다" 를 넣을 것인가
- 기본값을 **켬**으로 할 것인가 **끔**으로 할 것인가
  (켬이면 풀이 빨리 차지만 고지가 더 중요해진다)
- 끄는 스위치는 반드시 준다 — 이건 선택지가 아니다

---

## 작업 9~11 — 그 뒤

| 순 | 일 | 크기 | 왜 여기인가 |
|---|---|---|---|
| 9 | 지표 넷 대시보드 | 小 | 1~8 의 효과를 재는 자리. `UserActivityLog` 로 **지금 데이터로 낼 수 있다** |
| 10 | 농진청 표준성분표 적재 | 小 | 연결이 된 뒤라야 의미가 있다. **[작업 인프라 6] 용량 확인이 선행** |
| 11 | 좌표 기반 시안 대조 | 大 | 작업 3 이 남긴 것만 남았을 때 |

**지표 넷** (9): 가입 후 첫 검증까지 걸린 시간 · 단계별 이탈률(제품→BOM→검증)
· 7/28일 재방문 · 원료당 영양성분 보유율. 넷 다 새로 심을 것이 없다.

---

## 인프라 — 이것들은 위 작업과 **경쟁하지 않는다**

반나절짜리 여섯이고, 기능 작업과 별개로 먼저 끝내는 것이 맞다.
자세한 사유는 8-4 에 있다. 여기서는 **누가 하느냐**만 정리한다.

| # | 일 | 누가 | 비고 |
|---|---|---|---|
| I-1 | `dump_db` 매일 + **외부 보관** + 분기 복구 리허설 | **사용자** (PA 콘솔) | 가장 아픈 자리 |
| I-2 | `DB_PASSWORD`·`SECRET_KEY` 의 `default=` 제거 | 내가 | 운영 비밀번호가 `labeldata1!` 이면 **함께 교체** |
| I-3 | DRF 3.17.2 · sqlparse 올림 | 내가 | DRF 것이 실질적 |
| I-4 | **OpenAI 계정 월 하드 캡** | **사용자** | 코드로 대신할 수 없는 마지막 그물 |
| I-5 | 루트 `package.json` 삭제 | 내가 | 남의 것(`"demo-repo"`)이라 경보 소음만 만든다 |
| I-6 | DB·디스크 남은 용량 **숫자로** 확인 | **사용자** | 작업 10 의 선행 조건 |
| I-7 | 업로드 파일(media) 백업 | **사용자** + 내가 | I-1 다음으로 아프다 |

**I-2·I-3·I-5 는 지금 바로 할 수 있다.** 나머지 넷은 PythonAnywhere·OpenAI
콘솔이라 내가 접근할 수 없다.

---

## 한눈에 — 순서와 까닭

```
인프라 I-2 I-3 I-5   내가 지금            ┐
인프라 I-1 I-4 I-6   사용자가             ┘ 사람이 오기 전에 끝낸다

 1  게스트 승격        小   유입의 모든 일이 이것 없이는 새는 깔때기
 2  link_… 실행        0    만들어 두고 안 돌렸다. 0 → 65
 3  항목별 비교 규칙    小   오탐이 기능을 닫게 만든다  ┐
 4  지어냄 차단         小   가장 나쁜 오탐            ├ 한 묶음
 5  정답셋 + 회귀       小   3·4 가 나아졌는지 재려면  ┘
 6  [계산하기]서 묻기   中   기능이 있는데 자리가 틀렸다
 7  성적서 → 등급 A     中   A 를 만드는 유일한 경로
 8  공용 풀 (1단계)     中   가장 큰 기회 · 가장 되돌리기 어렵다
 9  지표 대시보드       小   1~8 을 재는 자리
10  농진청 적재         小   연결된 뒤라야 의미가 있다
11  좌표 대조           大   3 이 남긴 것만 남았을 때
```

### 지금 판단이 필요한 것 넷

1. **작업 3 의 갈래** — A(JS 에서, 1~2일) / B(서버로, 3~5일). **A 를 권한다**
2. **작업 5 의 정답셋** — 라벨 20~30 건과 시안. 내가 못 만든다
3. **작업 8 의 약관·기본값** — 1 단계(재기)는 그냥 해도 되고, 2 단계 전에 필요
4. **인프라 I-1·I-4·I-6** — 사용자만 할 수 있다. **이게 먼저다**
