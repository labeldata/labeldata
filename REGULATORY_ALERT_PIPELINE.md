# 부적합·행정처분·수거검사 알림 — 수집 → 표시 → 알림 → 처리

한 화면(`/regulatory/`)의 세 탭이 모두 "알림"이다. 그런데 그동안 셋이 서로 다른
말과 서로 다른 주소를 썼다. 이 문서는 지금의 흐름을 한 줄로 세우고, 어디를
고치면 세 탭이 같이 바뀌는지를 적는다.

---

## 1. 네 단계와 그 경계

| 단계 | 하는 일 | 코드 |
|---|---|---|
| **수집** | 식약처 OpenAPI·새올에서 원본을 받아 저장 | `services/collector.py` |
| **매칭** | 원본 ↔ 내 제품·원료·업체를 맞춰 "내 알림"을 만든다 | `services/matcher.py`, `collector._trigger_inspection_match` |
| **표시** | 목록·배지·상세로 보여 준다 | `views.news_list`, `selectors.py` |
| **처리** | 읽음 / 조치 / 끄기 | `views.mark_tab_read`, `views.save_match_action`, `services/mute.py` |

경계 규칙 하나만 지키면 화면이 어긋나지 않는다.

> **건수를 세는 규칙은 `selectors.py` 한 곳에만 둔다.**
> 사이드바 배지·홈 대시보드·탭 배지·목록 헤더·카운트 API 가 모두 여기를 본다.
> 예전에 이 규칙이 네 곳에 복제돼 화면마다 숫자가 달랐다.

---

## 2. 알림은 네 갈래로 들어온다

| 갈래 | 무엇이 걸리나 | 저장되는 표 | 끄는 법 |
|---|---|---|---|
| 내 제품 매칭 | 뉴스 키워드 ↔ 내 BOM·원료·제품명 | `NewsProductMatch` | 키워드/원료 **끄기** |
| 원료 보관함 매칭 | 뉴스 키워드 ↔ BOM 미연결 원료 | `NewsIngredientMatch` | 키워드/원료 **끄기** |
| 키워드 알림 | 내가 등록한 키워드 | `PushNotificationLog(trigger_type='keyword')` | 그 **키워드 삭제** |
| 수거검사 | 품목보고번호·인허가번호·업소명 | `InspectionMatch` | 업소명 **끄기** |

**받겠다(`AlertRule`) 와 안 받겠다(`AlertMute`) 는 서로 다른 표다.**
같은 말을 양쪽에 넣으면 어느 쪽이 이겼는지 화면에서 설명할 수 없으므로,
직접 등록한 키워드를 끄려 하면 409 로 되돌려 보내 키워드를 지우게 한다
(`views.alert_mutes_api`).

---

## 3. 처리는 세 가지뿐이고, 서로 다른 일이다

세 탭 어디서든 같은 말과 같은 단추를 쓴다.

| 처리 | 뜻 | 되돌릴 수 있나 | 주소 |
|---|---|---|---|
| **모두 읽음** | 봤다 | (다시 안 읽음으로 못 돌림) | `POST api/mark-tab-read/` |
| **조치** (진행 중·완료·해당없음·메모) | 처리했다 | 이력이 쌓임 | `POST api/save-action/` |
| **끄기** | 앞으로 안 받겠다 | 받지 않기 목록에서 해제 | `POST api/alert-mutes/` |

**읽음과 조치 완료를 섞지 않는다.** 예전에는 부적합·행정처분 탭에서 배지를
지울 방법이 상세 하단의 "전체 알림 일괄 처리"(= 되돌릴 수 없는 조치 완료
기록) 뿐이라, 그냥 훑고 넘기려던 사람이 조치 이력을 남겨야 했다.
지금은 세 탭 모두 툴바에 **모두 읽음**이 있고, 그 단추는 조치 이력을 남기지
않는다.

---

## 4. "왜 나한테 왔는가" 는 상세의 표준 항목이다

세 탭의 상세가 모두 **알림 사유** 라는 같은 제목·같은 자리를 쓴다.
좌변(원본 쪽 값)과 우변(내 쪽 값)을 화살표로 잇는다 — 어느 쪽을 꺼야 하는지는
사람마다 달라서, 양쪽을 다 보여 줘야 고를 수 있다.

```
알림 사유   [대장균]  ↔  [정제수]        위해도 50pt · 유사도 90
🔕 이것 때문에 알림이 잦다면  [키워드 '대장균' 끄기] [내 원료 '정제수' 끄기]
```

- 행정처분·지자체처분은 업체명 하나로 걸린다 → 좌변이 업체명이고, 끄는 것도 업체다.
- 수거검사는 업소명이 오탐의 주범이다(회사명이 짧으면 남의 업체까지 걸린다)
  → 업소명을 끈다. 내 제품 번호가 맞아 걸린 건은 끄는 것이 아니라
  "해당 없음(이 건만 삭제)" 이 맞다.
- 키워드 알림은 **내가 등록한 것** 때문에 왔다 → 그 자리에서 바로 지운다.
  예전에는 설정 모달을 열어 등록해 둔 목록에서 같은 말을 눈으로 찾아야 했다.

---

## 5. 끄면 두 가지가 함께 일어난다 (`services/mute.py`)

1. **앞으로 안 생긴다** — `matcher` 와 `collector` 가 `AlertMute` 를 보고 매칭을
   만들지 않는다.
2. **이미 쌓인 것을 치운다** — 그러지 않으면 껐는데도 목록이 그대로라
   "안 먹었다" 고 판단하고 같은 것을 또 누른다.

치우는 표시는 기존의 **오탐지/해당 없음과 같은 것**을 쓴다
(`false_positive_yn` / `dismissed_yn`, 수거검사는 삭제).
상태를 새로 만들면 배지·필터·오탐 학습이 저마다 다른 규칙을 갖게 된다.

되돌리면(해제) **앞으로 오는 것부터** 다시 온다. 치운 지난 알림은 되살리지
않는다 — 되살리면 정리한 목록이 갑자기 불어나 무엇이 새 알림인지 알 수 없다.

**한 가지 예외를 둔다.** 뮤트로 한꺼번에 치워진 건은 오탐 학습(Approach C)의
모수에서 뺀다. 한 번에 수백 건이 오탐으로 표시되므로 그대로 태우면 그 키워드의
FP 누적이 폭증해, 다른 원료에 걸린 정상 매칭까지 등급이 깎인다
(`matcher._load_fp_patterns_for_user`).

---

## 6. 앱(식품 안심 알리미) 푸시 — 웹과 같은 규칙을 따른다

푸시는 **수집 즉시 나가지 않는다.** 로그만 먼저 쌓고
`send_pending_alerts` 커맨드가 일 3회(10·14·17시 KST) 배치로 내보낸다.
수거검사 판정 변동(`PHASE_JUDGMENT`)만 예외로 즉시 나간다.

```
수집 03:00 ─→ 매칭 ─→ PushNotificationLog(sent_at=NULL) / InspectionMatch(fcm_sent_at=NULL)
                                   │
                        (여기 사이에 사용자가 끄면?)
                                   │
배치 10:00 ─→ 사용자별로 묶어 FCM 1건
```

이 시차가 "껐는데 또 온다" 의 원인이었다. 03시에 걸린 알림을 09시에 꺼도,
로그는 발송 대기에 그대로 남아 10시에 울렸다. 키워드 삭제는 더 나빴다 —
`PushNotificationLog.rule_triggered` 가 `SET_NULL` 이라, 규칙을 지우면 로그는
"누가 부른 알림인지"만 잃은 채 그대로 나갔다.

그래서 **끄는 모든 길이 예약된 푸시를 함께 거둔다**
(`push_service.cancel_pending_logs`).

| 어디서 | 무엇을 거두나 |
|---|---|
| 웹 — 알림 끄기 | 치운 매칭의 뉴스로 걸린 제품·원료 푸시 |
| 웹 — 키워드 삭제 | 그 규칙으로 걸린 키워드 푸시 |
| 앱 — 키워드 삭제·끄기 (`mobile.views.rule_detail`) | 같음 (비회원은 그 기기만) |
| 웹 — 업체 끄기 | `InspectionMatch` 자체를 지우므로 수거검사 푸시도 함께 사라진다 |

거두는 방법도 갈라 둔다.

- **아직 안 나간 로그**(`sent_at IS NULL`) → 삭제. 보낸 적이 없으니 이력도 아니다.
- **이미 나간 로그** → 읽음 처리만. 앱 알림 탭에 남기되 배지는 내린다.
  지우면 "어제 받은 그 알림" 을 다시 찾을 수 없다.

앱 알림 탭(`mobile.views.notifications_list`)은 `PushNotificationLog` +
`InspectionMatch` 를 병합해 보여 준다. 둘 다 끄기가 정리하는 대상이므로,
웹에서 끈 것은 앱 목록에서도 함께 사라진다 — 따로 맞출 것이 없다.

---

## 7. 한 곳만 고치면 세 탭이 같이 바뀌는 자리

| 무엇 | 어디 |
|---|---|
| 어떤 출처가 '행정처분' 탭인가 | `views.ADMIN_API_SOURCES` |
| 탭 키 이름 | `views.TAB_INSP_NEWS / TAB_ADMIN / TAB_INSPECTION` |
| 건수 산출 규칙 | `selectors.py` |
| 수거검사 판정 상태 | `models.InspectionResult.PENDING_JUDGMENTS / FINAL_JUDGMENTS`, `judgment_status_of()` |
| 끄기 값의 정규화 | `models.normalize_mute_value()` |
| 끄기 단추의 동작 | `static/js/regulatory_alert_mute.js` (세 화면이 함께 쓴다) |
| 모두 읽음 | `views.mark_tab_read` (옛 주소 `inspection_mark_all_read` 도 같은 처리) |
| 예약된 푸시 거두기 | `mobile/services/push_service.cancel_pending_logs` |

---

## 8. 화면이 스크립트에 기대지 않게 한다

이 화면은 **탭 전환도, 걸러 보기도, 페이지 넘김도 전부 페이지 이동**이다.
서버가 이미 아는 것을 화면에 그리지 않고 브라우저에서 고쳐 주는 구조를 두면,
스크립트가 한 번 멈출 때 그 모든 것이 함께 멈춘다.

실제로 그렇게 됐다. 인라인 `<script>` 한 블록에서 문자열 한 줄이 끊겨
(따옴표가 닫히기 전에 줄바꿈) 그 블록의 함수가 **전부** 정의되지 않았고,
증상은 "행정처분·수거검사 탭이 클릭되지 않는다" 하나로만 나타났다.
탭 단추는 멀쩡히 보이고 화면에 오류도 안 뜬다.

그래서 규칙을 둔다.

> **그 자리에 있는지 없는지, 어느 것이 켜져 있는지는 서버가 그린다.**
> 스크립트는 이미 그려진 것을 바꾸는 일만 한다.

| 무엇 | 서버가 그리는 것 | 스크립트가 하는 것 |
|---|---|---|
| 지금 탭 | `#regSidebar[data-view]`, `#regPage` 클래스, `.rs-vtab--active` | 없음(초기화는 확인만) |
| 모두 읽음 단추 | 지금 탭에 미확인이 있을 때만 그린다 | 눌러서 다 읽으면 거둔다 |
| 수거검사 진행/완료 필터 | `insp_status` 로 걸러 그린다 | 주소를 바꿔 다시 요청 |
| 상세 조건 잠금 | 수거검사 탭이면 `disabled` | 없음 |

`manage.py check` 의 **templates.E006** 이 끊긴 문자열을 배포 전에 잡는다.
정규식 리터럴과 백틱은 건너뛴다.

---

## 9. 아직 남은 것

- **앱을 안 쓰는 사용자에게 키워드 알림이 아무 일도 하지 않는다.** ← 확인됨
  키워드 매칭은 `PushNotificationLog` 로만 남는데, 그 로그는 `AppDevice` 마다
  만들어진다(`push_service._save_keyword_logs`, `backfill_alerts_for_rule`).
  앱을 깐 적 없는 웹 사용자는 기기가 없으므로 로그가 하나도 안 생긴다.
  실측:

  | | 등록 응답 matched_count | 목록의 '키워드' 배지 | 상세의 키워드 구역 |
  |---|---|---|---|
  | 앱 기기 없음 | **0** | **안 뜸** | **안 뜸** |
  | 앱 기기 있음 | 5 | 뜸 | 뜸 |

  게다가 응답은 `matched_count=0` 인데 `previews` 는 5건을 함께 돌려준다 —
  화면에는 "일치하는 정보가 없습니다" 라고 뜨면서 미리보기가 5건 보인다.
  고치는 길은 둘 중 하나다.
    ① 로그인 사용자에게 웹 전용 `AppDevice`(platform='web')를 하나 보장한다.
       views.news_list 에 그러겠다는 주석과 쓰이지 않는 import 가 있었는데
       (걷어냄) 실제로 붙은 적은 없다.
    ② 키워드 매칭을 기기가 아니라 **사용자**에 붙는 표로 옮긴다. 제품·원료
       매칭(`NewsProductMatch`/`NewsIngredientMatch`)과 같은 결이 된다.
  ②가 옳지만 표가 하나 늘고 앱 API 도 함께 옮겨야 한다. ①은 작지만
  "웹 사용자에게 가짜 기기를 만든다" 는 빚이 남는다.

- **정적 파일 캐시 무효화가 저장소 전반에 빠져 있다.**
  이 화면 것(`regulatory.css`, `regulatory_list.js`)은 `?v={{ STATIC_BUILD_DATE }}`
  를 붙였지만, 저장소에는 아직 CSS 16곳·JS 5곳이 버전 없이 걸려 있다.
  `check_static_cache_busting` 은 **고정값**(`?v=3`)만 잡고 **빠진 것**은 안 잡는다.
  검사를 넓히면 한 번에 21건이 뜨므로, 고칠 결심과 함께 넓히는 게 맞다.

- **목록 줄이 키보드로 안 열린다.** `<tr onclick=…>` 이라 Tab 으로 닿지 않고
  Enter 로도 안 열린다. 상세를 여는 유일한 길이라 키보드만 쓰는 사용자는
  이 화면을 못 쓴다. `<tr tabindex="0" role="button">` + keydown 으로 열거나,
  제품명 칸을 실제 `<a href="?id=…">` 로 두면 된다(주소가 이미 그 모양이다).

- **모바일 앱**에는 아직 '받지 않기' 화면이 없다. 웹에서 끈 것은 앱 푸시·앱
  알림 탭에 그대로 반영되지만, 앱에서 새로 끄려면 웹으로 와야 한다.
  서버 쪽은 `GET/POST /regulatory/api/alert-mutes/` 로 이미 열려 있으므로,
  앱에 화면만 붙이면 된다 (앱 API 는 `/mobile/` 아래에 같은 모양으로 추가).
- **키워드 알림도 탭 배지에 넣을지** — 지금 배지의 모수(`actionable`)는 제품·원료
  매칭뿐이다. 키워드 매칭의 읽음 상태가 앱(`PushNotificationLog.is_read`)에만
  있어서, 넣으면 웹에서 지울 수 없는 숫자가 배지에 영구히 남는다.
  읽음 상태를 웹에서도 만질 수 있게 한 뒤에 다시 볼 일이다.
- **마이그레이션 그래프가 갈라져 있다** — 앱 전체를 도는 `manage.py migrate` 는
  `InconsistentMigrationHistory` 로 실패한다. 테스트도 그래서 `settings_test` 로
  마이그레이션을 건너뛰고 돈다(`v1/config/settings_test.py` 참고).
  이번에 넣은 `regulatory/0005_alertmute` 는 앱을 지정하면 깨끗이 돈다 —
  본서버에서도 `manage.py migrate regulatory` 로 넣는다.
  (CREATE TABLE 한 줄뿐이라 기존 표는 건드리지 않는다)
