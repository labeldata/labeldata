# 사진·문서 판독 고도화 — 현황과 다음 작업

> 최종 갱신 2026-09-03.
> **이 문서가 판독(OCR) 관련 유일한 기준이다.** 초기 설계안(`ocr_system_design.md`,
> 2025-12)은 채택되지 않았고, 무엇을 왜 버렸는지는 부록 A 에 남겼다.
>
> **지금 진행 중인 일은 §14 다.** OCR 원문을 판독에 섞는 작업으로, 1단계(가부
> 판정)는 통과했고 원재료명은 확실히 좋아졌다. 다만 아직 `OCR_HYBRID` 는 꺼져
> 있다 — 남은 문제와 다음에 할 일이 §14 끝에 있다.
>
> **§15 는 본서버 시험에서 나온 것**이다(2026-09-03). 정답지 점수로는 안 보이던
> 결함 하나(표시면 지시문이 읽을 항목을 상한선으로 만들고 있었다)와, §12 의
> "주의사항·기타표시사항을 읽지 않는다" 결정을 뒤집은 이유가 있다.

---

## 1. 지금 쓰는 방식 — OCR이 아니라 VLM 직접 추출

**전통적 OCR 엔진은 한 줄도 없다.** Tesseract·EasyOCR·Google Vision·Naver
Clova·AWS Textract — 코드베이스에 0건이다.

```
사진/PDF → 전처리(PIL·PyMuPDF) → base64 → GPT-4o mini (vision) → JSON
```

보통 이런 시스템은 두 단계다.

1. OCR 엔진이 글자와 좌표를 뽑고
2. 규칙이나 NLP가 "이 글자는 소비기한 칸" 이라고 붙인다

우리는 **그 두 단계를 모델 한 번 호출로 합쳤다.** 이미지를 넣으면 바로
`{"pog_daycnt": {"value": "...", "confidence": "high"}}` 가 나온다.

| 항목 | 우리가 하는 것 |
|---|---|
| 분류 | **VDU** (Visual Document Understanding) / **KIE** (Key Information Extraction) |
| 방식 | End-to-end, OCR-free |
| 모델 | GPT-4o mini vision (`settings.OCR_MODEL`) |
| 출력 | 스키마 강제 JSON (`response_format={"type":"json_object"}`) |

### 이 선택의 결과 (중요)

전통 OCR은 **글자를 못 읽는다.** VLM은 **값을 지어낸다.** 실패의 모양이 다르다.

그래서 프롬프트에 "지어내지 마시오 / 빈 값이 잘못된 값보다 낫다" 가 그렇게 크게
박혀 있고, 판독 결과를 **확인 창 없이 저장하는 경로를 두지 않는다.** 이 결과는
법적 표시물에 그대로 들어간다.

### 코드 위치

| 파일 | 하는 일 |
|---|---|
| `v1/label/services/ocr_service.py` | 표시사항 사진 판독 (프롬프트·타일링·모델 호출) |
| `v1/label/services/label_phrases.py` | 주의사항·기타표시사항 상용 문구 (화면 버튼 + 프롬프트 공용) |
| `v1/label/templatetags/quick_phrases.py` | 그 문구를 화면 버튼으로 뿌리는 태그 |
| `v1/label/services/ocr_snap.py` | 판독값을 표시기준 목록·상용 문구에 맞춤 |
| `v1/label/services/ocr_rawmtrl.py` | 원재료명 괄호 짝 검사 + 등록 정보 순서 맞추기 |
| `v1/label/services/ocr_boxes.py` | 읽은 자리 — 좌표 받기·되돌리기·겹침 채점 |
| `v1/static/js/label/ocr_box_editor.js` | 사진 위 번호 상자 편집기 (관리자 전용) |
| `v1/label/services/ocr_reconcile.py` | 식약처 등록 정보와 대조 |
| `v1/label/services/ocr_learning.py` | 교정 이력 축적 → 다음 판독 프롬프트로 되먹임 |
| `v1/label/services/ocr_benchmark.py` | 정답지 채점 규칙 |
| `v1/label/services/ocr_lab.py` | 정답지 기반 측정 실행 |
| `v1/label/services/ocr_prompt.py` | 프롬프트 판 관리 + 자동 초안 |
| `v1/products/services/vision_service.py` | 문서함 서류 판독 (품목제조보고서·COA) |
| `/label/ocr-lab/` | 관리자 고도화 화면 (staff 전용) — 사용법은 아래 9장 |
| 홈 `?import=1` | 홈의 "사진으로 시작하기" → 제품 생성 후 불러오기 창 자동 열기 |

---

## 2. 이미 들어가 있는 기법 — 용어 대응표

| 코드 | 정식 용어 |
|---|---|
| 2×2 오버랩 조각 전송 (`build_image_payload`) | **Image tiling / sliding window** |
| 전체 키 강제 JSON | **Constrained decoding** (schema-guided) |
| `confidence` + `candidates` | **Self-reported confidence + N-best hypotheses** |
| "빈 값이 잘못된 값보다 낫다" | **Abstention (IDK option)**, hallucination guardrail |
| `learned_hints()` 교정 이력 주입 | **Error-feedback prompting** (in-context learning) |
| `char_confusions()` | **Character confusion matrix** |
| `ocr_snap` 목록 스냅 | **Gazetteer / closed-vocabulary normalization** |
| `label_phrases` 상용 문구 주입·확정 | **Lexicon-constrained decoding + phrase-level gazetteer** |
| `ocr_rawmtrl.bracket_problems` | **Structural validity check** (구조 제약 위반 = 오독 신호) |
| `ocr_rawmtrl.align_with_api` | **Knowledge-grounded sequence alignment** (슬롯 정렬) |
| `ocr_boxes` 항목별 bbox | **Grounded KIE / visual grounding** (근거 위치 제시) |
| `ocr_boxes.score` IoU 채점 | **Localization eval** (detection 계열 지표) |
| 영역만 다시 읽기 | **Targeted re-ask** (지역 재질의) |
| PDF 텍스트 레이어 병행 | **Hybrid OCR-free + native text extraction** |
| `OcrCorrection` 축적 | **Human-in-the-loop 피드백 루프**, weak supervision 데이터셋 |
| `ocr_benchmark` 유사도 채점 | **Regression eval harness** (variance-aware) |
| `ocr_reconcile` 식약처 대조 | **Entity linking + knowledge-grounded verification** |
| `ocr_prompt` 자동 초안 | **Automatic Prompt Optimization** (APE/OPRO 계열) |

### few-shot 에 대해

용어는 **few-shot**(퓨샷)이다.

- **zero-shot** — 예시 없이 지시만. 초기 상태였다.
- **few-shot** — 정답 예시 몇 개를 프롬프트에 넣어 형식과 판단 기준을 보여준다.

`learned_hints()` 가 넣는 것은 정답 예시가 아니라 **오답 사례**다("이렇게 잘못
읽은 적이 3번 있다"). 엄밀히는 few-shot 이 아니라 error-feedback 이다. 진짜
few-shot(정답 예시 주입)은 아직 없고 **2순위**에 있다.

---

## 3. 데이터마이닝 관점 — 적용 현황

| 항목 | 상태 | 위치 |
|---|---|---|
| 문자 혼동 행렬 | ✅ **완료 (2026-08-31)** | `ocr_learning.char_confusions()` |
| 닫힌 집합 사전(gazetteer) | ✅ **완료 (2026-08-31)** — 식품유형·알레르기 | `ocr_snap.py` |
| 분리배출 마크 스냅 | ✅ 기존 | `ocr_apply.map_recycling_mark()` |
| 연관규칙(association rule) | ❌ 미적용 → **2순위** | — |
| 이상치 탐지 (판독 직후) | ⚠️ 열량 정합성만, 그것도 **저장 후 검증 단계** → **2순위** | `validation_service.check_calorie_consistency` |

---

## 4. 1순위 — 완료 (2026-08-31)

### ① PDF 텍스트 레이어 추출 — 가장 큰 효과

**문제.** `vision_service._pdf_to_base64_images()` 가 PDF를 2배 해상도로
**그림으로 렌더링해서** 모델에 보냈다. 그런데 인쇄용 라벨 도안과 품목제조보고서는
대개 문서 프로그램에서 뽑은 PDF 라 **글자가 그대로 박혀 있다.** 확실한 원문을
버리고 오독 가능성이 있는 경로로 돌아간 것이다. 코드베이스 전체에서 PyMuPDF 의
`get_text()` 를 부르는 곳이 한 군데도 없었다.

**한 일.**

- `extract_pdf_text(file_path, max_pages=2)` 신규 — 텍스트 레이어를 읽는다.
- 글자가 40자 미만이면 스캔본으로 보고 빈 문자열을 돌려준다. 도장·서명만 벡터로
  들어간 스캔 PDF 가 몇 글자를 흘리는데, 그 조각을 "원문" 이라고 넘기면 모델이
  그것을 믿고 나머지를 지어낸다.
- 원문이 있으면 프롬프트에 함께 실어 **"글자는 이 원문을 그대로 옮기고, 그림은
  어느 값이 어느 항목인지 판단하는 데만 쓰라"** 고 지시한다.
- 프롬프트에 싣는 원문 상한 12,000자 (`PDF_TEXT_MAX_CHARS`) — 표가 빽빽한
  문서에서 입력 토큰이 폭발하는 것을 막는다.
- `metadata['pdf_text_used']` 에 원문을 썼는지 남긴다. 남기지 않으면 "그림만
  봤을 때" 와 "원문을 함께 봤을 때" 의 정확도를 나눠 볼 수 없다.

**주의.** 스캔한 PDF 와 이미지 파일은 예전 그대로 동작한다. 회귀 없음.

### ② 닫힌 집합 스냅 (gazetteer)

**문제.** 식품유형은 「식품의 기준 및 규격」이 정한 목록이고, 알레르기는
표시기준이 정한 22종이다. 그런데 판독 결과를 자유 문구 그대로 받았다.

```
판독 "즉석섭취식품류"  ->  실제 유형 "즉석섭취식품"
판독 "대두류"          ->  알레르기 "대두"
```

이 값들은 뒤에서 **키로 쓰인다.** 식품유형은 유형별 표시항목 규칙을 찾는 키이고,
알레르기는 원재료에서 검출한 것과 대조하는 키다. 한 글자가 어긋나면 규칙을 못
찾고, 못 찾은 것을 화면은 "해당 규칙 없음" 으로 조용히 넘긴다.

**한 일.** `v1/label/services/ocr_snap.py` 신규.

- 판정은 **고친 글자 수(편집거리)** 로 한다. 비율(`fuzz.ratio`)만 쓰면 짧은
  이름에서 무너진다 — "대두류"→"대두" 는 한 글자 차이인데 세 글자 중 하나라
  80점밖에 안 나온다.
- 예산은 이름 길이에 따른다: 4자 이하 1글자, 10자 이하 2글자, 그 이상 3글자.
- **모호하면 손대지 않는다.** 같은 거리에 후보가 둘이면("과지류" ↔ 과자류/과채류)
  판독값을 그대로 둔다. 틀린 쪽으로 스냅하면 판독이 맞았을 때보다 나쁘다.
- 두 글자짜리 이름은 한 글자만 달라도 거절한다 — "빵류"를 "면류"로 맞추게 된다.
- 목록에 없는 알레르기 문구는 **지우지 않는다.** 22종 밖의 문구를 적어 두는
  라벨이 있고, 지우면 정보가 사라진다.
- 바꾼 값에는 `snapped_from` 을 남기고, 확인 창이 "사진에서는 ○○로 읽었고
  표시기준 목록에 맞춰 고쳤습니다" 를 보여 준다. **말없이 고치지 않는다.**

호출 순서는 `/label/ocr-extract/` 에서 **스냅 → 등록 정보 대조** 다. 표기를
목록에 맞춘 뒤에 대조해야 "같은 유형인데 표기만 다른 것" 이 불일치로 안 잡힌다.

### ③ 교정 이력 묶기 + 문자 혼동 행렬

**문제.** `build_hints()` 가 `(항목, 판독값, 고친값)` 세 값이 **완전히 같은**
교정만 2회 이상일 때 힌트로 썼다. 실제 오독은 매번 조금씩 다르다 — "송정동" 을
한 번은 "성정동", 한 번은 "송전동" 으로 읽으면 각각 1회라 영원히 힌트가 안 된다.
운영에서 힌트가 거의 안 붙던 이유가 이것이다.

**한 일.**

- **값 단위 묶기**: 같은 항목에서 **같은 정답으로 고쳐진 것**을 한 덩어리로 센다.
  판독값이 서로 달라도 "이 자리에서 이 값을 자꾸 틀린다" 는 같은 사실이다.
  덩어리 합이 2회 이상이면 힌트로 쓰고 판독값은 대표 두 개만 보여 준다.
- **글자 단위 (`char_confusions`)**: 바뀐 글자만 떼어 모은다. `Levenshtein.opcodes`
  로 **한 글자 치환**만 센다. 립/집, 송/성, 0/O, ㎖/㎗ 같은 것이 잡힌다.
  - 고친 자리가 4곳 이상이면 값을 통째로 갈아 끼운 것이라 제외한다 — 그런
    사례에서 글자 혼동을 캐면 우연히 겹친 글자쌍을 배운다.
  - 상한 12쌍. 너무 많으면 "이 글자를 저 글자로 바꿔라" 로 읽혀서 오히려 멀쩡한
    글자를 고친다.

두 종류는 겹치지 않는다. **값 단위는 같은 제품이 여러 번 올라와야 쌓이고, 글자
단위는 서로 다른 제품에서도 쌓인다** — 데이터가 적을 때 글자 단위가 먼저 힘을 낸다.

### 확인 방법

```bash
python manage.py ocr_accuracy                 # 항목별 정답률
python manage.py ocr_accuracy --hints         # 프롬프트에 실제로 붙는 힌트
python manage.py ocr_accuracy --by source     # 사진만 vs 등록 정보 대조
python manage.py ocr_accuracy --by variant    # 영역 선택 vs 사진 전체
```

`--hints` 로 값 단위 힌트와 글자 혼동이 둘 다 나오는지 본다. 아직 교정 이력이
얇으면 비어 있는 게 정상이다.

---

## 4-2. 1순위 2차 — 완료 (2026-08-31, 같은 날)

### ⑬ 주의사항·기타표시사항 상용 문구를 판독에 물림 (⚠️ 2026-09-01 되돌림)

> **프롬프트에 목록을 싣는 부분은 물렸다.** 모델이 그 목록의 문장을 지어냈다 —
> 라벨에 없는 보관 문구와 분쟁해결기준 문구가 통째로 들어갔다. §12 ⑨ 참고.
> 지금은 **판독 뒤 문장 확정(`ocr_snap`)과 화면 버튼**에만 쓴다.
> 그리고 이 두 칸은 아예 읽지 않는다(§12 결정).

**문제.** 주의사항과 기타표시사항은 **대부분 우리가 이미 만들어 둔 문구**다.
제품 화면 "자주 사용하는 문구" 버튼 28개가 그것이다. 그런데 그 목록이
`_tab_basic_info.html` 안에만 박혀 있어서, 판독은 그 존재를 몰랐다. 같은
문장을 매번 처음 보는 글자처럼 읽고 있었던 것이다.

**한 일.** `v1/label/services/label_phrases.py` 신규. 목록을 여기로 옮기고
**화면 버튼도 판독 프롬프트도 여기서 가져간다.**

- 화면은 `{% quick_phrases "cautions" %}` 템플릿 태그로 버튼을 그린다.
  목록을 두 벌로 두면 어느 날 한쪽만 고쳐지고, 그때 판독은 옛 문장을 기다린다.
- 프롬프트에 문장을 **통째로** 싣는다. 짧은 이름만 실으면 "그 문장" 을 알 수
  없어 글자를 확정하는 데 못 쓴다. 약 1,300자 (입력 토큰 ~800).
- 두 가지를 얻는다: ① 흐리게 찍혀도 문장을 끝까지 확정한다 ② **어느 칸인지**
  가른다 — 1399 는 주의사항, HACCP 인증은 기타표시사항이다.
- 판독 뒤에도 `ocr_snap` 이 줄·문장 단위로 상용 문구에 맞춘다(88점 이상).

**여기가 위험한 지점이다.** 흔한 문구를 프롬프트에 실으면 모델이 사진에 없는
것까지 채우려 든다 — 이 문서 §1이 말한 "VLM은 값을 지어낸다" 가 정확히 이
모양으로 나온다. 그래서 두 겹으로 막았다.

- 프롬프트에 못 박았다: **"사진에 없는 문구를 이 목록에서 가져와 채우지
  마시오. 이 목록은 읽은 글자를 확정하는 데만 쓴다."**
- 스냅은 **숫자가 완전히 같을 때만** 한다. 상담 번호는 회사마다 다르다 —
  "1577-9999" 를 목록의 "1577-1255" 로 맞추면 남의 회사 번호를 인쇄한다.
  글자만 보면 두 문장은 98점이라 그냥 통과한다.

### ⑭ 원재료명 — 괄호 짝 검사와 등록 정보 순서 맞추기

`v1/label/services/ocr_rawmtrl.py` 신규.

**괄호 짝 검사.** 모델이 대괄호와 소괄호를 혼동한다("정제소금(국산]").
그런데 괄호는 **열림과 닫힘이 반드시 짝**이고 순서가 있다 — 그 규칙이
깨졌다는 것은 그 자리를 잘못 읽었다는 **증거**다. 값을 하나하나 눈으로
대조하지 않고도 다시 볼 자리를 짚을 수 있다.

- 짝이 깨지면 확인 창에 자리(몇 번째 글자)와 무엇을 혼동했는지 띄우고
  확신도를 low 로 내린다.
- **고치지는 않는다.** 어느 쪽을 잘못 읽었는지는 사진을 봐야 알고, 임의로
  닫아 주면 틀린 자리를 덮어 버린다.
- 프롬프트에도 규칙을 넣었다 — 옮긴 뒤 괄호 수와 종류를 세어 보고, 안 맞으면
  짝을 지어내 맞추지 말고 low 로 두라고.

**등록 정보 순서 맞추기.** 식약처 등록 정보의 원재료는 **원산지와 복합원재료를
뺀 채 라벨과 같은 순서**로 적혀 있다. OCR 을 거치지 않았으니 이름과 순서가
틀릴 이유가 없다 — 이 제품 원재료의 **뼈대**다.

```
등록 정보  돼지고기, 정제소금, 백설탕
사진 판독  정재소금(국산), 돼지고가(국내산), 백설탕 2%
합친 결과  돼지고기(국내산), 정제소금(국산), 백설탕 2%
```

- **뼈대는 등록 정보에서, 살(원산지·복합원재료·함량)은 사진에서** 가져온다.
- 이름 대조는 **고친 글자 수**로 한다. 비율만 쓰면 무너진다 — "돼지고기" →
  "돼지고가" 는 한 글자 차이인데 네 글자 중 하나라 75점이다. 원재료 이름은
  대개 서너 글자라 이 구간이 그대로 급소다(`ocr_snap` 과 같은 방식).
- 쉼표로 가를 때 **괄호 안의 쉼표는 세지 않는다.** 안 그러면 복합원재료가
  통째로 부서진다.
- 등록 정보의 원재료 중 사진에서 찾은 것이 **60% 미만이면 손대지 않는다.**
  다른 제품이거나 판독이 무너진 것이고, 그때 합치면 사진에 없는 원재료를
  인쇄하게 된다.
- 사진에만 있는 원재료는 **버리지 않고** 뒤에 붙인다. 등록 정보가 오래됐을 수
  있다.
- 바꾼 값에는 `snapped_from` 과 설명을 남긴다. **말없이 고치지 않는다.**

지금까지 원재료명은 'loose' 판정이라 대조해도 아무 일도 일어나지 않았다 —
표기가 달라 점수가 낮은 게 정상이라 늘 'unsure' 로 끝났다. 이제 대조가 실제로
값을 만든다.

### ⑮ `/label/ocr-lab/` — 화면 안 사용법과 사진 확대

**문제.** 사용법이 이 문서 §9 에만 있었다. 화면을 보면서 문서를 따로 여는
사람은 없다. 그리고 정답지를 만들 때 사진이 폭에 맞춘 `<img>` 한 장이라
원재료명 한 줄이 읽히지 않았다 — **정답을 적으려면 사진이 읽혀야 한다.**

**한 일.**

- 카드 넷(①정답지 ②측정 ③프롬프트 ④실사용)마다 접이식 **"사용법"** 을
  붙였다. 칸·옵션·버튼마다 무엇이 일어나는지, 결과를 어떻게 읽는지.
  접어 두었다 — 늘 펼쳐 두면 자주 쓰는 사람에게는 방해다.
- 맨 위에 **"이 화면은 무엇을 하는 곳인가"** — 권장 순서와, 이 방식이 AI
  개발의 어느 단계인지(아래 §10).
- 정답지 편집 창이 제품 화면 확인 창과 **같은 사진 뷰어**를 쓴다
  (`photo_viewer.js` — 회전 · 확대(버튼·휠) · 끌어서 이동 · 더블클릭 원복).
  정답을 적는 눈과 판독값을 보는 눈이 같은 도구여야 한다.

### ⑯ 읽은 자리를 번호로 표시하고, 고쳐서 다시 읽힌다 — 관리자 화면

> 검토 내용은 §5 ⑯ 에 있다. **1~3단계를 넣었고 4단계(사용자 화면)는 넣지
> 않았다** — 좌표가 믿을 만한지 아직 재지 않았기 때문이다.

**응답에 위치를 더했다.** `v1/label/services/ocr_boxes.py` 신규.

- 항목마다 `img`(몇 번째 이미지)와 `bbox`(그 이미지 안에서 0~1000 정규화)를
  받는다. 픽셀로 달라고 하면 모델이 원본 크기를 알아야 하는데 우리는 보내기
  전에 리사이즈하므로 모델이 보는 크기와 원본이 다르다.
- 조각 좌표를 **원본 픽셀로 되돌린다.** 조각은 우리가 잘랐으니 그 자리는
  확실하다 — 틀릴 여지는 모델이 준 상자 쪽에만 있다.
  (`build_image_regions()` 가 이미지와 함께 잘린 자리를 들고 다닌다.)
- **기본은 꺼져 있다** (`want_boxes=False`). 좌표를 요구하면 항목마다 숫자
  네 개를 더 뱉게 되고 그만큼 값에 쓸 주의가 갈린다. 평소 판독 경로는 그대로다.
- 못 잡으면 `null` 이다. 없는 좌표를 지어내면 **맞는 값에 틀린 상자**가 붙고,
  사람이 멀쩡한 값을 의심해 지운다 — 지금 없는 불신을 만드는 것이다.

**화면.** 정답지 편집 창에 "위치 보정" 모드가 붙었다
(`ocr_box_editor.js` 신규).

- **판독 위치**(파란 점선)와 **정답 위치**(초록 실선)를 색과 선으로 가른다.
  파란 것은 모델이 말한 자리라 끌어서 고칠 수 없다 — 고치면 "모델이 뭐라고
  했는지" 가 사라진다.
- 상자를 끌어 옮기고, 모서리로 크기를 바꾸고, 빈 곳을 끌어 새로 그린다.
  상자는 **퍼센트로** 얹어서 확대·축소를 해도 따라간다.
- **"이 영역만 다시 읽기"** — 고친 자리만 잘라 다시 읽힌다. §5 ⑤ 표적 재질의가
  여기서 완성된다. 평소 판독 경로를 그대로 쓰고(잘라 낸 조각을 같은 함수에
  넣는다) 여백을 12% 붙인다 — 딱 잘라 보내면 항목명이 잘려 무슨 항목인지
  모른다.
- 다시 읽은 값은 **정답 칸에 자동으로 들어가지 않는다.** 그것도 판독값이라
  틀릴 수 있는데 여기 들어가는 값은 채점의 잣대가 된다.

**측정.** `OcrTruthCase.expected_boxes` 신규(마이그레이션 `common/0009`).
②에 "읽은 위치도 재기" 를 켜면 정답 위치와의 겹침(IoU)을 잰다.

- 정답 위치를 적어 둔 항목만 센다. 안 적어 둔 항목은 "위치를 모른다" 는
  뜻이지 "틀렸다" 가 아니다.
- **값 점수와 위치 점수를 따로 낸다.** 한 숫자로 합치면 "값이 나빠지고 위치가
  좋아진" 경우를 알 수 없는데, 그게 정확히 우리가 봐야 하는 것이다.
- 같은 자리라고 볼 겹침은 30% 다. 여기서 재는 것은 "정확히 같은 상자인가" 가
  아니라 **"사람이 그 자리를 보러 갈 수 있는가"** 다.

**다음 판단.** 이 화면으로 **① 좌표가 맞는가 ② 좌표를 물어봤을 때 값이
내려가는가** 를 먼저 재야 한다. ②가 눈에 띄면 거기서 멈춘다 — 위치는 편의고
값이 본질이다.

### ⑰ 분리배출 표시 두 건 (운영에서 발견)

**PE 에 기타플라스틱을 찍으면 검증이 울었다.** 호환표에 `PE` 가 아예 없었다.
PE 는 분리배출 표시가 정한 일곱 재질(PET/HDPE/LDPE/PP/PS/PVC/OTHER)에 없는
표기라, 라벨에 "PE" 라고만 적힌 것은 고밀도인지 저밀도인지 가려지지 않은
것이다. HDPE·LDPE·기타 어느 쪽으로 표시해도 어긋났다고 볼 수 없다.

같은 자리에서 **반대 방향 결함**도 나왔다. 재질 대조가 그냥 문자열 포함이라
`"PET(용기)"` 안에 `"PE"` 가 들어 있었다 — PET 용기에 LDPE 마크를 찍어도
통과했다. 영문 코드는 **낱말 단위로** 견주도록 고쳤다(`_material_has`).
한글은 그대로 포함으로 본다 — "폴리에틸렌수지" 처럼 붙여 쓰는 표기가 흔하다.

**마크를 그려 놓고 그 옆에 읽은 문구를 통째로 또 적었다.**
`prv_recycling_mark_text` 는 미리보기에서 마크 옆에 인쇄되는 글자인데,
판독 결과를 그대로 넣고 있었다. "비닐류 PP" 를 읽으면 비닐(PP) 마크를 그리고
그 옆에 "비닐류 PP" 를 또 썼다.

- 종류를 정했으면 마크가 재질을 말한다. 덧붙일 것은 **마크 하나로 표현할 수
  없는 나머지**뿐이다 — "비닐류 PP / 띠지:PP, 리드지:PET" 의 "/" 뒤쪽.
- 종류를 못 정했으면 읽은 문구를 그대로 남긴다. 마크를 못 그리므로 사람이
  무엇을 봤는지 알아야 직접 고를 수 있다.
- 덧붙일 것이 없으면 **기존 문구를 건드리지 않는다.** 사진 한 장 읽었다고
  사람이 맞춰 둔 표시가 사라지면 안 된다.

### ⑱ 확인 창에서 현재 값이 한 줄로 잘리던 문제

이 표가 있는 이유가 두 값을 견주는 것인데, 현재 값 칸이 한 줄 말줄임이라
**정작 견주기 어려운 긴 항목**(원재료명·주의사항)이 통째로 잘렸다.

- 줄바꿈을 살려(`pre-wrap`) 끝까지 보여 준다 — 주의사항은 여러 줄이 한 값이다.
- 칸 너비를 읽은 값 쪽과 비슷하게 맞췄다(0.8fr → 1fr).
- 320px 에서 멈추고 그 안에서 스크롤한다. 900자짜리 원재료명 하나가 화면을
  통째로 밀어내지 않게 — 옆의 입력칸도 12줄에서 멈추므로 두 칸 높이가 비슷하다.


---

## 5. 2순위 — 다음 작업 (선행조건 있음)

> **"나중에" 가 아니라 "선행조건이 있다".** 아래는 전부 비용이 배수가 되거나
> 데이터가 쌓여야 의미가 생긴다. `/label/ocr-lab/` 으로 **먼저 재고** 걸 것.

### ④ Self-consistency (앙상블)

같은 사진을 2~3회 읽어 필드별 다수결. 비용이 2~3배.

- **선행조건**: 측정 화면에서 "들쭉날쭉"(편차 30점 이상)으로 뜨는 항목이 무엇인지
  확인. 그 항목에만 걸면 비용이 전체 배수가 아니라 일부에만 붙는다.
- 구현 위치: `ocr_service.extract_label_from_image` 을 N회 부르고 필드별로
  최빈값 선택. `ocr_lab.measure_case` 가 이미 N회 호출 구조를 갖고 있다.

### ⑤ 표적 재질의 (targeted re-ask)

`confidence: low` 인 항목만, 그 영역을 크롭해 다시 묻기. 전체 재판독보다 훨씬 싸다.

- **선행조건**: 영역 좌표를 알아야 한다. 지금 프롬프트는 값만 받고 **위치를 안
  받는다.** 응답 스키마에 `bbox` 를 추가하거나, 조각(타일) 단위로 되묻는 방식 중
  하나를 골라야 한다. 스키마 변경은 `ocr_lab` 채점과 확인 창을 함께 손봐야 한다.
- **⑯ 과 같은 뿌리다.** 위치를 받기 시작하면 둘이 동시에 열린다 — 따로 하지 말 것.

### ⑯ 읽은 자리를 사진 위에 번호로 표시 — 1~3단계 완료, 4단계 남음

> ✅ **1~3단계는 §4-2 ⑯ 으로 들어갔다** (2026-08-31). 아래는 그때의 검토
> 근거이고, **4단계(사용자 화면)는 아직 하지 않았다** — 좌표가 믿을 만한지
> 재기 전에는 넣지 않는다.
>
> 2026-08-31 검토. **⑤ 표적 재질의와 같은 뿌리다** — 둘 다 "응답에 위치를
> 받는다"는 한 가지 스키마 변경 위에 선다. 따로 하지 말 것.

**제안 내용.** 판독 결과의 각 항목이 사진의 **어디에서** 나왔는지 번호 배지로
겹쳐 보여 준다. 값이 틀렸으면 사용자가 그 자리를 고쳐 다시 읽힌다.

**왜 좋은 생각인가.** 지금 사용자가 아는 것은 "이 값이 틀렸다"뿐이고, 왜
틀렸는지는 모른다. 위치를 보여 주면 **틀린 이유가 보인다** — 옆 칸을 읽었는지,
작업지시서의 표를 읽었는지, 아예 못 찾았는지. 그리고 이건 `variant=crop`
통계가 이미 말하고 있는 것(영역을 고르면 나아진다)을 **항목 단위로** 내리는
일이다.

지금도 사용자는 판독 **전에** 영역을 고를 수 있다(`import_modal.js` →
`cropPhoto`). 없는 것은 **되돌아오는 쪽** — 모델이 어디를 봤는지다.

#### 먼저 답해야 하는 것: 좌표를 믿을 수 있는가

**이게 전부다.** GPT-4o 계열은 값은 잘 읽어도 **픽셀 좌표는 자주 어긋난다.**
그러면 새 실패 모양이 생긴다.

- **맞는 값에 틀린 상자**를 그리면, 지금은 없는 불신이 생긴다 — "엉뚱한 데를
  가리키니 값도 틀렸겠지" 하고 멀쩡한 값을 지운다.
- 좌표를 요구하면 **값 정확도가 내려갈 수 있다.** 항목 29개에 숫자 네 개씩
  더 뱉게 하는 것이고, 그만큼 값에 쓸 주의가 갈린다. 이건 재 봐야 안다.
- 우리는 조각 5장을 보낸다. 좌표는 **어느 이미지 기준인지**까지 받아 원본
  좌표로 되돌려야 한다. 조각의 잘린 위치를 우리가 알고 있으니 계산은 되지만,
  틀릴 자리가 하나 더 생긴다.

**지킬 것.** 좌표를 못 잡으면 `null` 로 둔다. 없는 좌표를 지어내면 사용자를
엉뚱한 데로 보낸다 — 값 지어내기와 같은 병이다. 그리고 **좌표가 틀렸다고 값을
버리지 않는다.** 값과 좌표는 따로 판정한다.

#### 사용자 화면인가, 관리자 화면인가 — 관리자 화면 먼저

| | 사용자 확인 창 | 관리자 `/label/ocr-lab/` |
|---|---|---|
| 목적 | 라벨을 **만드는** 것 | 판독을 **고치는** 것 |
| 틀린 값을 만났을 때 | 그 자리에서 고쳐 쓰면 끝 | 왜 틀렸는지 알아야 한다 |
| 좌표가 틀렸을 때 | **불신이 생긴다** | 그것 자체가 측정 대상이다 |
| 재판독 비용 | 누를 때마다 과금, 상한 없음 | 이미 회차 상한(12회)이 있다 |
| 정답이 있는가 | 없다 | **있다** — 정답지 |

좌표가 맞는지 **재려면 정답이 필요하고, 정답은 관리자 화면에만 있다.** 맞는지도
모르는 표시를 법적 표시물 작업 화면에 먼저 띄우는 것은 순서가 뒤집힌 것이다.

그리고 사용자 쪽 이득이 생각보다 작다. 확인 창은 이미 **값을 눈으로 보고 고치는**
자리다. 한두 항목이 틀렸을 때는 영역을 다시 잡아 재판독을 기다리는 것보다
그 자리에서 타이핑하는 쪽이 빠르다.

**다만 예외가 분명하다 — 원재료명과 주의사항.** 이 둘은 한 줄이 200~300자라
다시 타이핑하는 값이 크게 비싸다. 여기서는 "이 영역만 다시 읽기"가 실제로
이긴다. 사용자 화면에 넣는다면 **전 항목이 아니라 이 긴 항목들만**이다.

#### 넣는 순서

1. **스키마에 위치를 더한다** — 항목마다 `bbox: [x, y, w, h]` 와 `image`(전체/조각
   번호). 못 잡으면 `null`. 조각 좌표를 원본 좌표로 되돌리는 것은 서버가 한다.
2. **관리자 화면에서 좌표 자체를 잰다** — 정답지 편집 창의 사진 위에 항목 번호
   배지를 겹쳐 그리고, 사람이 O/X 한다. 동시에 **값 정확도가 내려갔는지**를
   기존 프롬프트와 A/B 로 잰다(②의 "프롬프트 판" 이 이걸 하라고 있는 것이다).
   → 값이 눈에 띄게 내려가면 **여기서 멈춘다.** 위치는 편의고 값은 본질이다.
3. **관리자 화면에서 영역을 고쳐 다시 읽는다** — 배지를 끌어 옮기고 "이 영역만
   다시 읽기". 이 순간 ⑤ 표적 재질의가 완성된다.
4. **그다음에야 사용자 화면** — 긴 항목 한정으로 "여기서 읽었습니다" 배지와
   "영역을 고쳐 다시 읽기". 한 번 판독에 재질의 **횟수 상한**을 반드시 건다.

#### 곁들여 열리는 것

위치를 받기 시작하면 한 가지 UI 기능이 아니라 **여러 개가 같이 열린다.**

- **⑤ 표적 재질의** — `confidence: low` 인 항목만 그 영역을 잘라 되묻는다.
- **§11 ③(나) 조건부 조각** — 조각을 언제 붙여야 하는지를 좌표가 말해 준다.
  비용 절감의 핵심 선행조건이 이것이다.
- **⑪ 능동학습** — 좌표가 흔들리는 사진이 곧 모델이 헤매는 사진이다.

그래서 이건 "번호를 표시하는 일" 이 아니라 **스키마 확장**이다. 하려면 ⑤와
묶어서 한 번에 할 것.

### ⑥ Retrieval-based few-shot

식품유형에 맞는 정답 예시만 골라 프롬프트에 주입. 지금은 전역 상위 8개 고정이다.

- **선행조건**: `OcrTruthCase` 가 식품유형별로 몇 장씩 쌓여야 한다.
- 멀티모달 few-shot(사진+정답 쌍)은 토큰 비용이 크다. **텍스트 예시만**
  (판독 원문 → 구조화 JSON) 쓰는 쪽이 실용적이다.

### ⑦ 연관규칙 (association rule) — 데이터마이닝

"식품유형=즉석섭취식품 → 소비기한 5일 이내" 같은 규칙을 `FoodItem` 9천여 건에서
캐서 판독 결과의 **개연성 검사**로 쓴다.

- 지금 `ocr_reconcile` 은 **품목보고번호가 읽혀야만** 작동한다. 연관규칙은
  번호가 없어도 작동한다 — 그게 이 항목의 존재 이유다.
- 구현 스케치: `FoodItem` 을 `prdlst_dcnm` 으로 묶어 `pog_daycnt` 분포를 뽑고,
  판독값이 그 분포에서 크게 벗어나면 확인 창에 "이 유형에서는 드문 값" 표시.
- **주의**: 규칙을 검증(validation)에 넣지 말 것. 통계적 드묾은 위법이 아니다.
  판독 확인 창의 참고 표시로만 쓴다.

### ⑧ 이상치 탐지 (판독 직후)

열량÷내용량 비, 나트륨 자릿수, 배합비 합계를 **판독 직후** 걸러낸다.

- 지금은 `check_calorie_consistency` 가 **저장 후 검증 단계**에서만 본다.
  판독 확인 창에서 미리 짚어주면 사용자가 그 자리에서 고친다.
- 재사용할 것: `validation_service._total_amount`, `_KCAL_RE`, `normalize_units`.

---

## 6. 3순위 — 데이터가 더 쌓인 뒤

### ⑨ 파인튜닝

`OcrCorrection` 이 수백~수천 쌍 쌓이면 vision fine-tuning. 지금 스키마
(`ocr_value` / `final_value` / `field` / `confidence` / `model` / `variant` /
`source`)가 이미 학습셋 모양이라 그대로 쓸 수 있다.

**순서상 마지막이다.** 프롬프트·앙상블·사전 스냅으로 짜낼 게 아직 많고,
파인튜닝은 되돌리기 어렵고 모델 버전에 묶인다.

### ⑩ LLM-as-judge 채점

지금 `ocr_benchmark` 의 rapidfuzz 유사도는 "쉬레드치즈/쉐르드치즈"(경미한 오독)와
"통째로 지어낸 값"을 가르려고 만든 것이다. 그래서 **원재료명처럼 순서만 바뀐
경우**를 틀렸다고 센다. 의미 단위 채점이 필요하다.

- 도입하면 `ocr_benchmark.score_one` 만 갈아 끼우면 된다 — 파일 기반 벤치마크와
  화면 기반 측정이 **같은 모듈**을 쓰도록 이미 맞춰 뒀다.

### ⑪ 능동학습 (active learning)

정답지를 아무거나 쌓지 말고, 모델이 가장 헷갈려한 사진(편차 큰 것)을 우선
정답지로 올리라고 `/label/ocr-lab/` 이 추천.

### ⑫ 프로그램 최적화 (DSPy 류)

프롬프트를 손이 아니라 측정으로 자동 탐색. `ocr_prompt.suggest_revision` 이
초보적으로 하는 것의 정식 버전이다.

---

## 7. 작업 중 발견한 결함 (미수정)

### `document_ai_extract_api` 가 항상 500

- 위치: `v1/products/views.py:5466`
- 내용: `from v1.products.services.vision_service import VisionAIService` —
  **`VisionAIService` 클래스가 존재하지 않는다.** 코드베이스 어디에도 없다.
  함수 맨 위의 import 라 호출 즉시 `ImportError` 로 500 이 난다.
- 라우팅됨: `products/urls.py:129` `documents/<id>/ai-review/extract/`
- 현재 이 URL 을 부르는 화면·JS 는 없다. 그래서 드러나지 않았다.
- 판단: 이번 작업 범위 밖이라 손대지 않았다. 고치려면 `process_document_vision`
  을 쓰도록 바꾸거나, 쓰지 않는 엔드포인트면 URL 과 함께 지워야 한다.

---

## 8. 배포 메모

- **마이그레이션 필요**: `python manage.py migrate common`
  (`0008_ocrcorrection_source_ocrpromptversion_and_more` — 판독 교정 이력에
  `source` 추가, 정답지·프롬프트 판·측정 기록 3개 모델 신설)
- 정적 파일: `ocr_lab.js` 신규, `basic_info_ocr.js` / `products_common.css` 변경.
  `STATIC_BUILD_DATE` 가 서버 재시작마다 갱신되므로 배포하면 자동으로 갈린다.
- **읽은 자리 배포분**: `python manage.py migrate common`
  (`0009_ocrtruthcase_expected_boxes` — 정답지에 항목별 정답 위치).
  정적 파일 `js/label/ocr_box_editor.js` 신규.
- **4-2 차 배포분 (마이그레이션 없음).** 새 파일 `label_phrases.py`,
  `ocr_rawmtrl.py`, `v1/label/templatetags/quick_phrases.py`(새 패키지 —
  `__init__.py` 가 함께 올라가야 템플릿 태그를 못 찾아 `_tab_basic_info.html` 이
  통째로 500 이 난다). `ocr_lab.html` 이 `js/products/photo_viewer.js` 를 함께
  싣는다.
- 새 설정 없음. `OCR_REVISION_MODEL` 을 안 넣으면 자동 초안은 `gpt-4o` 를 쓴다.
- **로컬 venv 를 requirements.txt 에 맞춰 재설치했다 (2026-08-31).** 그 전에는
  Django 5.1.6 / pillow 11.1.0 / cryptography 44.0.2 가 깔려 있어서 pin 과
  어긋나 있었다. 재설치 뒤 Django 5.2.17 에서 전체 343건 통과를 확인했다.
- **남은 보안 건**: `sqlparse==0.5.4` 에 GitHub 권고 4건(high 3, moderate 1).
  전부 `0.6.0` 에서 수정되고 Django 는 `sqlparse>=0.3.1` 만 요구하므로 올려도
  된다. 다만 우리 코드는 sqlparse 를 직접 쓰지 않고 Django 내부 호출 경로
  (RunSQL 마이그레이션, MySQL CHECK 제약 파싱, 테스트 러너)에 **공격자가 넣는
  입력이 닿지 않아** 실질 노출은 없다. 판단은 보류 중.
- `ocr_snap` 의 식품유형 사전은 6시간 캐시다. `FoodType` 을 갱신했는데 스냅이
  옛 목록을 쓰면 캐시 키 `ocr_snap:food_types` 를 지운다.

---

## 9. 관리자 화면 사용법 — 판독 테스트와 학습

**주소** `/label/ocr-lab/` · 좌측 사이드바 맨 아래 **판독 고도화**
(staff 계정에만 메뉴가 보이고, 화면 자체도 staff 전용이다)

> 2026-08-31 부터 **같은 내용이 화면 안에 있다.** 카드마다 "사용법" 을 펴면
> 칸·옵션·버튼별 설명이 나온다. 아래는 그 원본이자, 화면 밖에서 읽을 때의
> 참고다. 둘이 어긋나면 화면 쪽을 고칠 것 — 사람은 문서를 안 읽는다.

### 학습에는 두 가지가 있다

| | 자동 학습 | 수동 고도화 |
|---|---|---|
| 무엇이 | 사용자가 확인 창에서 고친 값 | 정답지로 잰 점수 |
| 누가 | 사용자가 평소 쓰기만 하면 쌓인다 | 관리자가 화면에서 돌린다 |
| 어떻게 반영 | 30분 뒤 다음 판독 프롬프트에 힌트로 자동 | 프롬프트 판을 만들어 **사람이 켜야** |
| 관리자 할 일 | 없음 | 아래 ①~③ |

**자동 학습은 이미 돌고 있다.** 제품 화면에서 사진을 읽고 "선택 항목 채우기" 를
누르면 그때마다 (판독값, 사용자가 쓴 값) 쌍이 쌓인다. 같은 실수가 2번 이상
반복되면 다음 판독부터 프롬프트에 경고로 붙는다. 관리자가 켜고 끌 것이 없다.

아래는 그것만으로 부족할 때 쓰는 **수동 고도화** 절차다.

### ① 정답지 만들기 (첫 단계, 여기가 제일 중요하다)

정답지가 없으면 아무것도 못 잰다. 두 가지 길이 있다.

**(가) 사진으로 만들기** — 새 사진으로 시작할 때

1. 사진 고르기 → 이름 입력 → (있으면) 품목보고번호 입력
2. **"판독해서 초안을 채우기"** 를 켠 채로 "정답지 만들기"
   - 5~15초 뒤 판독 결과가 초안으로 채워지고 편집 창이 열린다
3. **사진을 확대해 가며 틀린 값을 손으로 고친다**
   - 편집 창의 사진은 제품 화면 확인 창과 같은 뷰어다: 회전, 확대(버튼·휠),
     확대한 뒤 끌어서 이동, 더블클릭으로 원래대로
   - 표시사항 글씨는 작다. **확대하지 않고 적은 정답은 정답이 아니다**
4. **"정답 확인"** 체크를 켜고 저장

> ⚠️ **3번을 건너뛰면 안 된다.** 초안은 판독 결과다. 그대로 확인 처리하면
> 자기 답을 자기가 채점하는 꼴이고, 점수가 100점 가까이 나오면서 아무 뜻도
> 갖지 않는다.

**(나) 검증된 표시사항에서 가져오기** — 이쪽이 더 정확하고 빠르다

1. 이미 검토·승인까지 마친 제품의 **표시사항(제품) 번호**를 입력
   (제품 화면 주소 `/products/123/` 의 `123`)
2. 그 제품의 표시사항 **사진**을 함께 올림
3. 값이 자동으로 채워진다 — 판독값이 아니라 **실제로 인쇄에 쓰인 값**이다
4. 사진이 그 제품의 것이 맞는지만 확인하고 **"정답 확인"** 켜기

**정답 편집 창에서 할 수 있는 것**

- 라벨에 **없는 항목은 비워 둔다** — 채점에서 자동으로 빠진다
- **읽을 영역** `x, y, 너비, 높이` (원본 픽셀)를 적으면 "영역만 읽기" 방식도
  함께 잴 수 있다. 안 적으면 사진 전체로만 잰다
- **품목보고번호**를 적어 두면 등록 정보 대조의 효과까지 함께 잴 수 있다

**몇 장이 필요한가.** 5~10장이면 방향은 보인다. 다만 **식품유형을 섞어야
한다** — 같은 유형 열 장보다 다른 유형 다섯 장이 낫다. 잘 틀리는 사진
(빽빽한 원형 라벨, 작업지시서에 얹힌 라벨)을 일부러 넣을 것.

### ② 재기

목록에서 잴 정답지를 체크하고 옵션을 고른 뒤 **"고른 정답지로 재기"**.

| 옵션 | 언제 쓰나 |
|---|---|
| 프롬프트 판 | 비우면 지금 켜져 있는 판. 판끼리 견줄 때 지정 |
| 모델 | 비우면 설정값(`OCR_MODEL`). `gpt-4o` 등을 넣어 비교 |
| **회차** | **3 이상 권장** |
| 영역만 읽기 | 정답지에 영역을 적어 둔 것만 의미 있음 |
| 등록 정보 대조 | 켜면 대조 전/후 점수를 함께 낸다 |
| 교정 이력 힌트 | 끄면 프롬프트 자체의 힘만 잰다 |

> ⚠️ **한 번 돌린 결과로 판단하지 말 것.** 같은 사진도 매번 다르게 읽힌다.
> 회차 1로 재면 운이 좋았는지 나빴는지 알 수 없다.

> ⚠️ **한 번에 12회까지.** (정답지 수 × 회차) 이 값을 넘으면 거절한다.
> 사진 한 장에 5~15초라 그 이상은 웹 요청이 먼저 끊기고, 끊기면 **돈만 쓰고
> 결과가 안 남는다.** 정답지 10장을 3회 재려면 5장씩 두 번에 나눠 돌린다.

**결과 읽는 법**

- **평균** — 전체 점수. 85점 이상이면 쓸 만하다
- **편차** — 최고와 최저의 차. **30점 이상이면 "들쭉날쭉"** 으로 표시된다.
  평균 60점이어도 편차가 크면 늘 55점인 항목보다 **더 급하다** — 같은 사진을
  매번 다르게 읽는다는 뜻이라 사용자가 결과를 믿을 수 없다
- **"약함"(빨강)** — 평균 60점 미만
- 등록 정보 대조를 켰으면 파란 줄에 **"대조하면 +N점"** 이 나온다
- **"자세히"** 를 누르면 사진별로 무엇을 어떻게 틀렸는지(정답 vs 판독) 나온다

### ③ 프롬프트 고치기

**(가) 자동 초안** — 측정 결과에서 시작

1. 측정 결과의 **"이 결과로 프롬프트 초안 만들기"**
2. **무엇을 근거로 고치려는지 먼저 보여 준다** — 약한 항목과 실제 오독 사례.
   이걸 읽어야 나중에 초안을 검토할 기준이 생긴다
3. "초안 만들기" → 꺼진 채로 저장된다
4. ③의 판 목록에서 **열어서 전문을 끝까지 읽는다**
5. 그 판으로 다시 재 본다 (②에서 "프롬프트 판" 에 지정)
6. 점수가 올랐으면 **"사용"** 으로 켠다

**(나) 직접 고치기**

- **"기본 프롬프트 보기"** → 전문을 보고 → "이 내용으로 새 판 만들기"
- 판을 열어 고치고 저장 → 재 보고 → "사용"

> ⚠️ **"사용" 을 누르는 순간 모든 사용자의 판독에 쓰인다.** 판독 결과는 법적
> 표시물에 그대로 들어간다. 재 보지 않은 판은 켜지 말 것.

> 되돌리려면 **"기본 프롬프트로 되돌리기"** — 코드에 박힌 원래 프롬프트로
> 즉시 돌아간다. 판을 지울 필요 없다.

> 응답 형식(맨 끝 JSON 키 목록)을 지운 프롬프트는 **저장을 거부한다.**
> 그 키가 없으면 판독 결과를 화면이 못 읽는데, 켠 뒤에 알게 되면 그 사이의
> 판독이 전부 헛돈다.

### ④ 실사용 통계 (화면 맨 아래)

정답지 채점과 **다른 각도**다. 정답지는 우리가 고른 사진이고, 이쪽은 사용자가
실제로 올린 사진이다. "확인 창에서 고치지 않고 그대로 쓴 비율" 을 센다.

- **항목별** — 어느 칸을 자주 고치는가
- **값의 출처별** — 사진만(photo) / 등록 정보로 채움(api) / 둘이 일치(both) /
  둘이 다름(conflict). **등록 정보 대조가 실제로 도움이 되는지**가 여기서 보인다
- **판독 방식별** — 영역 선택(crop)이 사진 전체(whole)보다 나은가
- **모델별** — 모델을 바꿔 본 효과

### 권장 순서 (처음 쓸 때)

```
1. 검증된 표시사항 3~5건으로 정답지 만들기 (② 나 방식)  — 30분
2. 회차 3으로 재기 (기본 프롬프트, 등록 정보 대조 켜기)   — 5분
3. 약한 항목 / 들쭉날쭉 항목 확인                          — 10분
4. 자동 초안 만들고 전문 검토                              — 20분
5. 그 판으로 다시 재기 → 올랐으면 켜기                     — 5분
6. 2주 뒤 ④ 실사용 통계로 실제 효과 확인
```

### 명령줄로도 된다

화면 없이 서버에서 볼 때.

```bash
python manage.py check_ocr                   # 판독 설정·연결 점검
python manage.py ocr_accuracy                # 항목별 실사용 정답률
python manage.py ocr_accuracy --hints        # 프롬프트에 실제로 붙는 힌트
python manage.py ocr_accuracy --by source    # 등록 정보 대조 효과
python manage.py ocr_accuracy --by variant   # 영역 선택 vs 사진 전체
python manage.py ocr_accuracy --samples pog_daycnt   # 그 항목이 어떻게 틀렸나

# 파일로 재기 (서버에 사진을 올려 둘 수 있을 때)
python manage.py ocr_benchmark --make-answer ~/bench/샐러드.jpg
python manage.py ocr_benchmark --dir ~/bench --runs 5 --details
```

### 자주 걸리는 것

| 증상 | 원인 |
|---|---|
| "정답 확인된 정답지가 없습니다" | 정답지를 만들고 **"정답 확인"** 을 안 켰다 |
| "한 번에 N번을 읽게 됩니다" | 정답지 수 × 회차 > 12. 나눠 돌릴 것 |
| 점수가 비정상적으로 높다 | 판독 초안을 안 고치고 확인 처리했다 |
| "고칠 곳을 찾지 못했습니다" | 항목 점수가 이미 충분히 높다 (평균 90↑, 편차 25↓) |
| 힌트가 안 붙는다 | 같은 실수가 2번 이상 반복돼야 한다. 초기에는 정상 |
| 화면이 500 | `python manage.py migrate common` 을 안 했다 |

---

## 10. 이 방식은 AI 개발의 어느 단계인가

`/label/ocr-lab/` 이 하는 일은 **모델을 학습시키는 것이 아니다.** 모델
가중치는 손대지 않고, 지시문(프롬프트)만 바꿔 가며 **점수로 고른다.** 정식
이름은 **평가 주도 개발(eval-driven development)** 이고, 파인튜닝의 앞 단계다.

| 화면 | 정식 용어 |
|---|---|
| ① 정답지 만들기 | **데이터 라벨링(annotation)** · **골든셋/평가셋 구축** |
| ② 재기 (회차 ≥ 3) | **오프라인 평가** · **회귀 테스트(regression eval)** · **분산 인지 평가** |
| ② 판·모델 바꿔 재기 | **A/B 비교** · **절제 실험(ablation)** — "힌트 끄기" 가 전형 |
| ③ 프롬프트 고치기 | **프롬프트 엔지니어링**, 자동 초안은 **자동 프롬프트 최적화(APE/OPRO)** |
| ③ 판 관리 | **버전 관리 + 배포 게이트** — 재 보지 않은 판은 켜지 않는다 |
| ④ 실사용 통계 | **온라인 평가** · **암묵적 피드백** — "고치지 않은 비율" = 수용률 |
| 사용자 교정 이력 | **Human-in-the-loop 피드백 루프** (자동으로 돈다) |
| §6 ⑨ 파인튜닝 | **지도학습 미세조정(SFT)** — 순서상 마지막 |

**왜 이 순서인가.** 파인튜닝은 되돌리기 어렵고 모델 버전에 묶인다. 프롬프트는
5분이면 되돌린다. 그리고 파인튜닝을 하더라도 **정답지와 측정 고리는 그대로
필요하다** — 학습셋이 곧 정답지고, 나아졌는지는 결국 재서 안다. 그래서 지금
쌓는 것이 나중에 버려지지 않는다.

---

## 11. 호출 비용 — 176건 $1.57 을 뜯어보면

> 2026-08-31 검토. **아직 아무 절감안도 적용하지 않았다.** 무엇을 줄일지보다
> 무엇을 재고 있는지가 먼저다 — 아래 ①을 하기 전에는 나머지를 넣어도 얼마가
> 줄었는지 알 수 없다.

### 지금 얼마를 쓰고 있나

176건에 $1.57 → **건당 평균 $0.0089 (약 12원)**.

그런데 이 176건은 **성격이 다른 여섯 경로**가 섞인 숫자다.

| 경로 | 무엇 | 이미지 |
|---|---|---|
| `ocr_service.extract_label_from_image` | 표시사항 사진 판독 | **5장** (전체 + 조각 4) |
| `vision_service.process_document_vision` | 문서함 서류 판독 | PDF 쪽수만큼 |
| `ai_validation_service` | 표시사항 AI 검증 | 없음 (텍스트) |
| `ocr_prompt.suggest_revision` | 프롬프트 초안 (`gpt-4o`) | 없음 |
| `regulatory/ai_parser` | 규제 뉴스 파싱 | 없음 |
| `check_openai` | 연결 진단 | 없음 |

**표시사항 판독 한 건의 구조.** 모델은 `gpt-4o-mini`(`.env` 에 `OCR_MODEL` 이
없어 기본값). 사진 한 장을 전체 1 + 2×2 조각 4 = **5장**으로 나눠 전부
`detail: high` 로 보낸다.

여기가 함정이다. **gpt-4o-mini 는 이름과 달리 비전에서는 싸지 않다.** 이미지를
텍스트 토큰으로 환산할 때 큰 배수가 붙어서(우리가 아는 값은 33배 남짓 —
**OpenAI 요금 페이지에서 확인할 것**), `detail: high` 사진 한 장이 2만 토큰대가
된다. 5장이면 12만 토큰대다. 거칠게 계산하면 **표시사항 판독 한 건에 20~26원.**

즉 **평균 12원은 텍스트 호출이 섞여 낮아진 값**이고, 실제 사진 판독은 그
두 배쯤이다.

### 이게 적절한가 — 그렇다. 다만 배수가 문제다

절대 금액은 문제가 아니다. 라벨 하나를 사람이 손으로 옮기면 10분이 넘고, 잘못
옮기면 인쇄를 다시 한다. **26원은 그것에 비하면 없는 돈이다.** 이 문서 §4 ①의
"한 장에 1원이 안 되는 구간" 은 조각 전송을 넣기 전의 계산이라 지금은 맞지 않다
— 5배가 붙었다.

문제가 되는 것은 **규모**다. 하루 200장이면 월 15만원대다. 그리고 지금은
그 15만원이 **어디서 나오는지 우리가 못 쪼갠다.**

### ① 먼저 할 것 — 사용량을 기록한다 (절감 0, 그러나 전제)

여섯 경로 어디에서도 `response.usage` 를 읽지 않는다. 토큰도 비용도 남기지
않는다. 그래서 "176건 $1.57" 을 경로별로 나눌 수 없고, 아래 절감안을 넣어도
**얼마가 줄었는지 잴 수 없다.**

- `response.usage.prompt_tokens / completion_tokens` 를 호출마다 남긴다.
- `OcrRun`(측정)과 판독 경로에 붙이면 `/label/ocr-lab/` 이 "이번 측정에 얼마
  썼는가" 를 그 자리에서 보여 줄 수 있다. 지금은 회차를 올릴 때 얼마가 나갈지
  아무도 모르는 채로 누른다.
- **측정 비용과 운영 비용을 갈라야 한다.** 176건 중 상당수가 정답지 초안과
  벤치마크 회차일 수 있다. 그건 줄일 대상이 아니라 투자다.

### ② 위험이 낮아 바로 넣을 만한 것

| 안 | 절감 | 왜 안전한가 |
|---|---|---|
| **같은 사진 재판독 캐시** | 중복분 전부 | 파일 해시로 최근 결과를 재사용. 지금은 창을 닫았다 다시 열거나 같은 파일을 두 번 고르면 **그대로 두 번 결제된다**. 정확도 영향 없음, 두 번째는 오히려 즉시 응답 |
| **전체 사진만 `detail: low`** | 약 18% | 조각을 보낼 때 첫 장은 "어느 칸이 어느 항목인지" 배치만 보는 용도다 — 프롬프트에도 그렇게 적혀 있다. 글자는 조각이 읽는다. 조각이 없는 작은 사진에는 적용하지 않는다 |

`detail: low` 는 **재고 넣을 것.** 우리 의도가 "첫 장은 배치용" 이라는 것과
모델이 실제로 그렇게 쓰는지는 다른 문제다. `/label/ocr-lab/` 에서 앞뒤를 재면
5분이면 답이 나온다.

### ③ 효과는 크지만 반드시 재고 넣을 것

**(가) 모델 교체 — 잠재적으로 가장 큼**

`gpt-4o-mini` 의 이미지 토큰 배수가 크다는 것은, 배수가 작은 다른 모델이
**정확도를 유지하면서 더 쌀 수 있다**는 뜻이다(`gpt-4.1-mini` 계열). 다만

- 단가와 배수는 **내 기억이 아니라 OpenAI 요금 페이지에서 확인해야 한다.**
- 그리고 싼 것보다 **읽히는 것**이 먼저다. `/label/ocr-lab/` ②의 "모델" 칸에
  모델 이름만 넣으면 같은 정답지로 정확도를 바로 견줄 수 있다. **그 칸은
  정확히 이걸 하라고 만든 것이다.**
- 정확도가 같거나 높으면서 싸면 `.env` 의 `OCR_MODEL` 한 줄로 바꾼다.

**(나) 조각을 조건부로 — 최대 80%, 그러나 지금은 못 넣는다**

전체 1장만 먼저 읽고, `confidence` 가 낮은 항목이 있을 때만 조각을 붙여 2차로
묻는 방식(§5 ⑤ 표적 재질의와 같은 줄기). 쉬운 사진은 5장 → 1장이다.

**지금 적용하면 안 되는 이유가 분명하다.**

- 조각 전송은 §1 이 적어 둔 그 사고 —"원재료명과 주의사항을 통째로 지어내던
  일" — 을 막으려고 넣은 것이다. 조각이 없으면 모델이 보는 유효 해상도가
  축마다 절반이 된다.
- 2차 호출은 **1차가 낮은 확신도를 스스로 신고할 때만** 돈다. VLM 의 실패
  모양은 "못 읽었다" 가 아니라 **"자신 있게 지어낸다"** 다. 자신 있게 틀리면
  2차가 아예 안 돈다 — 비용은 줄고 오류는 조용히 늘어난다.
- 어려운 사진은 **느려진다**: 5~15초 → 10~30초.

넣으려면 순서가 있다. `/label/ocr-lab/` 에 "조각 없이 재기" 옵션을 먼저 넣고,
**조각을 뺐을 때 점수가 얼마나 내려가는지**를 정답지로 잰다. 내려가는 폭이
작은 식품유형이 있으면 거기부터 조건부로 돌린다.

**(다) 조각을 4장 → 2장 — 40%**

라벨은 대개 가로로 길다. 사진 비율을 보고 2×2 대신 세로 2분할(또는 가로
2분할)로 자르면 5장 → 3장이다. 위험은 (나)보다 작지만 같은 성질이다 —
표시사항이 사진 한쪽 구석에 몰려 있으면 유효 해상도가 반만 오른다. 역시
정답지로 재고 넣을 것.

### ④ 넣지 않는 것과 그 이유

| 흔히 떠올리는 안 | 왜 안 하는가 |
|---|---|
| **사진 해상도를 낮춘다** (2000 → 1200) | **비용이 안 줄어든다.** `detail: high` 토큰 수는 "2048 상자에 맞춘 뒤 짧은 변을 768 로" 정규화한 다음 512 타일 수로 센다. 2000px 이든 1200px 이든 타일 수가 같다. **정확도만 떨어진다** — 게다가 조각을 자를 때 원본 해상도가 필요하다 |
| **`max_tokens=4000` 을 낮춘다** | 출력은 **실제로 쓴 만큼만** 과금된다. 상한을 낮춰도 절감이 0 이고, 대신 원재료명이 중간에서 잘린다. 예전 2000 에서 실제로 잘렸다 |
| **프롬프트를 줄인다** | 오늘 늘린 상용 문구 1,300자는 입력 토큰 800 남짓, mini 기준 건당 $0.0001 이다 — **이미지 한 장의 3% 미만.** 텍스트로 아낄 것이 없는 구조다. 여기서 정확도를 깎는 것은 손해 |
| **프롬프트 초안 모델을 mini 로** (`gpt-4o` → mini) | 관리자가 가끔 누르는 것이라 총액에서 미미하고, 여기는 **품질이 중요한 자리**다. 초안이 나쁘면 사람 시간이 더 든다 |
| **프롬프트 캐싱에 기대기** | OpenAI 자동 캐싱은 긴 프리픽스에 붙지만, 우리 비용의 대부분은 **이미지**이고 이미지는 매번 다르다. 캐싱으로 줄어드는 것은 이미 미미한 텍스트 쪽뿐이다 |

### 권장 순서

```
1. 호출마다 usage 기록 → ocr-lab 에 "이번 측정에 든 비용" 표시   ← 여기부터
2. 같은 사진 재판독 캐시 (위험 없음)
3. ocr-lab ② 에서 모델 후보 비교 (정확도 먼저, 그다음 단가)
4. 전체 사진 detail:low 를 앞뒤로 재고 적용
5. "조각 없이 재기" 옵션을 넣어 조각의 실제 값어치를 잰다
6. 그 결과가 좋을 때만 조건부 조각(2단계 호출)
```

---

## 12. 첫 정답지 하나로 돌린 측정 기록 (2026-08-31 ~ 09-01)

> 정답지: `삼립베이커리소시지(SLICE)1000g(스티커)`. **한 장짜리 표본**이라
> 여기서 나온 수치를 일반화하면 안 된다. 다만 **무엇이 왜 틀리는지**는
> 이 한 장에서 대부분 드러났고, 그게 아래 목록이다.

| 회차 | 평균 | rawmtrl_nm | 무엇을 바꿨나 |
|---|---|---|---|
| 1 | 88.0 | **39.1** | (기준) 2×2, 조각 봉합 지시 없음 |
| 2 | 82.2 | 79.5 | 가로 띠 + 위치 표시 + 정답지 수정 (셋을 한꺼번에) |
| 3 | 91.7 | **92.8** | 2×2 + **조각 봉합 지시** |
| 4 | 88.3 | 80.1 | 가로 띠 (같은 지시) |
| 5 | 93.0 | 89.5 | 알레르기 "함유" 제거, NFKC, 이름만 교정 |
| 6 | 92.2 | 74.3 (편차 34) | — (지어낸 원재료 출현) |
| 7 | 91.7 | 90.7 (편차 6.8) | 품목보고번호 한 자리 복구, 지어냄 경고 |
| 8 | 91.8 | **91.7 (편차 5.8)** | "None" 버그 수정, 회차 3회 완주 |

### 배운 것 — 순서대로

**① 조각을 세로로 자르면 줄이 반토막 난다.** 표시사항 본문은 폭 전체를 쓰는
줄이라 2×2 로 가르면 모든 줄이 좌우로 끊긴다. 원재료 15개 중 6개만 읽히고,
없는 원산지가 엉뚱한 원료에 붙고, 없는 원료("전분")가 생겼다.

**② 그런데 고친 것은 자르는 방향이 아니라 지시였다.** 가로 띠를 만들어 재 봤더니
80.1 이었고, 같은 2×2 에 **"조각 경계에서 끊겼으면 이웃 조각에서 나머지를 찾아
이어라"** 한 줄을 넣었더니 92.8 이 나왔다. 가로 띠는 진단에는 값을 했고 처방으로는
필요 없었다. 옵션으로 남겨 두었다(세로로 긴 라벨은 다를 수 있다).

**③ 품목보고번호 한 자리가 등록 정보 대조를 통째로 죽인다.** 96.8점짜리 사소한
오독으로 보이지만 조회는 완전일치라 아무것도 못 찾는다. 화면에는 "대조하면
+0점" 만 떴고, 그건 "대조가 도움이 안 된다" 로 읽힌다.

**④ 측정이 조용히 거짓말을 했다.** 회차 3을 걸었는데 분당 토큰 한도(429)로 두 번이
죽고 한 번만 돌았다. 표에는 "3회" 로 뜨고 **편차가 0** 이라 "안정적" 으로 읽혔다.
사진 한 장이 6~7만 토큰이라 분당 20만 한도면 세 번이 한계다.

**⑤ 원재료명의 문제가 도중에 바뀌었다.** "못 읽음"(6/15)에서 **"지어냄"**으로.
같은 사진 두 번에 "다시마추출물, 다른가공품, L-아르지닌" 이 나왔고 편차가 34
였다. 등록 정보에 없는 이름이 곧 그 신호다.

**⑥ 뜻이 같은 표기 차이에 점수를 깎으면 진짜 오독이 묻힌다.** `℃`(한 글자)와
`°C`(두 글자)를 옳게 읽고도 89.7점이었다. NFKC 로 펼쳐 해결.

**⑦ 산문 지시로 안 되는 것이 있다.** "혼입가능성 문장을 cautions 에도 옮겨라" 를
세 번 다르게 썼는데 세 번 다 사라졌다. 모델이 그 검은 박스를 allergens 에 쓰고
나면 같은 자리를 두 번 쓰지 않는다. **응답 스키마에 칸을 만들어 주는 것**이
산문 열 줄보다 세다(`cross_contamination`).

**⑧ 내가 넣은 버그가 하루 만에 프로덕션에 나갔다.** 괄호를 잘못 묶어 문자열
"None" 이 주의사항 맨 앞에 붙었고, 그 칸이 비는 경우가 보통이라 거의 매번
붙었다. **판독 결과를 손대는 코드는 값이 없을 때를 먼저 시험할 것.**

### 이 정답지에서 아직 안 풀린 것

| 항목 | 점수 | 무엇이 남았나 |
|---|---|---|
| `cautions` | 60.3 (편차 23.1) | 혼입가능성 문장과 "재냉동 금지" 문장 **두 개를 통째로 놓친다.** 문장을 옮기는 순서도 매번 다르다 |
| `recycling_mark` | 72.7 | 보조 표기를 "라벨: PP" / "포장재질: PP" 로 읽는다. 정답은 "비닐류 PP" |
| `additional_info` | 79.5 (편차 23.7) | 홈페이지 주소를 읽는 회차와 안 읽는 회차가 갈린다 |
| `allergens` | 90.5 (편차 **28.6**) | `cross_contamination` 칸을 만든 뒤 흔들리기 시작했다 — **물질 이름이 그쪽으로 새는지 확인 필요** |
| `rawmtrl_nm` | 91.7 (편차 5.8) | `정제소금→소금`, `소콜라겐→대두단백`, `혼합제제[…]` 괄호가 풀림 |

`cautions` 45.2점짜리 회차를 문장 단위로 뜯어보면 **정답 7문장 중 5개를 읽었고
읽은 것은 평균 91.6점**이다. 즉 점수가 낮은 이유는 오독이 아니라 **문장 두 개를
통째로 못 읽어서**다. 채점기를 문장 단위로 바꾸면 점수가 오르는 게 아니라
**무엇이 빠졌는지가 보인다** — 그게 §11 ①에서 말한 "진짜 점수가 드러난다" 다.

### 다음 정답지에서 확인할 것

1. **등록 정보가 있는 제품**으로 만들 것. 이 정답지는 대조 이득이 +0.2점에
   그쳤는데, 등록 정보 자체가 없어서인지 매칭이 안 돼서인지 아직 못 갈랐다.
   "자세히" 에서 "찾지 못했습니다" 가 뜨는지 먼저 보라.
2. **식품유형을 섞을 것.** 소시지 한 장으로는 유형별 표시항목 차이를 볼 수 없다.
3. 위 다섯 가지가 **이 제품만의 일인지** 확인. 특히 `cautions` 와
   `recycling_mark` 는 라벨 배치를 타는 항목이라 다른 제품에서 다를 수 있다.
4. `allergens` 편차는 **새로 생긴 것**이라 다음 측정에서 먼저 볼 것.

### 두 번째 정답지 — 더블치즈&바질치킨 샐러드 (2026-09-01)

영양성분까지 채워진 정답지라 채점 항목이 23개로 늘었다.

| 회차 | 평균 | cautions | additional_info | rawmtrl_nm | 무엇을 바꿨나 |
|---|---|---|---|---|---|
| 9 | 92.7 | 51.7 | 55.4 (편차 **79.1**) | 80.1 | (첫 측정) |
| 10 | 92.1 | 24.9 | 52.2 (편차 **82.9**) | 86.7 | 상용 문구 목록을 프롬프트에서 뺌 |
| 11 | — | (제외) | (제외) | — | 두 칸을 아예 읽지 않기로 |

**⑨ 상용 문구 목록을 프롬프트에 실었더니 그 문장을 지어냈다.**
샐러드 라벨에 없는 세 문장이 판독값에 들어왔다.

```
주의사항     "직사광선을 피하고 서늘한 곳에 보관하시기 바랍니다."
             "개봉 후에는 변질될 우려가 있으니 반드시 밀봉하여 냉장 보관하시고…"
기타표시사항  "본 제품은 공정거래위원회 고시 소비자분쟁해결기준에 의거…"
```

셋 다 §4-2 ⑬ 에서 프롬프트에 실은 목록의 문장이고, 글자 하나 안 틀렸다.
그때 "목록에 있다는 이유로 적으면 그것이 곧 지어낸 값이다" 라고 못 박아
두었는데 **못 박은 것으로 막히지 않았다.** 흔한 문구일수록 모델이 이미
알고 있어서, 안 읽고도 그럴듯하게 채운다.

목록은 판독 **뒤에** 읽은 문장을 확정하는 데만 쓴다(`ocr_snap`). 그쪽은
모델이 실제로 읽은 문장에만 손대므로 없는 것을 만들 수 없다. 화면의 빠른
입력 버튼도 그대로다.

**⑩ 목록을 빼도 지어냄은 모양만 바뀌었다.** 이번에는 목록에 없는 새 문장을
만들었다("질병가족력이 있는 경우 섭취하지 마십시오"). 그리고 알레르기 칸의
"○○ 함유" 줄을 통째로 주의사항으로 옮겨 적었다.

**⑪ 전용 칸을 만들었더니 옆 칸이 나빠졌다.** "…혼입가능성 있음" 에
`cross_contamination` 칸을 줬는데, 혼입 문장은 두 제품 모두에서 **끝내 안
나왔고** `allergens` 가 두 제품 모두에서 나빠졌다(소시지 100 → 편차 28.6,
샐러드 73.3). 세 칸이 같은 검은 박스를 두고 다툰 것이다. **되돌렸다.**

> §4-2 ⑬ 에서 "칸을 만들어 주면 그 칸을 채운다" 가 통했다고 적었는데,
> 이 자리에서는 빗나갔다. **칸을 만드는 것이 늘 답은 아니다** — 그 칸이
> 이웃 칸과 같은 자리를 읽어야 할 때는 오히려 다툼을 만든다.

**⑫ 모델이 옮겨 적지 않고 다시 쓴다.**

```
정답  고객상담실 080-739-8572(수신자 부담)
판독  상품명에 대한 문의는 080-739-8572로 하시기 바랍니다
```

번호는 맞는데 문장을 새로 썼다. "그대로 옮기라" 를 기타표시사항까지 묶고
강하게 했다.

**⑬ 프롬프트의 예시가 그 예시대로 실패했다.** "지시서 품명이 '…샐러드_후면'
이어도 라벨 안 제품명이 답이다" 를 프롬프트에 적어 뒀는데
`더블치즈&바질치킨 샐러드_후면` 이 그대로 나왔다. **설득이 안 되는 것은
코드로 뗀다**(`strip_design_suffix`).

**⑭ 복합원재료 대괄호를 첫 하위 원료에서 닫는다.**

```
정답  쉬레드치즈[모짜렐라, 체다, 혼합제제(…)]
판독  쉬레드치즈[모짜렐라], 체다, 혼합제제(…)
```

나머지가 별개 원재료로 밀려 나가 **원재료 수가 늘고 함량 순서가 뒤틀린다.**
괄호 짝은 맞으므로 `bracket_problems` 가 못 잡는다 — 프롬프트로 다뤄야 하는
자리다. 소시지의 `혼합제제[…]` 와 같은 실패라 **일관된 패턴**이다.

### 결정: 주의사항·기타표시사항을 읽지 않는다 (2026-09-01) — ⚠️ 2026-09-03 뒤집힘

> **아래 결정은 §15 에서 뒤집혔다.** 지금은 기본으로 읽는다. 그 판단이 왜
> 서 있었는지는 남길 값이 있어 그대로 둔다.


무엇을 해도 흔들렸다. 목록을 실으면 목록을 지어내고, 빼면 새로 지어내고,
칸을 만들면 옆 칸을 망쳤다. `additional_info` 편차가 82.9 였다.

그런데 **이 두 칸에는 이미 화면에 빠른 입력 버튼 스물여덟 개가 있다.**
두어 번 눌러 정확한 문장을 넣을 수 있다. 지어낸 문구가 법적 표시물에
들어가는 위험이 버튼 두 번 누르는 수고보다 크다.

- 기본 끄기. `.env` 의 `OCR_READ_FREETEXT=True` 로 켠다.
- 측정 화면 ② 에 "주의사항·기타표시사항도 읽기" 체크가 따로 있다 — 모델을
  바꿨을 때 나아졌는지 견주는 자리다.
- **안 읽은 칸은 0점이 아니라 채점 제외**다. 일부러 안 읽은 것을 "못 읽었다"
  로 세면 옵션을 껐다 켰다 하는 비교가 거짓말이 된다.
- 확인 창이 왜 그 칸이 없는지 말해 준다. 그냥 빠져 있으면 "사진에 없었나
  보다" 로 읽힌다.

**되돌릴 조건**: 아래 §13 이 되면 이 두 칸을 다시 켤 수 있다. 원문이 있으면
지어냈는지를 기계로 검출할 수 있기 때문이다.

---

## 13. OCR 원문과 VLM 을 섞는다 — 착수 전 검토 (2026-09-01)

> 아래는 착수 전에 세운 계획이다. **실제로 해 본 결과와 그 뒤의 계획은 §14** 에
> 있다. 이 절은 그때의 판단을 그대로 남긴다 - 무엇을 예상했고 무엇이 빗나갔는지
> 견주려면 원래 계획이 남아 있어야 한다.

> 2026-09-01 검토. **아직 아무것도 구현하지 않았다.** 먼저 확인해야 할 것이
> 하나 있고(아래 1단계), 그 답에 따라 나머지가 전부 달라진다.

### 왜 이 방향인가 — 측정이 이미 답을 줬다

| VLM 이 하는 일 | 점수 |
|---|---|
| 짧고 정형화된 값 (제품명·식품유형·내용량·품목보고번호·소비기한·보관방법·영양성분 12개) | **100, 편차 0** |
| 긴 자유 문구 (주의사항·기타표시사항) | **25~52, 편차 80** |
| 원재료명 (길고 구조가 있음) | 86~92, 괄호가 무너짐 |

VLM 은 **레이아웃 이해**(어느 칸이 어느 항목인가)가 탁월하고 **긴 문자열의
축자 전사**를 못 한다. 생성 모델의 본성이다 — 다음 토큰을 확률로 뽑으니 긴
문장에서 "그럴듯한" 쪽으로 흐른다.

전통 OCR 은 정확히 반대다. **축자 전사는 잘하고**(글자를 보고 글자를 내므로
지어낼 수가 없다) **어느 칸인지는 모른다.** 둘은 상보적이다.

### 새 방식이 아니라 이미 검증된 방식의 확장이다

문서함 PDF 는 **이미 하이브리드**다(§4 ①). 텍스트 레이어를 뽑아 "글자는 이
원문을 그대로 옮기고, 그림은 어느 값이 어느 항목인지 판단하는 데만 쓰라" 고
지시한다. 사진에는 텍스트 레이어가 없어서 못 했을 뿐이다.

### 진짜 이득 — 지어냄을 기계로 검출할 수 있다

```
규칙   모델이 낸 값은 OCR 원문에 실제로 있는 문자열이어야 한다
검증   판독값이 원문에 (유사) 부분문자열로 있는가? 없으면 지어낸 것
```

지금 지어냄을 잡는 수단은 **원재료명의 등록 정보 대조 하나뿐**이다. 주의사항에는
아무 수단이 없어서 두 칸을 통째로 뺐다. 원문이 있으면 **모든 칸에 같은 검사**를
걸 수 있고, 그러면 두 칸을 되살릴 수 있다.

### 갈림길 — OCR 이 우리 라벨을 읽는가

우리가 다루는 것은 6pt 원형 스티커, 곡면 용기, 작업지시서에 얹힌 도안이다.
**OCR 이 못 읽으면 "원문" 이 쓰레기가 되고, 그걸 "그대로 옮기라" 고 하면
쓰레기를 확정한다.**

추측이 아니다. §4 ① 에서 이미 겪었다 — 스캔 PDF 가 도장·서명만 벡터로 흘릴 때
그 조각을 원문이라 넘기면 모델이 그것을 믿고 나머지를 지어냈다. 그래서 40자
미만이면 버리는 가드를 넣었다. **같은 가드가 필요하다.**

### 엔진 후보

| 후보 | 한국어 | 비용 | PythonAnywhere |
|---|---|---|---|
| **Naver CLOVA OCR** (권장) | 최상급, 라벨·영수증 특화 | 건당 수 원 | API 라 문제없음 |
| Google Cloud Vision | 좋음 | 건당 ~2원 | API 라 문제없음 |
| PaddleOCR / EasyOCR | 좋음 | 무료 | **디스크·메모리 부담** (PyTorch/Paddle) |
| Tesseract | 낮음, 작은 글씨에서 무너짐 | 무료 | 가벼움 |

비용은 문제가 아니다. 지금 판독 1건이 20~26원인데(§11) OCR 2원은 10% 증가다.
자유 문구 두 칸을 되살릴 수 있다면 남는 장사다.

`requirements.txt` 가 33줄뿐인 지금 구성에 PyTorch 를 들이는 것은 서버 자원부터
확인할 일이라, **클라우드 API 가 현실적**이다.

### 부작용 — 미리 적어 둔다

- **원문 순서가 뒤죽박죽**일 수 있다(다단·회전·곡면). 좌표로 정렬해야 하고
  그게 또 하나의 실패 지점이다.
- 입력 토큰 +2~3천. 이미지 6~7만에 비하면 5% 미만이라 무시할 수 있다.
- 속도 +1~3초.
- **OCR 이 틀린 자리를 VLM 이 못 고치게 될 위험.** "원문에서만 고르라" 고 하면
  원문의 오독이 그대로 굳는다. 원문은 **강제가 아니라 우선**이어야 하고, 둘이
  다르면 어느 쪽도 고르지 말고 **불일치를 짚는** 쪽이 안전하다.

### 넣는 순서

```
1. OCR 엔진 하나로 우리 라벨 원문만 뽑아 보는 관리자 도구      ← 여기서 판가름
   (판독과 무관. 정답지 사진 두 장이면 확인된다)
2. 읽히면 프롬프트에 원문 동봉 (문서함 경로와 같은 지시)
3. 판독값이 원문에 있는지 검증 → 없으면 confidence 를 내리고 표시
4. /label/ocr-lab/ 에 "OCR 원문 함께 보내기" 옵션으로 A/B
5. 자유 문구 두 칸을 다시 켜서 재기  ← 이게 되면 성공
```

**1번을 먼저 한다.** OCR 이 우리 라벨을 못 읽으면 2~5번이 전부 무의미하고,
읽으면 지금까지 못 풀던 문제 대부분이 한 번에 풀린다.

---

## 14. 실제로 해 본 결과와 남은 문제 (2026-09-02 ~ 09-03)

> §13 의 계획대로 착수했다. **1단계는 통과했고 원재료명은 확실히 좋아졌다.**
> 그런데 원문을 넣으면 **다른 칸이 하나씩 무너진다.** 그 목록과 고치는 순서를
> 여기 남긴다.
>
> 지금 상태: `OCR_HYBRID` 는 **꺼져 있다.** 아직 켜면 안 된다.

### 1단계 — OCR 이 우리 라벨을 읽는가: **읽는다**

Google Cloud Vision (`DOCUMENT_TEXT_DETECTION`) 으로 정답지 5장의 원문을 뽑아
정답이 그 안에 있는지 셌다.

| 정답지 | 긴 칸 회수율 | 원문 |
|---|---|---|
| #1 더블치즈&바질치킨 샐러드 | 0.996 | 1,327자 |
| #2 삼립베이커리소시지 | 0.996 | 1,103자 |
| #3 얌 우동면 | 0.971 | 2,072자 |
| #5 조인 계란후라이 | 0.968 | 1,729자 |
| #6 숯불갈비버거 | 0.992 | 2,033자 |
| **평균** | **0.977** | |

문턱 0.9 를 넘었다. 가장 길고 VLM 이 가장 못하던 칸들이 특히 잘 읽혔다 —
`cautions` 218자에서 99.5, `rawmtrl_nm` 176자에서 99.4.

**빗나간 것은 하나도 남김없이 OCR 실패가 아니었다.**

| 처음 점수 | 진짜 원인 |
|---|---|
| storage_method 76.9 / 86.7 | `℃`(U+2103) 와 `°C` 를 다른 글자로 셈 → NFKC 로 해결 |
| additional_info 55.8 | 흩어져 인쇄된 문구를 한 덩어리로 찾음 → 조각 채점으로 해결 |
| cautions 59.1 | 위와 같음 (문장 단위) |
| storage_method 70.0 | **정답지가 틀림** (`-18도` 로 적혀 있었다) |
| pog_daycnt 85.7 | **정답지가 틀림** (`표기` vs `표시`) |
| storage_method 75.0 | **정답지가 틀림** (`냉동보관(0~10℃)` — 0~10℃ 는 냉장이다) |
| recycling_mark | 도형이라 글자로 찾을 수 없다 → 채점에서 뺌 |

**OCR 오독으로 판명된 사례는 5장 · 항목 약 100개에서 0건이다.** 정밀도를
직접 재지는 않았지만, 4단계의 위험이 처음 걱정한 것보다 낮다는 신호다.

측정 도구가 **정답지의 오류를 세 건 잡아냈다는 것**도 소득이다. 그중 하나
(`냉동보관(0~10℃)`)는 같은 오타가 실제 표시사항에 들어갔다면 보관방법 위반이다.

### 만든 것

| 파일 | 하는 일 |
|---|---|
| `services/ocr_text.py` | Vision 어댑터, 정답지 기반 회수율, 빗나간 자리 설명 |
| `services/ocr_ground.py` | 판독값 ↔ 원문 대조(2단계), 혼입가능 물질 수리 |
| `commands/ocr_ground_check.py` | 1단계 가부 판정 |
| `commands/ocr_ab.py` | 옵션 켜고/끄고를 나란히 재는 A/B |
| `OcrTruthCase.ocr_text` | 원문 보관 (같은 사진을 다시 부르지 않는다) |

설정 네 개. **넷 다 기본은 꺼짐이다.**

```
GOOGLE_VISION_API_KEY        Vision 인증 (서비스 계정보다 붙이기 쉽다)
OCR_GROUND                   판독값을 원문과 대조해 confidence 를 내린다
OCR_HYBRID                   원문을 판독에 함께 넣는다
OCR_HYBRID_DROP_TILES        조각 이미지까지 뺀다 (토큰 절감)
OCR_TPM_LIMIT                측정이 판독 사이를 얼마나 쉴지 계산하는 데 쓴다
```

### A/B 실측 — 이긴 것과 진 것

정답지 한 장씩, 1회. **회차가 하나라 작은 숫자는 잡음이다.** 큰 숫자만 읽는다.

| 항목 | #1 | #2 | #3 | #5 | #6 |
|---|---|---|---|---|---|
| **rawmtrl_nm** | +14.5 | +19.8 | **+79.5** | +25.0 | **+27.0** |
| bssh_nm | +7.1 | +52.6 | +50.0 | +50.0 | 0 |
| pog_daycnt | 0 | — | +100 | +100 | — |
| frmlc_mtrqlt | +5.3 | 0 | +100 | — | 0 |
| **allergens** | 0 | **-80.4** | +60.0 | -33.9 | **-80.4** |
| **content_weight** | 0 | 0 | 0 | 0 | **-29.4** |
| nutrition_basis | 0 | — | -31.8 | -53.8 | — |
| recycling_mark | +2.7 | +1.5 | +54.5 | -12.2 | +1.5 |

**`rawmtrl_nm` 은 다섯 장 모두 크게 올랐다.** 이 작업의 목적이고 흔들림이 없다.
그리고 판독이 원래 심하게 실패하던 라벨(#3 48.3, #5 66.6)이 94.1 / 90.7 로
살아났다 — 어려운 라벨일수록 원문이 크게 돕는다.

### 고친 것 세 가지 — 무엇을 배웠나

**① 원문 주입과 조각 제거는 다른 일이다**

처음에는 둘을 한 덩어리로 묶었다. 조각을 빼니 `nutrition_basis` 가 100 → 37,
`recycling_mark` 100 → 84.8, `storage_method` 100 → 88.9 로 무너졌다.

무너진 셋은 성격이 같다 — **배치를 봐야 읽히는 칸**이다. 영양성분표의 기준
표기는 표의 머리글이고, 분리배출은 도형이고, 보관방법은 표 칸에 있다. 원문은
줄을 늘어놓을 뿐 그 구조를 담지 못한다. **조각이 그걸 보여 주던 것이었다.**

그래서 `OCR_HYBRID`(정확도)와 `OCR_HYBRID_DROP_TILES`(토큰 절감)를 갈랐다.
조각을 되살리니 셋이 회복됐다. **지금 구성은 토큰을 아끼지 않는다** — 정확도만
가져간다.

**② "원문을 그대로 옮기라" 가 너무 넓었다**

문서함 PDF 에서 통하던 지시를 그대로 썼더니 `prdlst_nm` 이 -37.9, `allergens`
가 -87.7 로 무너졌다. 그 칸들은 **원문을 그대로 옮기면 틀리는 칸**이다 —
알레르기는 "함유" 를 떼야 하고, 제품명은 작업지시서 품명을 걸러야 한다.
시스템 프롬프트에 그 규칙이 이미 있는데 뒤에 붙인 한 줄이 덮었다.
**모델은 시킨 대로 한 것이다.**

PDF 가 통한 이유는 문서 한 장이 통째로 본문이기 때문이다. 사진은 다르다 —
라벨 밖의 작업지시서 표까지 원문에 섞여 들어온다.

지시문을 칸별로 갈랐다(긴 문구는 원문을 그대로, 나머지는 사진을 먼저).
**`prdlst_nm` 은 회복됐다**(-37.9 → 0).

**③ 사용자가 고른 표시면 이름을 원문에도 붙인다**

실사용에서는 사용자가 주표시면·일괄표시면·영양성분표를 하나씩 골라 준다.
판독 프롬프트는 그 이름을 이미 쓰고 있었는데, 원문은 면별로 받아 놓고도
한 덩어리로 이어 붙여 그 정보를 버리고 있었다. 이제 토막마다 이름표가 붙는다.

    [1) 주표시면 — 이 면에 있는 항목: 제품명, 내용량, 특정성분 함량]
    [2) 영양성분표 — 이 면에 있는 항목: 열량, 나트륨, ...]

**아직 효과를 재지 못했다** — 아래 "막힌 것" 참고.

---

### 남은 문제와 개선 계획

#### 1순위 — `allergens` 가 여전히 무너진다 (100 → 12.9)

**증상.** 라벨의 검은 박스에는 두 줄이 나란히 인쇄된다.

    쇠고기, 조개류(굴) 함유                            <- 알레르기
    메밀, 땅콩, 닭고기, 게, 새우, ... 혼입가능성 있음   <- 주의사항

사진만 볼 때는 모델이 두 줄을 제대로 가른다(100점). 원문을 넣으면 두 줄이
그냥 이어진 글자라 시각적 경계가 없고, **훨씬 긴 뒷줄이 답으로 나온다.**

**해 본 것.** 지시문을 두 번 고쳤다. 안 움직였다. 그래서 코드로 뗐다 —
원문을 갖고 있으니 어느 물질이 어느 줄에 적혔는지 알 수 있다
(`ocr_ground.repair_allergens`). 혼입 줄에만 있는 물질을 알레르기 값에서
덜어 내되, 양쪽에 다 적힌 물질은 남긴다.

**그런데 실측에서 여전히 12.9 다.** 두 가지 중 하나다.

- 그 커밋이 안 들어간 채로 쟀다 (점수가 소수점까지 이전과 같았다)
- 수리가 돌았는데 못 잡았다

**다음 할 일 — 진단이 먼저다.** 지금은 점수만 보여서 왜 안 잡혔는지 알 수
없다. `ocr_ab` 에 `--dump` 를 붙여 **판독값 원문과 OCR 원문을 함께 찍게**
한다. 세 가지가 갈린다.

| 보이는 것 | 뜻 | 할 일 |
|---|---|---|
| 모델 값이 혼입 물질 목록 | 수리가 못 잡았다 | 물질 이름 가르는 규칙을 고친다 |
| 모델 값이 엉뚱한 제3의 값 | 다른 곳을 읽었다 | 면 이름표로 해결될 가능성 |
| 원문에 혼입 줄이 없다 | Vision 이 그 줄을 못 읽었다 | 수리로는 못 고친다 |

**추측으로 또 고치지 않는다.** 이번에 이미 두 번 그렇게 했고 두 번 다 빗나갔다.

#### 2순위 — `content_weight` 100 → 70.6 (#6 에서 새로 나타남)

내용량은 주표시면에 있는데, 영양성분표에도 "총 내용량 87 g" 이 있다. 원문에서
둘이 구분되지 않아 섞였을 가능성이 크다.

**면 이름표(③)가 정확히 이걸 겨냥한 것**이라 먼저 그 효과를 확인한다.
안 되면 `content_weight` 도 "원문을 그대로 옮기면 안 되는 칸" 목록에 넣는다.

#### 3순위 — 면 이름표의 효과를 잴 수 없다 (막힌 것)

면 이름표는 `extract_label_from_parts`(면을 여러 개 고른 경우)에만 붙는데,
**정답지 측정은 `extract_label_from_image`(사진 한 장)를 부른다.**
그래서 A/B 로 잴 수가 없다.

두 길이 있다.

- **정답지에 면을 넣는다.** `OcrTruthCase` 에 면별 사진을 여러 장 두거나,
  `crop_box` 를 면마다 하나씩 두고 role 을 붙인다. 측정 체계가 바뀌는 일이라
  작지 않다.
- **실사용 화면에서 눈으로 본다.** 면을 나눠 읽고 `allergens`·`nutrition_basis`
  ·`content_weight` 를 확인한다. 빠르지만 숫자가 안 남는다.

먼저 후자로 방향을 보고, 효과가 있으면 전자에 투자한다.

#### 4순위 — 측정 자체가 얇다

**1회 측정이다.** `rawmtrl_nm` +14.5~+79.5 처럼 다섯 장 모두 같은 방향인 것은
믿을 만하지만, `recycling_mark` ±1.5 같은 숫자는 잡음과 구분되지 않는다.
방향이 정해지면 `--runs 3` 으로 5장 전체를 돌려 확정한다.

**분당 토큰 한도가 발목을 잡는다.** 판독 한 번이 6~7만 토큰이고 한도가
200,000(등급 1)이라 분당 세 번이 한계다. 5장 3회 A/B 가 11분이다. 게다가 이
열쇠는 실서비스와 함께 쓰므로, 우리가 아무리 아껴 불러도 사용자가 사진을
올리고 있으면 창이 남의 요청으로 차 있다 — 실제로 `Used 200000, Requested
4692` 가 나왔다. **등급 2 로 올리면 한도가 열 배가 되고 이 문제가 사라진다.**
올린 뒤 `.env` 의 `OCR_TPM_LIMIT` 을 함께 고쳐야 대기가 줄어든다.

#### 미룬 것 — 토큰 절감

§13 은 토큰 70% 절감(6~7만 → 1.5~2만)을 기대했다. **그건 조각을 빼야 나오고,
그 대가가 배치 칸 셋이었다.** 지금은 정확도만 가져가고 절감은 포기한 상태다.

되찾으려면 "배치를 봐야 읽히는 칸"만 따로 다뤄야 한다. 예를 들어 영양성분표
조각만 남기고 나머지를 빼는 식인데, 어느 조각이 어느 면인지는 **사용자가 면을
골라 줬을 때만** 안다. 그래서 3순위(면 이름표)가 풀려야 여기로 올 수 있다.

### 다음에 이어서 할 순서

1. `ocr_ab --dump` 로 `allergens` 진단 → 원인에 맞게 수리
2. 실사용 화면에서 면을 나눠 읽어 면 이름표 효과 확인
3. 좋으면 `--runs 3` 으로 5장 확정 → `OCR_HYBRID=True`
4. 그 뒤에 정답지의 면 구분, 그리고 토큰 절감 재도전

**켜는 조건은 그대로다.** 목적한 칸이 올라도 **잘 읽던 칸이 하나라도 무너지면
채택하지 않는다.** `ocr_ab` 가 그 판정을 직접 말한다.

---


## 15. 본서버 시험에서 나온 것 (2026-09-03)

측정 화면이 아니라 **실제 화면에서 사진을 올려 본** 결과다. 여기서 나온 셋은
정답지 점수로는 안 보이던 것이다.

### ① 표시면 지시문의 목록이 곧 "읽을 항목" 이었다

일괄표시면을 골라 읽었더니 **원산지·유통전문판매원·주의사항·기타표시사항이
통째로 비어서** 왔다. 사진에는 다 인쇄돼 있었다.

원인은 `REGION_ROLES` 의 `wants` 다. 이 목록이 지시문에 "여기서 찾을 항목" 으로
나가고, 바로 다음 줄이 **"각 장에 적힌 것만 읽으세요"** 다. 두 문장이 붙으면
목록이 상한선이 된다 — 모델은 시킨 대로 했다.

`wants` 는 "그 면에서 보통 읽히는 것" 을 적어 둔 것이었고 전수 목록이 아니었다.
화면(`photo_cropper.js` 의 hint)과 같은 문자열이라 **고르는 사람에게 보여 주는
설명**이 그대로 모델 지시가 된 셈이다.

두 가지를 함께 고쳤다.

- 일괄표시면·원재료명 영역의 목록에 그 면에 실제로 인쇄되는 것을 빠짐없이 넣었다
- 지시문에 **"목록은 흔한 것을 짚어 준 것이지 거기까지만 읽으라는 뜻이 아니다"**
  를 넣었다. 목록만 채우면 새 칸이 생길 때마다 같은 사고가 난다

> 이 경로는 **영역을 골라 읽을 때만** 탄다(`extract_label_from_parts`).
> 표시사항 작성 화면은 사진 한 장을 통째로 보내므로 영향이 없었다 — 그래서
> §14 의 정답지 측정(`extract_label_from_image`)에도 안 잡혔다. §14 3순위가
> 말한 "면 이름표를 잴 수 없다" 가 실제로 물린 자리다.

### ② 주의사항·기타표시사항을 다시 읽는다 (§12 결정 뒤집기)

`OCR_READ_FREETEXT` 기본값을 **True** 로 바꿨다.

§12 는 "지어낸 문구가 법적 표시물에 들어가는 위험이 버튼 두 번보다 크다" 로
껐고, 되돌릴 조건을 §13(원문 대조)으로 걸어 뒀다. 운영에서 그 저울이 달라졌다.

- 라벨에 인쇄된 문장은 **빠른 입력 버튼 스물여덟 개에 없는 것이 많다.**
  버튼 두 번이 아니라 통째로 타자였다
- 사진을 올린 사람은 안내 문구를 붙여 놔도 "사진에 있는데 왜 안 읽나" 를
  먼저 물었다. 안 읽는 것이 결함으로 읽혔다
- 지어낼 위험은 그대로지만 **이 값이 확인 창을 건너뛰는 경로는 없다.**
  체크한 항목만 칸에 들어가고 저장은 따로 누른다

되돌리려면 `.env` 에 `OCR_READ_FREETEXT=False`. 측정 화면은 예전처럼 이 값과
무관하게 껐다 켜서 견준다. **§12 의 채점 규칙은 그대로다** — 끈 상태에서 안 읽은
칸은 0점이 아니라 채점 제외다.

### ③ 영역 고르기에 확대·축소와 제외 영역

4000px 사진이 화면에서 900px 로 줄어 보인다. 일괄표시면의 위아래 끝이 몇 픽셀
안에 뭉쳐서 경계를 짚을 수가 없었다.

- **확대·축소**(`+` / `−` / 맞춤, Ctrl+휠). 보는 배율만 바꾼다 — 자를 때는 언제나
  원본에서 잘라내므로 확대해서 골랐다고 화질이 달라지지 않는다. 확대해도 고른
  상자는 그대로 두고 배율만큼 늘려 준다(회전은 좌표계가 통째로 바뀌므로 예전처럼
  비운다)
- **제외할 영역.** 일괄표시면 옆에 작업지시서 표나 다른 제품 라벨이 같이 찍혀
  있으면 사각형 하나로는 피해 갈 수가 없다. 덮어 둔 자리는 잘라낼 때 흰색으로
  지워 보낸다 — 모델이 아예 못 본다. "전체 사용" 도 이 제외를 반영한다

> ③ 은 §14 2순위(`content_weight` 가 영양성분표의 "총 내용량" 과 섞임)와 §13 의
> 토큰 절감에도 걸린다. 사용자가 면을 더 정확히 갈라 줄수록 그 둘이 쉬워진다.

---


## 부록 A. 채택하지 않은 초기 설계안 (2025-12)

> 원본은 `ocr_system_design.md` 였다. 1,068줄 중 살아남을 내용이 이만큼이라
> 파일을 지우고 여기로 옮겼다. 필요하면 `git log -- ocr_system_design.md` 로
> 전문을 꺼낼 수 있다.

**무엇을 제안했나.** 사진에서 **Google Cloud Vision 또는 Tesseract 로 글자를
뽑고**, 정규식·규칙 기반 파서(`text_parser.py`)가 "이 글자는 소비기한 칸" 이라고
붙이는 두 단계 구조였다. Celery 로 비동기 처리, 필드별 파서 클래스, 신뢰도
점수까지 설계돼 있었다.

**왜 그 길로 가지 않았나.**

- 두 단계 중 **뒷단(파싱)이 실제로는 더 어렵다.** 라벨마다 항목 이름이 다르고
  ("제조원" / "업소명 및 소재지" / "제조업소명"), 칸 배치도 제각각이라 정규식이
  끝없이 늘어난다. VLM 은 그 판단을 **레이아웃을 보고** 한다.
- 그래서 두 단계를 **모델 한 번 호출로 합쳤다**(§1). `text_parser.py` 는
  "GPT-4o mini 가 OCR 과 파싱을 동시에 처리하므로 더 이상 사용하지 않는다" 는
  주석 두 줄만 남은 빈 파일로 있다.

**그런데 이 판단이 절반만 맞았다는 것이 2026-09-01 에 드러났다.** 측정해 보니
VLM 은 **레이아웃 이해는 탁월한데 긴 문자열의 축자 전사를 못 한다** — 주의사항을
지어내고 원재료명의 괄호 구조를 무너뜨린다(§12).

그래서 §13 에서 **OCR 을 다시 들이되 역할을 바꿔** 넣으려 한다. 그때는 OCR 이
주역(글자를 뽑고 규칙이 파싱)이었고, 이번에는 **검증자**다 — VLM 이 값을 내고,
OCR 원문이 "그 글자가 사진에 실제로 있었는가" 를 답한다. 설계안을 되살리는 것이
아니라, 버린 조각 중 쓸모 있는 하나를 다른 자리에 끼우는 것이다.
8314
