# 표시사항 업무 흐름을 이 시스템으로 옮기기

회사에서 포장지가 만들어지기까지의 여섯 단계 가운데, **1·2 → 3 (표시사항
초안 작성)** 과 **5 (도안 검증)** 를 이 시스템 안에서 하게 하려면 무엇을
고쳐야 하는가.

## 지금 하는 일

```
1. 배합비(원료·성분)        ─┐
                             ├─→ 3. 표시사항 초안(워드)
2. 기본 정보(제품명·중량…)  ─┘        │
                                      ↓
                             4. 포장지 디자인 파일(마케팅)
                                      ↓
                             5. 담당자 검증
                                      ↓
                             6. 인쇄
```

파일이 사람 손을 건너다니면서 **전달 누락 · 오타 · 옛 파일 사용**이 생긴다.

## 각 단계가 지금 어디까지 되는가

| 단계 | 현재 | 빈 곳 |
|---|---|---|
| 1. 배합비 | BOM 탭(원료·함량 %), 원료 사진 → BOM 행, 배합비 순서 검증 | 엑셀·워드 규격서를 읽는 입구가 없다 — 사진뿐 |
| 2. 기본 정보 | 기본 정보 탭, 사진·품목보고번호로 채우기 | 대체로 됨 |
| 3. 표시사항 초안 | 미리보기 + 규정 검증 + PDF → 문서함 등록 | ~~PDF가 이미지라 글자를 복사할 수 없다~~ → **① 완료** |
| 4. 디자인 파일 | (타 시스템) | 요청·회신 통로가 거래처용 서류 요청뿐 |
| 5. 검증 | 사진으로 불러오기 = **채우는** 도구 | **대조 도구가 없다.** 기록도 안 남는다 |
| 6. 인쇄 | — | 범위 밖 |

## 오류가 나는 자리와 원인

**오타 — 3→4 사이.**
`exportToPDF` 는 `html2canvas` 로 화면을 이미지로 떠서 PDF에 붙인다. 받는
사람은 글자를 선택할 수 없어 원재료명 300자를 손으로 다시 친다. 실제 검수
의견에 나온 `과당1`(원래는 `과당]`)이 그 흔적이다 — 대괄호를 1로 쳤다.

**전달 누락 — 1·2가 시스템 밖에 있다.**
워드·엑셀 파일이 원본이고 시스템은 사본이다. 원본이 바깥에 있으면 누락은
계속 난다.

**옛 파일 확인 — 버전이 파일에 안 찍힌다.**
문서함에 버전이 쌓이지만 디자인 담당자가 받은 것이 몇 번인지 알 수 없다.

## 개선 항목

### ① 표시사항을 글자로 내보내기 — **완료**

산출물이 이미지 PDF 하나뿐이었다. 붙여넣을 수 있는 글자를 함께 내준다.

- 미리보기 머리에 **글자 복사** / **텍스트 파일 저장**
- 줄 도구에 **이 줄만 복사** (원재료명 하나만 달라는 요청이 잦다)
- 항목명과 값을 탭으로 갈라 워드·엑셀에 붙이면 그대로 표가 된다
- 화면 전용 표시(미입력 안내·지적 번호)는 빼고, 영양정보 표도 함께 담는다

3→4 오타의 원인을 끊는다.

### ② 대조 모드 — **완료**

불러오기 창에 "디자인 시안과 대조" 를 뒀다. 같은 판독을 쓰되 뜻이 반대다.

    채우기   빈 제품에 값을 넣는다        (표시사항을 만드는 단계)
    대조     확정한 값과 시안이 같은지 본다 (인쇄 전 검증 단계)

대조 창에는 체크박스도 고칠 칸도 반영 단추도 없다. 다른 항목을 위에, 같은
항목을 접어서 아래에 둔다. **값은 바뀌지 않는다** — 어느 쪽이 맞는지는 원본
자료를 보고 정할 일이고, 우리가 고르면 틀린 쪽을 정본으로 삼을 수 있다.

### ③ 대조 기록 — **완료**

시안 파일이 문서함의 **포장지 시안**(`DESIGN_PROOF`)으로 들어가고, 그 파일의
metadata 에 대조 결과가 붙는다 — 누가 언제 무엇이 달랐는지. 활동 로그에도
`DESIGN_COMPARED` 로 남는다. 다시 대조하면 버전이 오른다.

우리가 낸 도안(`LABEL_DESIGN`)과 **다른 종류**로 둔다. 하나는 우리가 낸
것이고 하나는 받은 것이라, 같은 칸에 쌓으면 어느 것이 정본인지 알 수 없다.

### ④ 도안 슬롯 — **완료**, 상태는 남음 (아래 "순서와 남은 것" 4번)

시안이 문서함에 자리를 얻었다. 남은 것은 **상태**다. 지금은

```
DRAFT / REQUESTING / SUBMITTED / REVIEW / PENDING / CONFIRMED
```

여섯인데 이 흐름에 필요한 **"도안 대조"** 단계가 없다. 표시사항 확정과 인쇄
승인은 다른 일이다. 문서 요청(`DocumentRequest`)으로 디자인 담당자에게 시안을
요청하고 회신되면 자동으로 대조 큐에 올리는 것도 남았다.

### ⑤ 배합비 엑셀 붙여넣기

회사가 엑셀로 배합비를 만드는 습관을 바꾸라고 하면 안 옮겨온다. BOM 탭에
엑셀 표를 그대로 붙여넣는 입력을 둔다. 영양성분 탭이 이미 Handsontable 을
쓰고 있어 같은 방식으로 된다.

### ⑥ 원재료명 조립

3번에서 사람이 가장 많이 손대는 것이 원재료명 문자열이다. 배합비 순서 ·
복합원재료 괄호 · 알레르기 문구를 규정대로 조립하는 것. 검증
(`check_ingredient_order_by_ratio`, `check_rawmtrl_brackets`)은 있지만
**BOM에서 그 문자열을 만들어 주는 쪽**이 약하다. 여기가 완성되면 3번이
"작성" 이 아니라 "확인" 이 된다.

### ⑦ 원료 규격서(엑셀·PDF) → BOM

지금 원료 입구는 사진(`ingredient_photo.py`)뿐인데 거래처는 엑셀·PDF 규격서를
보낸다. `vision_service.extract_pdf_text` 가 이미 있으니 그 통로를 넓힌다.

## 이미 5번에 쓰이는 것

- 도안 안의 중복 숫자 대조 — 앞면 박스와 표의 내용량·열량이 다르면 알린다
  (`ocr_repeats.py`)
- 규정 검증 지적이 표의 어느 줄인지 짚어 준다
- 사람 검수에서 나온 항목들 — 열량-영양성분 계산, 해동방법, 교환 안내,
  원산지 강조 표시

## 순서와 남은 것

| | 항목 | 상태 |
|---|---|---|
| 1 | ① 글자 내보내기 | **완료** — 내보내기 메뉴(PDF · 워드 · 텍스트) · 글자 복사 · 줄별 복사 |
| 2 | ② 대조 모드 | **완료** — 표시사항 탭의 "디자인 시안과 대조" |
| 3 | ③ 대조 기록 + ④ 도안 슬롯 | **완료** — 시안은 문서함 "포장지 시안" 으로, 결과는 그 파일에 |
| 4 | ④ 도안 대조 **상태** | 남음 — 워크플로에 그 단계가 없다. `DocumentRequest` 로 요청→회신→대조 큐 |
| 5 | ⑤ 배합비 엑셀 붙여넣기 | 남음 — 1번이 파일에서 시스템으로 넘어오는 입구 |
| 6 | ⑥ 원재료명 조립 | 남음 — 3번을 작성에서 확인으로 |
| 7 | ⑦ 규격서 파싱 | 남음 — 가장 크다. 앞의 것들이 자리잡은 뒤에 |

계획에는 없었지만 그 사이에 한 것 — 사람 검수 항목 4종을 검증에 옮김,
도안 안의 중복 숫자 대조, 검증 결과 창을 다시 짬(적합은 이름만), 지적 번호로
표의 줄을 짚기, 규정 검증(AI) 감춤, 시안 대조의 정확도·가시성(아래 작업 기록),
업소 항목(제조원·유통전문판매원) 오탐 줄이기, 문서함을 판 단위로 묶기.

## 이 흐름 밖에 남아 있는 것

업무 흐름 항목은 아니지만 아직 열려 있는 것들. 예전 `LABEL_IMPROVEMENT_STATUS.md`
에 있던 것 중 살아 있는 것을 여기로 옮겼다(그 문서는 닫았다 — 나머지는 git 이력에).

### A. 원료 중복 생성 — 앞으로만 막았다

운영 실측: 548건 중 겹치는 그룹 40개, 여분 108건(**19.7%**). 새로 만드는 쪽은
막았지만 **이미 쌓인 것은 그대로**다. 병합 도구가 없어서 사용자가 어느 것이
정본인지 고를 수가 없다.

### B. AI 한도 카운터가 파일 캐시에 있다 — 반나절

`ai_rate_limit.py` 의 일일·분당 카운터가 `FileBasedCache` 에 있고
`MAX_ENTRIES: 500` 이다. 항목이 넘치면 컬링으로 **카운터가 소멸해 한도가
초기화**될 수 있다. 검증 결과 캐시·농수산물 목록 캐시가 같은 500칸을 나눠 쓴다.
지금은 파일 30개라 여유가 있는 잠재 위험이다. DB 로 옮기면 끝난다.

### C. 제품 상세 탭들의 디자인 편차 — 1~2일

토큰과 공용 버튼 클래스가 **이미 있는데 아무도 안 쓴다**는 것이 핵심이다.
문서함은 오늘 정리했고(`.doc-modal`, `.doc-status-bar`, `.doc-type-tag`),
남은 것은 인라인 `style=` 이 많은 순서로:

| 템플릿 | 인라인 style= |
|---|---|
| `_tab_basic_info.html` | 124 |
| `_tab_permissions.html` | 49 |
| `product_detail.html` | 43 |

### D. `label_preview.html` 과 `label_preview.js` 의 중복 함수

같은 이름의 함수가 두 곳에 있다. 오늘까지 다섯 개를 한쪽으로 모았는데
(`exportToPDF`, 내보내기 일가, 분리배출 마크, `initNutritionData`) **아직 15개가
남았다** — `loadSavedPreviewSettings`, `setupEventListeners`,
`updatePreviewStyles` 등.

이것이 실제로 사고를 냈다. 중복을 지우면서 살아남은 쪽이 닿지 않는 코드였고
**PDF 저장이 통째로 죽었다.** 남은 것도 같은 방식으로 위험하니 한 번에 한 묶음씩,
지운 뒤 실제로 눌러 보고 넘어간다.

### E. 데이터 쪽 — 브라우니 케이크의 영양성분

표에 적힌 값이 서로 맞지 않는다. 탄수화물 9 g + 지방 4.5 g + 단백질 1 g 이면
80.5 kcal 이고 전부 지방이라 해도 130 kcal 인데 309 kcal 로 인쇄돼 있다.
비율이 3.8배라 **어느 기준으로 환산해도 맞출 수 없다** — 도안 자체가 틀렸다.
게다가 DB 에는 열량만 100 g 기준으로 바뀌고 다량영양소는 65 g 기준으로 남아
기준이 섞여 있다. 시스템이 고칠 일이 아니라 사용자가 정할 일이다.

## 파일을 주고받는 비용

### 내보내기 — 워드 표로

| 방법 | 품이 | 새 의존성 | 비고 |
|---|---|---|---|
| 탭 구분 텍스트 (**지금 됨**) | 0 | 없음 | 워드·엑셀에 붙이면 그대로 표가 된다 |
| HTML 을 `.doc` 로 내려주기 | 반나절 | 없음 | 워드가 HTML 표를 연다. 서식·병합까지 된다 |
| `.docx` 정식 생성 | 하루 | `python-docx` | 회사 양식·로고·머리말이 필요할 때 |
| 회사 양식 `.docx` 에 값만 채우기 | 2~3일 | `docxtpl` | 양식이 이미 있고 그대로 써야 할 때 |

**대부분은 두 번째 줄이면 끝난다.** 붙여넣기가 번거롭다는 말이 나올 때만
`.docx` 로 올라가면 된다.

### 읽어오기

| 형식 | 난이도 | 비용 | 왜 |
|---|---|---|---|
| 엑셀 `.xlsx` | 낮음 | **이미 있음** (`openpyxl`) | 셀 값이 구조화돼 있다 |
| PDF (글자가 든 것) | 낮음 | **이미 있음** (`PyMuPDF`) | `vision_service.extract_pdf_text` 가 이미 돈다 |
| PDF (스캔·사진) | 중간 | 호출 비용 | 이미 그 경로가 있다 (`_pdf_to_base64_images`) |
| 워드 `.docx` | 중간 | `python-docx` | zip+XML 이라 표 읽기는 쉽다 |
| 워드 `.doc` (구형) | **높음** | LibreOffice 설치 | 바이너리 포맷. 파이썬만으로는 사실상 못 읽는다 |
| 한글 `.hwp` | **높음** | 별도 | 국내 문서라면 실제로 마주친다 |

**진짜 비용은 파싱이 아니라 "양식이 제각각인 것"이다.** 배합비 엑셀은 회사마다,
담당자마다 열 순서와 머리글 이름이 다르다. 여기에 세 가지 길이 있다.

1. **우리 양식 고정** — 양식을 내려받아 채워 올리게 한다. 1~2일. 대신
   사용자가 자기 양식을 버려야 한다
2. **열 매핑 화면** — 올린 파일의 열을 보여 주고 "이 열이 원료명, 이 열이
   함량" 을 한 번 지정하게 하고 **회사별로 기억한다.** 3~5일.
   실무에서 가장 잘 통한다
3. **AI 로 알아서 읽기** — 이미 사진 판독과 문서 AI 검토가 하는 일이다.
   품은 적지만 호출 비용이 계속 들고 틀릴 수 있어 확인 창이 필수다.
   그 확인 창은 이미 있다(사진 불러오기)

권하는 조합 — **엑셀은 (2) 열 매핑, PDF·사진은 (3) AI + 기존 확인 창.**
워드는 우선순위가 낮다. 배합비는 대개 엑셀이고, 워드는 우리가 만들어 내보낼
쪽이라 읽을 일이 적다.

`.doc` 와 `.hwp` 는 지원하지 않기를 권한다. "PDF 로 저장해서 올려 주세요" 가
훨씬 싸고, 그 경로는 이미 있다.

---

## 작업 기록

### 2026-09-05 — 시안 대조를 쓸 수 있게, 문서함을 판 단위로

**시안 대조 (`b9f24d9`)** — 실제 시안을 견줘 봤더니 여덟 줄이 "다름" 인데 그
절반이 쉼표 뒤 공백 차이였다. 인쇄물에서 그건 조판이 정하는 것이지 표시 내용이
아니고, 그런 것이 쌓이면 **진짜 다른 두 줄이 그 안에 묻힌다.**

- 등급을 넷으로: 같음 / 띄어쓰기만 다름 / 일부만 읽힘 / 다름. 펼치는 것은 마지막 하나
- 300자짜리 원재료명은 앞뒤로 같은 부분을 잘라 내고 **달라진 자리만** 노랗게
- 내 값보다 눈에 띄게 짧게 읽힌 항목이 절반을 넘으면 "표시사항 표 부분만 잘라
  다시 올리세요" 를 먼저 말한다. 시안은 표시사항이 그림 한구석에 작게 들어
  있어서 전체를 올리면 절반만 읽힌다 — 시안이 틀린 게 아니라 우리가 덜 읽은 것
- "1일 영양성분 기준치 … 2000 kcal" 은 상용 문구다. 이걸 세는 바람에 모든
  라벨이 "앞면 박스와 열량이 다릅니다" 로 지적됐다
- 대조 줄이 채우는 창의 다섯 칸 틀에 들어가 **한 칸씩 밀려 있었다** — 항목명이
  22px 칸에서 한 글자씩 세로로 쌓이고 시안 값이 62px 칸에 눌렸다 (`6087398`)

**업소 항목 (`6087398`, `38fc1c9`)** — 제조원·유통전문판매원·소분원·수입원은
거의 언제나 다른 회사인데 라벨에서는 한 표 안에 붙어 찍히고 넷이 전부
"업체명 + 주소" 로 똑같이 생겼다. 값만 보고는 어느 칸의 것인지 알 수 없다.

- `ocr_company.py` — 한 칸에 뭉쳐 온 것을 가르고, 두 칸에 같은 회사가 들어온
  것과 한쪽이 빠진 것을 짚는다. **지우지는 않는다**
- 식약처 등록 **업소명은 제조원**이고 등록 정보에 주소가 없다. 견줄 때
  **업체명 부분만** 본다 — 예전에는 주소까지 통째로 견줘서 짧은 업소명이
  지명에 걸렸다("대한" 이 "충청남도 대한로" 에)
- 등록 업소명이 제조원 아닌 칸에서 발견되면 자리가 바뀐 것이다. 값만 봐서는
  알 수 없고 등록 정보가 있어야 드러난다
- 그래도 못 잡는 경우가 있었다. 실제 라벨은 항목 이름이 왼쪽 칸에서 두 줄로
  접히고(유통전문/판매원), 로고가 이름과 회사 사이에 끼고, 유통전문판매원이
  제조원보다 위에 있다. 판독이 눈에 먼저 띄는 회사를 두 칸에 넣었다 →
  **수상하면 그 네 줄만 다시 읽는다.** 틀린 낌새가 보일 때만 돌므로 평소
  비용은 그대로다 (`OCR_COMPANY_RECHECK`, 기본 켬)

**문서함 (`d98e650`, `4187325`, `4fdfde7`)** — 같은 문서가 목록에 다섯 번
나왔다. 다섯 개의 문서가 아니라 한 문서의 다섯 판이다.

- 최신 판만 한 줄에 놓고 예전 판은 접어 둔다. 판 사슬은 **줄로 이어져 있어서**
  (v1 ← v2 ← v3) 끝까지 거슬러 올라가야 하고, 사슬이 끊긴 것은 판 번호로 잇는다
- 준수율 바가 도넛 + 두 줄짜리 칩으로 60px 을 먹었다 → `1/5` + 가는 막대 +
  한 줄 칩, 40px. **갖춘 것은 조용히 둔다** — 다섯이 다 색을 쓰면 어느 것이
  문제인지 알 수 없다
- 문서 모달 넷이 모서리·머리글·제목·단추가 저마다 달랐다 → `.doc-modal` 하나로
- 문서 종류를 고르고 파일을 끌어다 놓으면 **고른 종류가 지워졌다.** 끌기
  시작하면 화면 전체를 덮는 오버레이가 떠서 창 안의 드롭 영역이 파일을 받을 수
  없고, window 의 drop 이 창을 다시 여는데 그 함수가 폼을 초기화한다
- 거르개(전체/필수/만료 임박)의 활성 표시가 한 번도 안 켜졌다 — JS 가 찾는
  `.filter-pills` class 가 markup 에 없었다

**그 밖에** — 영양성분이 화면에 보이는데 "비어 있습니다" 라고 하던 것(탭을
떠날 때의 저장을 기다리지 않았다), 규정 검증 단추 색 강조, 미리보기 콘솔에서
열 때마다 뜨던 경고 셋(없는 것이 정상인 요소를 찾고 있었다).

### 2026-09-04 — 내보내기·대조 모드·업무 단계

`e37af50` 적합한 열여덟 줄이 봐야 할 두 줄을 덮고 있었다 ·
`82dd305` 규정 검증(AI) 감춤, 겹쳐 있던 확대 단추 ·
`167a3d6` 내보내기를 한 자리로(PDF · 워드 · 텍스트) ·
`97820fc` 시안 대조에 채우는 도구를 쓰고 있었다 ·
`1adf089` 5단계 도구가 1단계 자리에 있었다 — 시안 대조를 표시사항 탭으로